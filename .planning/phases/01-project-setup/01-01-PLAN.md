---
phase: 01-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/backend/app.py
  - fp-cercador/backend/requirements.txt
  - fp-cercador/backend/.env.example
  - fp-cercador/.gitignore
  - fp-cercador/backend/scrapers/__init__.py
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03

must_haves:
  truths:
    - "El directori fp-cercador/backend/ existeix amb els seus subdirectoris (scrapers/, data/)"
    - "app.py es pot importar sense errors d'importació (assumint dependències instal·lades)"
    - "requirements.txt conté exactament les 6 dependències declarades a PROJ-03"
    - ".gitignore exclou .env i la cache Python"
    - "scrapers/ és un paquet Python importable (té __init__.py)"
  artifacts:
    - path: "fp-cercador/backend/app.py"
      provides: "Flask app stub amb CORS i dotenv, sense rutes"
      contains: "load_dotenv()"
    - path: "fp-cercador/backend/requirements.txt"
      provides: "Llista plana de 6 dependències Python"
      contains: "flask"
    - path: "fp-cercador/backend/.env.example"
      provides: "Plantilla del fitxer .env amb ADMIN_TOKEN"
      contains: "ADMIN_TOKEN="
    - path: "fp-cercador/.gitignore"
      provides: "Exclusions de git per secrets i cache Python"
      contains: ".env"
    - path: "fp-cercador/backend/scrapers/__init__.py"
      provides: "Marcador de paquet Python per scrapers/"
  key_links:
    - from: "fp-cercador/backend/app.py"
      to: "fp-cercador/backend/.env.example"
      via: "load_dotenv() carrega variables del fitxer .env (còpia de .env.example)"
      pattern: "load_dotenv\\(\\)"
    - from: "fp-cercador/.gitignore"
      to: "fp-cercador/backend/.env"
      via: "patró .env al .gitignore exclou el fitxer real del repositori"
      pattern: "^\\.env$"
---

<objective>
Crear l'esquelet Python de `fp-cercador/backend/`: el stub Flask (`app.py`),
la declaració de dependències (`requirements.txt`), la plantilla de secrets
(`.env.example`), el marcador de paquet per `scrapers/` i el `.gitignore` arrel.

Purpose: Estableix la capa de configuració i el punt d'entrada del backend
perquè qualsevol desenvolupador pugui clonar el repositori i arrencar el
servidor Flask després d'un sol `pip install -r requirements.txt`. Les fases
2-4 afegiran lògica; aquesta fase només declara l'esquelet.

Output: 5 fitxers de configuració Python + 1 directori empaquetat (`scrapers/`
amb `__init__.py`) + directori buit `data/` (creat com a prerequisit).
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/phases/01-project-setup/01-PATTERNS.md

<interfaces>
<!-- Cap interface pre-existent: projecte de zero. -->
<!-- Els contractes d'aquest plan són els que defineixen els patrons per a totes les fases posteriors. -->

Contracte app.py (stub — Fase 4 afegirà rutes):
```python
# Importacions fixes — NO afegir res més en aquesta fase
from flask import Flask
from flask_cors import CORS
from dotenv import load_dotenv

load_dotenv()               # Ha d'anar abans de qualsevol os.getenv()
app = Flask(__name__)
CORS(app)                   # Wildcard origins per defecte

if __name__ == '__main__':
    app.run(debug=True)
```

Contracte requirements.txt (llista plana, 6 línies, sense versions):
```
flask
flask-cors
pdfplumber
requests
beautifulsoup4
python-dotenv
```

Contracte .env.example (exactament 1 línia segons D-07):
```
ADMIN_TOKEN=canvia-aquest-token-per-un-de-segur
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear arrel del projecte i estructura de directoris backend</name>
  <files>
    - fp-cercador/backend/scrapers/__init__.py
    - fp-cercador/backend/data/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-project-setup/01-CONTEXT.md (decisions D-01, D-02)
    - .planning/phases/01-project-setup/01-PATTERNS.md (secció "Estructura de directoris arrel")
  </read_first>
  <action>
    Crear tota l'estructura de directoris del backend i el marcador de paquet
    Python per a `scrapers/`. Executar aquests comandaments des de
    `/Users/rogermasellas/AI/Cercador Graus/`:

    ```bash
    mkdir -p fp-cercador/backend/scrapers
    mkdir -p fp-cercador/backend/data
    ```

    Llavors crear `fp-cercador/backend/scrapers/__init__.py` amb el contingut
    exacte següent (un comentari, sense codi):

    ```python
    # Paquet scrapers — contingut a la Fase 2
    ```

    Llavors crear `fp-cercador/backend/data/.gitkeep` com a fitxer buit (0 bytes)
    perquè git pugui rastrejar el directori. Aquest fitxer es pot eliminar quan
    Plan 02 afegeixi `ofertes.json`, però el creem ara per garantir que el
    directori es commeteja encara que ofertes.json arribi més tard.

    RAONAMENT: L'absència de `__init__.py` provoca `ModuleNotFoundError` a la
    Fase 2 quan es faci `from scrapers.pdf_scraper import ...` (RESEARCH.md
    Pitfall 1). El fitxer ha de ser creat ara, no més tard.
  </action>
  <verify>
    <automated>test -d "fp-cercador/backend/scrapers" && test -d "fp-cercador/backend/data" && test -f "fp-cercador/backend/scrapers/__init__.py" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Directori `fp-cercador/backend/scrapers/` existeix (test -d retorna 0)
    - Directori `fp-cercador/backend/data/` existeix (test -d retorna 0)
    - Fitxer `fp-cercador/backend/scrapers/__init__.py` existeix (test -f retorna 0)
    - `fp-cercador/backend/scrapers/__init__.py` conté exactament la línia `# Paquet scrapers — contingut a la Fase 2` (verificable amb `grep -q "Paquet scrapers" fp-cercador/backend/scrapers/__init__.py`)
    - Comandament `python3 -c "import sys; sys.path.insert(0, 'fp-cercador/backend'); import scrapers; print('OK')"` imprimeix `OK` sense errors
  </acceptance_criteria>
  <done>
    Estructura de directoris del backend creada, scrapers/ és importable com a
    paquet Python, data/ existeix llest per a ofertes.json de Plan 02.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear app.py stub (Flask + CORS + dotenv)</name>
  <files>
    - fp-cercador/backend/app.py
  </files>
  <read_first>
    - .planning/phases/01-project-setup/01-CONTEXT.md (decisions D-04)
    - .planning/phases/01-project-setup/01-RESEARCH.md (§ Pattern 1: Flask app stub mínim, línies 112-132; § Pitfall 2: Ordre de load_dotenv())
    - .planning/phases/01-project-setup/01-PATTERNS.md (secció "fp-cercador/backend/app.py")
  </read_first>
  <action>
    Crear `fp-cercador/backend/app.py` amb el contingut EXACTE següent. NO
    afegir cap ruta, cap import addicional, cap configuració més enllà del
    que hi ha aquí:

    ```python
    from flask import Flask
    from flask_cors import CORS
    from dotenv import load_dotenv

    load_dotenv()

    app = Flask(__name__)
    CORS(app)

    if __name__ == '__main__':
        app.run(debug=True)
    ```

    RESTRICCIONS (aplicades per D-04 i PATTERNS.md "Anti-patrons a evitar"):
    - NO afegir cap `@app.route(...)` — les rutes són responsabilitat de la Fase 4
    - NO afegir `app.config['SECRET_KEY']` ni cap altra config
    - NO cridar `os.getenv('ADMIN_TOKEN')` aquí — no hi ha cap ruta que ho necessiti
    - `load_dotenv()` HA d'anar abans de `Flask(__name__)` (ordre documentat de python-dotenv; RESEARCH.md Pitfall 2)
    - `CORS(app)` sense arguments (wildcard origins per a dev; RESEARCH.md State of the Art)
    - Usar exactament 4 espais d'indentació Python (PEP 8)
  </action>
  <verify>
    <automated>test -f "fp-cercador/backend/app.py" && grep -q "^from flask import Flask$" fp-cercador/backend/app.py && grep -q "^from flask_cors import CORS$" fp-cercador/backend/app.py && grep -q "^from dotenv import load_dotenv$" fp-cercador/backend/app.py && grep -q "^load_dotenv()$" fp-cercador/backend/app.py && grep -q "^app = Flask(__name__)$" fp-cercador/backend/app.py && grep -q "^CORS(app)$" fp-cercador/backend/app.py && grep -q "app.run(debug=True)" fp-cercador/backend/app.py && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Fitxer `fp-cercador/backend/app.py` existeix
    - Conté literalment la línia `from flask import Flask` (grep retorna match)
    - Conté literalment la línia `from flask_cors import CORS`
    - Conté literalment la línia `from dotenv import load_dotenv`
    - Conté literalment la línia `load_dotenv()` (sense arguments)
    - Conté literalment la línia `app = Flask(__name__)`
    - Conté literalment la línia `CORS(app)`
    - Conté `app.run(debug=True)` dins d'un bloc `if __name__ == '__main__':`
    - NO conté cap `@app.route` (grep `"@app.route"` retorna 0 matches)
    - NO conté cap `os.getenv` (grep retorna 0 matches)
    - L'ordre és: load_dotenv() ABANS de Flask(__name__) (verificable mirant els números de línia: `grep -n "load_dotenv()" fp-cercador/backend/app.py` < `grep -n "Flask(__name__)" fp-cercador/backend/app.py`)
  </acceptance_criteria>
  <done>
    `app.py` conté exactament el stub mínim de 8 línies útils (4 imports, 1
    crida a load_dotenv, 2 línies d'app + CORS, bloc __main__). Fitxer llest
    perquè la Fase 4 afegeixi rutes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Crear requirements.txt, .env.example i .gitignore</name>
  <files>
    - fp-cercador/backend/requirements.txt
    - fp-cercador/backend/.env.example
    - fp-cercador/.gitignore
  </files>
  <read_first>
    - .planning/phases/01-project-setup/01-CONTEXT.md (decisions D-07, D-10)
    - .planning/REQUIREMENTS.md (PROJ-02, PROJ-03)
    - .planning/phases/01-project-setup/01-RESEARCH.md (§ Code Examples: .gitignore complet recomanat, línies 212-241; Pattern 2: Estructura del requirements.txt, línies 134-146; Pitfall 4: artefactes PDF de Fase 2)
    - .planning/phases/01-project-setup/01-PATTERNS.md (seccions requirements.txt, .env.example, .gitignore)
  </read_first>
  <action>
    Crear 3 fitxers amb contingut EXACTE:

    (a) `fp-cercador/backend/requirements.txt` — 6 línies exactes, una per
    dependència, sense versions fixes, sense comentaris, sense línies en blanc:

    ```
    flask
    flask-cors
    pdfplumber
    requests
    beautifulsoup4
    python-dotenv
    ```

    (b) `fp-cercador/backend/.env.example` — contingut literal segons D-07
    (exactament 1 línia, amb newline final):

    ```
    ADMIN_TOKEN=canvia-aquest-token-per-un-de-segur
    ```

    (c) `fp-cercador/.gitignore` — contingut complet segons D-10 + entrades
    addicionals de discreció del planificador per anticipar artefactes futurs
    (PDFs descarregats a Fase 2, fitxers d'IDE). `ofertes.json` NO s'exclou
    (D-09 explícit). Contingut exacte:

    ```gitignore
    # Secrets — mai al repositori
    .env

    # Cache Python
    __pycache__/
    *.pyc
    *.pyo
    *.pyd
    .Python

    # Entorns virtuals
    venv/
    .venv/
    env/
    .env.bak

    # Paquets instal·lats
    *.egg-info/
    dist/
    build/

    # PDFs descarregats (artefactes de Fase 2-3, poden ser grans)
    *.pdf

    # IDE
    .DS_Store
    .idea/
    .vscode/
    ```

    IMPORTANT: NO afegir `ofertes.json` ni `data/*.json` al .gitignore — la
    decisió D-09 és explícita que el fitxer de mostra s'ha d'incloure al repo.
  </action>
  <verify>
    <automated>test -f "fp-cercador/backend/requirements.txt" && test -f "fp-cercador/backend/.env.example" && test -f "fp-cercador/.gitignore" && [ "$(grep -c . fp-cercador/backend/requirements.txt)" = "6" ] && grep -q "^flask$" fp-cercador/backend/requirements.txt && grep -q "^flask-cors$" fp-cercador/backend/requirements.txt && grep -q "^pdfplumber$" fp-cercador/backend/requirements.txt && grep -q "^requests$" fp-cercador/backend/requirements.txt && grep -q "^beautifulsoup4$" fp-cercador/backend/requirements.txt && grep -q "^python-dotenv$" fp-cercador/backend/requirements.txt && grep -q "^ADMIN_TOKEN=canvia-aquest-token-per-un-de-segur$" fp-cercador/backend/.env.example && grep -q "^\.env$" fp-cercador/.gitignore && grep -q "^__pycache__/$" fp-cercador/.gitignore && grep -q "^\*.pyc$" fp-cercador/.gitignore && grep -q "^venv/$" fp-cercador/.gitignore && ! grep -q "ofertes\.json" fp-cercador/.gitignore && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `fp-cercador/backend/requirements.txt` existeix i conté EXACTAMENT 6 línies no buides (`grep -c . retorna 6`)
    - Cadascuna d'aquestes línies apareix al fitxer: `flask`, `flask-cors`, `pdfplumber`, `requests`, `beautifulsoup4`, `python-dotenv` (grep `-q "^<nom>$"` retorna 0 per cadascuna)
    - Cap línia del requirements.txt conté `==`, `>=`, `<=` o `~=` (sense versions; `grep -E "==|>=|<=|~=" fp-cercador/backend/requirements.txt` retorna 0 matches)
    - `fp-cercador/backend/.env.example` conté exactament la línia `ADMIN_TOKEN=canvia-aquest-token-per-un-de-segur`
    - `fp-cercador/.gitignore` existeix i conté TOTES aquestes entrades obligatòries (D-10): `.env`, `__pycache__/`, `*.pyc`, `*.pyo`, `*.pyd`, `.Python`, `venv/`, `.venv/`, `*.egg-info/` (cada patró verificable amb grep exacte)
    - `fp-cercador/.gitignore` NO conté `ofertes.json` ni `data/*.json` (D-09 explícit; `grep -q "ofertes" fp-cercador/.gitignore` retorna no-match)
    - `fp-cercador/.gitignore` conté `*.pdf` (anticipació d'artefactes de Fase 2; RESEARCH.md Pitfall 4)
  </acceptance_criteria>
  <done>
    Els 3 fitxers de configuració existeixen amb el contingut exacte. Un
    `pip install -r fp-cercador/backend/requirements.txt --dry-run` pot
    resoldre els 6 paquets. El .gitignore protegeix secrets i cache sense
    bloquejar el fitxer de mostra `ofertes.json` que arribarà a Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositori git ↔ disc local | Secrets reals (.env amb ADMIN_TOKEN valor real) mai han de creuar aquesta frontera cap al repositori |
| Fitxers de configuració ↔ runtime Flask | `app.py` carrega `.env` via python-dotenv — si `.env` es commeteja, el token queda exposat a tothom amb accés al repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | fp-cercador/backend/.env (fitxer real, no exemple) | mitigate | `.gitignore` conté `^\.env$` explícit (Task 3). Només `.env.example` (plantilla amb valor placeholder llegible) va al repo. PROJ-02 exigeix aquesta exclusió. |
| T-01-02 | Information Disclosure | fp-cercador/backend/.env.example | accept | El fitxer conté únicament el placeholder `canvia-aquest-token-per-un-de-segur`, no un secret real. Risc zero si es publica. |
| T-01-03 | Tampering | fp-cercador/backend/app.py debug mode | accept | `debug=True` queda al codi perquè l'ús és només local (`python app.py`). En desplegament CloudPanel s'usarà Gunicorn/uWSGI (fase fora d'abast). Risc acceptat en entorn de desenvolupament. |
| T-01-04 | Information Disclosure | fp-cercador/backend/scrapers/__init__.py | accept | Fitxer de marcador sense secrets ni lògica. Contingut públic per disseny. |
</threat_model>

<verification>
## Smoke tests del plan sencer (després de Tasks 1-3)

```bash
# Estructura
test -d fp-cercador/backend
test -d fp-cercador/backend/scrapers
test -d fp-cercador/backend/data
test -f fp-cercador/backend/app.py
test -f fp-cercador/backend/requirements.txt
test -f fp-cercador/backend/.env.example
test -f fp-cercador/backend/scrapers/__init__.py
test -f fp-cercador/.gitignore

# Compte de dependències (ha de ser exactament 6)
[ "$(grep -c . fp-cercador/backend/requirements.txt)" = "6" ]

# .env està exclòs (PROJ-02)
grep -q "^\.env$" fp-cercador/.gitignore

# app.py té l'ordre correcte (load_dotenv abans de Flask)
python3 -c "
with open('fp-cercador/backend/app.py') as f:
    content = f.read()
lines = content.split('\n')
load_line = next(i for i, l in enumerate(lines) if 'load_dotenv()' in l and 'import' not in l)
flask_line = next(i for i, l in enumerate(lines) if 'Flask(__name__)' in l)
assert load_line < flask_line, 'load_dotenv() must precede Flask(__name__)'
print('Order OK')
"

# pip resolució (opcional, pot no tenir connectivitat)
pip install -r fp-cercador/backend/requirements.txt --dry-run 2>&1 | grep -i "would install" || echo "dry-run skipped (no connectivity)"
```
</verification>

<success_criteria>
- [ ] Els 5 fitxers + 2 directoris buits (scrapers/, data/) existeixen exactament a les rutes esperades
- [ ] `app.py` conté les 4 importacions i les 4 crides exigides per D-04, en l'ordre correcte
- [ ] `requirements.txt` té exactament 6 línies no buides amb els 6 noms de paquet literals
- [ ] `.env.example` conté exactament `ADMIN_TOKEN=canvia-aquest-token-per-un-de-segur`
- [ ] `.gitignore` exclou tots els patrons de D-10 + `*.pdf` + entrades d'IDE
- [ ] `.gitignore` NO exclou `ofertes.json` (D-09)
- [ ] `scrapers/` és un paquet Python importable (té `__init__.py`)
</success_criteria>

<output>
Després de completar, crear `.planning/phases/01-project-setup/01-01-SUMMARY.md`
seguint la plantilla `summary.md`. Incloure:
- Llista dels fitxers creats amb els seus tamanys
- Confirmació de l'ordre `load_dotenv()` → `Flask(__name__)` a `app.py`
- Compte exacte de dependències a `requirements.txt` (ha de ser 6)
- Nota que `data/.gitkeep` es pot eliminar quan Plan 02 afegeixi `ofertes.json`
</output>
