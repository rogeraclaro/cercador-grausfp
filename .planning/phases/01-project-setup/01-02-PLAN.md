---
phase: 01-project-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/README.md
  - fp-cercador/frontend/index.html
  - fp-cercador/frontend/admin.html
  - fp-cercador/backend/data/ofertes.json
autonomous: true
requirements:
  - PROJ-01

must_haves:
  truths:
    - "El directori fp-cercador/frontend/ existeix amb index.html i admin.html"
    - "index.html és un HTML vàlid amb lang='ca', charset UTF-8 i comentari TODO Phase 5"
    - "admin.html és un HTML vàlid amb lang='ca', charset UTF-8 i comentari TODO Phase 6"
    - "ofertes.json és un array JSON vàlid amb registres dels 5 Grados (A, B, C, D, E)"
    - "README.md existeix amb els 4 passos de setup en ordre correcte"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: "Stub HTML vàlid per al cercador (Fase 5)"
      contains: "lang=\"ca\""
    - path: "fp-cercador/frontend/admin.html"
      provides: "Stub HTML vàlid per al panell admin (Fase 6)"
      contains: "lang=\"ca\""
    - path: "fp-cercador/backend/data/ofertes.json"
      provides: "Dades de mostra cobrind els 5 Grados per al desenvolupament del frontend"
      contains: "\"grado\""
    - path: "fp-cercador/README.md"
      provides: "Instruccions de setup per a nous desenvolupadors"
      contains: "ADMIN_TOKEN"
  key_links:
    - from: "fp-cercador/README.md"
      to: "fp-cercador/backend/.env.example"
      via: "README documenta cp backend/.env.example backend/.env com a primer pas"
      pattern: "\.env\.example"
    - from: "fp-cercador/backend/data/ofertes.json"
      to: "fp-cercador/frontend/index.html"
      via: "El frontend (Fase 5) consumirà ofertes.json via GET /api/ofertes"
      pattern: "\"grado\""
---

<objective>
Crear els fitxers del frontend stub i les dades de mostra de `fp-cercador/`:
`README.md` a l'arrel, `frontend/index.html` i `frontend/admin.html` com a
stubs HTML vàlids, i `backend/data/ofertes.json` amb 7 registres de mostra
que cobreixen els 5 Grados.

Purpose: Completa l'esquelet del projecte iniciat al Plan 01. El frontend
stub permet verificar l'estructura HTML sense lògica; `ofertes.json` permet
provar els filtres del cercador a la Fase 5 sense haver d'executar el
pipeline de scraping. El README fa el projecte clonable per a nous
desenvolupadors.

Output: 4 fitxers nous. Cap lògica de negoci — tot son stubs i dades de
mostra amb el schema correcte per a les fases posteriors.
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup/01-CONTEXT.md
@.planning/phases/01-project-setup/01-RESEARCH.md
@.planning/phases/01-project-setup/01-PATTERNS.md

<interfaces>
<!-- Cap interface pre-existent: projecte de zero. -->
<!-- Contractes derivats de les decisions D-03, D-05, D-06, D-07, D-08, D-09 i REQUIREMENTS.md DATA-01 -->

Schema ofertes.json (DATA-01 de REQUIREMENTS.md) — cada registre HA de tenir EXACTAMENT aquests camps:
```json
{
  "id": <integer, correlatiu, únic>,
  "grado": <"A" | "B" | "C" | "D" | "E">,
  "nivel": <1 | 2 | 3 | null>,
  "familia": <string>,
  "codigo": <string | null>,
  "denominacion": <string>,
  "plan_antiguo": <boolean>,
  "observaciones": <string>
}
```

Valors especials per Grado (del REQUIREMENTS.md HTML-06 i PATTERNS.md):
- Grado D: "codigo": null, "plan_antiguo": false
- Grado E: "codigo": null, "nivel": null, "plan_antiguo": false
- Grado C (exemple pla antic): "plan_antiguo": true, "observaciones": "(Plan antiguo)"

Stub HTML vàlid (D-05 del CONTEXT.md):
```html
<!DOCTYPE html>
<html lang="ca">
<head>
  <meta charset="UTF-8">
  <title>[títol específic per fitxer]</title>
</head>
<body>
  <!-- TODO: Phase N -->
</body>
</html>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear stubs HTML del frontend (index.html i admin.html)</name>
  <files>
    fp-cercador/frontend/index.html
    fp-cercador/frontend/admin.html
  </files>
  <read_first>
    - .planning/phases/01-project-setup/01-CONTEXT.md (decisions D-03, D-05)
    - .planning/phases/01-project-setup/01-PATTERNS.md (seccions "frontend/index.html" i "frontend/admin.html")
  </read_first>
  <action>
    Primer crear el directori si no existeix:

    ```bash
    mkdir -p fp-cercador/frontend
    ```

    Crear `fp-cercador/frontend/index.html` amb el contingut EXACTE
    (respectant indentació de 2 espais, newline final):

    ```html
    <!DOCTYPE html>
    <html lang="ca">
    <head>
      <meta charset="UTF-8">
      <title>Cercador FP España</title>
    </head>
    <body>
      <!-- TODO: Phase 5 -->
    </body>
    </html>
    ```

    Crear `fp-cercador/frontend/admin.html` amb el contingut EXACTE
    (diferència: títol "Administració" i comentari "Phase 6"):

    ```html
    <!DOCTYPE html>
    <html lang="ca">
    <head>
      <meta charset="UTF-8">
      <title>Cercador FP — Administració</title>
    </head>
    <body>
      <!-- TODO: Phase 6 -->
    </body>
    </html>
    ```

    RESTRICCIONS (D-05 i PATTERNS.md "Anti-patrons a evitar"):
    - `lang="ca"` obligatori — NO `lang="es"` ni `lang="en"`
    - `charset="UTF-8"` obligatori — caràcters catalans (·, à, è, í, etc.)
    - Comentari `<!-- TODO: Phase 5 -->` a index.html (no Phase 6)
    - Comentari `<!-- TODO: Phase 6 -->` a admin.html (no Phase 5)
    - Cap CSS, cap JavaScript, cap `<link>`, cap `<script>` en fase 1 — tot va a Fases 5 i 6
    - No són fitxers de 0 bytes (D-05 explícit)
  </action>
  <verify>
    <automated>test -f "fp-cercador/frontend/index.html" &amp;&amp; test -f "fp-cercador/frontend/admin.html" &amp;&amp; grep -q 'lang="ca"' fp-cercador/frontend/index.html &amp;&amp; grep -q 'lang="ca"' fp-cercador/frontend/admin.html &amp;&amp; grep -q 'charset="UTF-8"' fp-cercador/frontend/index.html &amp;&amp; grep -q 'charset="UTF-8"' fp-cercador/frontend/admin.html &amp;&amp; grep -q 'TODO: Phase 5' fp-cercador/frontend/index.html &amp;&amp; grep -q 'TODO: Phase 6' fp-cercador/frontend/admin.html &amp;&amp; echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `fp-cercador/frontend/index.html` existeix i NO és un fitxer de 0 bytes (`test -s` retorna 0)
    - `fp-cercador/frontend/admin.html` existeix i NO és un fitxer de 0 bytes
    - Ambdós fitxers contenen `lang="ca"` (grep exacte)
    - Ambdós fitxers contenen `charset="UTF-8"` (grep exacte)
    - `index.html` conté `&lt;!DOCTYPE html&gt;` a la primera línia
    - `admin.html` conté `&lt;!DOCTYPE html&gt;` a la primera línia
    - `index.html` conté el comentari `TODO: Phase 5` (NO Phase 6)
    - `admin.html` conté el comentari `TODO: Phase 6` (NO Phase 5)
    - Cap fitxer conté `&lt;script` ni `&lt;link` ni `&lt;style` (grep retorna 0 matches per a cadascun)
  </acceptance_criteria>
  <done>
    Dos stubs HTML vàlids creats a `fp-cercador/frontend/`. Cada fitxer té
    DOCTYPE, `lang="ca"`, `charset="UTF-8"` i un comentari TODO que indica
    la fase on es completarà. Cap CSS ni JS — els stubs seran completats a
    les Fases 5 i 6.
  </done>
</task>

<task type="auto">
  <name>Task 2: Crear ofertes.json (mostra) i README.md</name>
  <files>
    fp-cercador/backend/data/ofertes.json
    fp-cercador/README.md
  </files>
  <read_first>
    - .planning/phases/01-project-setup/01-CONTEXT.md (decisions D-06, D-07, D-08, D-09)
    - .planning/REQUIREMENTS.md (DATA-01, DATA-02)
    - .planning/phases/01-project-setup/01-RESEARCH.md (§ Pattern 3: Schema de ofertes.json, línies 152-169; § Registres de mostra, línies 245-319)
    - .planning/phases/01-project-setup/01-PATTERNS.md (seccions "ofertes.json", "README.md")
  </read_first>
  <action>
    Crear `fp-cercador/backend/data/ofertes.json` amb el contingut EXACTE
    següent (7 registres, cobertura dels 5 Grados — A, B, C, D×3, E):

    ```json
    [
      {
        "id": 1,
        "grado": "A",
        "nivel": 1,
        "familia": "Administració i Gestió",
        "codigo": "ADGG0108",
        "denominacion": "Operacions auxiliars de serveis administratius i generals",
        "plan_antiguo": false,
        "observaciones": ""
      },
      {
        "id": 2,
        "grado": "B",
        "nivel": 2,
        "familia": "Informàtica i Comunicacions",
        "codigo": "IFCT0209",
        "denominacion": "Sistemes microinformàtics",
        "plan_antiguo": false,
        "observaciones": ""
      },
      {
        "id": 3,
        "grado": "C",
        "nivel": 3,
        "familia": "Sanitat",
        "codigo": "SANT0208",
        "denominacion": "Atenció sanitària",
        "plan_antiguo": true,
        "observaciones": "(Plan antiguo)"
      },
      {
        "id": 4,
        "grado": "D",
        "nivel": 1,
        "familia": "Agrària",
        "codigo": null,
        "denominacion": "Agrojardineria i composicions florals",
        "plan_antiguo": false,
        "observaciones": ""
      },
      {
        "id": 5,
        "grado": "D",
        "nivel": 2,
        "familia": "Electricitat i Electrònica",
        "codigo": null,
        "denominacion": "Instal·lacions elèctriques i automàtiques",
        "plan_antiguo": false,
        "observaciones": ""
      },
      {
        "id": 6,
        "grado": "D",
        "nivel": 3,
        "familia": "Informàtica i Comunicacions",
        "codigo": null,
        "denominacion": "Administració de sistemes informàtics en xarxa",
        "plan_antiguo": false,
        "observaciones": ""
      },
      {
        "id": 7,
        "grado": "E",
        "nivel": null,
        "familia": "Sanitat",
        "codigo": null,
        "denominacion": "Audiologia protèsica",
        "plan_antiguo": false,
        "observaciones": ""
      }
    ]
    ```

    VERIFICACIÓ del schema (per cada registre):
    - Camps obligatoris presents: `id`, `grado`, `nivel`, `familia`, `codigo`,
      `denominacion`, `plan_antiguo`, `observaciones`
    - Grado D: `"codigo": null`, `"plan_antiguo": false` (HTML-06)
    - Grado E: `"codigo": null`, `"nivel": null`, `"plan_antiguo": false` (HTML-06)
    - Grado C (registre id=3): `"plan_antiguo": true`, `"observaciones": "(Plan antiguo)"`
    - IDs correlatius i únics: 1, 2, 3, 4, 5, 6, 7 (DATA-02)

    Si `fp-cercador/backend/data/` no existeix (perquè Plan 01 no s'ha
    executat encara), crear-lo:
    ```bash
    mkdir -p fp-cercador/backend/data
    ```

    Si existeix `fp-cercador/backend/data/.gitkeep` (creat per Plan 01),
    eliminar-lo ara que `ofertes.json` ocupa el directori:
    ```bash
    rm -f fp-cercador/backend/data/.gitkeep
    ```

    ---

    Crear `fp-cercador/README.md` amb el contingut EXACTE següent (D-08 —
    mínim, 4 passos de setup, sense seccions extenses):

    ```markdown
    # Cercador FP España

    Cercador de l'oferta formativa del Sistema de Formació Professional
    espanyol (Grados A–E).

    ## Setup

    1. Copia el fitxer d'exemple: `cp backend/.env.example backend/.env`
    2. Edita `backend/.env` i assigna un valor segur a `ADMIN_TOKEN`
    3. Instal·la les dependències: `pip install -r backend/requirements.txt`
    4. Arrenca el servidor: `cd backend && python app.py`
    ```

    RESTRICCIONS (D-08):
    - Exactament 4 passos numerats a la secció Setup
    - Pas 1 referencia `.env.example` (no una altra ruta)
    - Pas 2 menciona `ADMIN_TOKEN` explícitament
    - Pas 3 usa `pip install -r backend/requirements.txt`
    - Pas 4 usa `cd backend && python app.py`
    - Sense seccions addicionals (API docs, Contributing, License, etc.)
  </action>
  <verify>
    <automated>test -f "fp-cercador/backend/data/ofertes.json" &amp;&amp; test -f "fp-cercador/README.md" &amp;&amp; python3 -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); grads=set(r['grado'] for r in d); assert grads=={'A','B','C','D','E'}, f'Missing grados: {grads}'; assert len(d) == 7, f'Expected 7 records, got {len(d)}'; assert all(set(r.keys()) == {'id','grado','nivel','familia','codigo','denominacion','plan_antiguo','observaciones'} for r in d), 'Schema mismatch'; print('ofertes.json OK')" &amp;&amp; grep -q "ADMIN_TOKEN" fp-cercador/README.md &amp;&amp; grep -q "\.env\.example" fp-cercador/README.md &amp;&amp; echo "README OK"</automated>
  </verify>
  <acceptance_criteria>
    - `fp-cercador/backend/data/ofertes.json` existeix i és un fitxer JSON vàlid (python3 -c "import json; json.load(open(...))" no llança excepcions)
    - El fitxer conté exactament 7 registres (`len(d) == 7`)
    - Els 5 Grados estan representats: `set(r['grado'] for r in d) == {'A','B','C','D','E'}`
    - Cada registre conté exactament els camps: `id`, `grado`, `nivel`, `familia`, `codigo`, `denominacion`, `plan_antiguo`, `observaciones` (ni més ni menys)
    - El registre amb `id=3` (Grado C) té `"plan_antiguo": true` i `"observaciones": "(Plan antiguo)"`
    - El registre amb `id=7` (Grado E) té `"nivel": null` i `"codigo": null`
    - Els registres de Grado D (id=4,5,6) tenen `"codigo": null`
    - Els IDs son correlatius 1–7 sense gaps ni repeticions
    - `fp-cercador/README.md` existeix i conté la cadena `ADMIN_TOKEN`
    - `fp-cercador/README.md` conté la cadena `.env.example`
    - `fp-cercador/README.md` conté la cadena `pip install -r backend/requirements.txt`
    - `fp-cercador/README.md` conté `python app.py`
  </acceptance_criteria>
  <done>
    `ofertes.json` és un array JSON vàlid amb 7 registres de mostra que
    cobreixen els 5 Grados i segueixen el schema DATA-01. `README.md` és
    el document mínim de setup que permet clonar i arrencar el projecte
    sense configuració addicional (excepte definir ADMIN_TOKEN). La fase 1
    queda completament esqueletitzada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repositori git ↔ disc local | `ofertes.json` conté dades de mostra públiques — cap secret. `README.md` referencia `.env.example` (placeholder) i mai `.env` real. |
| Frontend ↔ Backend (futur) | `ofertes.json` serà servit per l'API (Fase 4) — les dades de mostra no contenen informació sensible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | fp-cercador/README.md | accept | El README documenta el workflow de setup però no conté cap secret real. Referencia `.env.example` (placeholder públic). Cap risc d'exposició. |
| T-02-02 | Information Disclosure | fp-cercador/backend/data/ofertes.json | accept | Fitxer de dades de mostra amb informació pública (títols de cursos FP). No conté PII ni secrets. S'inclou al repo intencionadament (D-09). |
| T-02-03 | Tampering | fp-cercador/frontend/index.html + admin.html | accept | Stubs sense lògica ni formularis. Cap vector d'atac en fase 1. La validació d'entrades es fa a la Fase 4 (API) i Fase 5/6 (frontend). |
| T-02-04 | Information Disclosure | fp-cercador/README.md instruccions ADMIN_TOKEN | mitigate | El README instrueix explícitament que cal CANVIAR el token del .env.example. El pas 2 diu "assigna un valor segur" — no usa el placeholder com a secret real. |
</threat_model>

<verification>
## Smoke tests del plan sencer (després de Tasks 1-2)

```bash
# Estructura frontend
test -d fp-cercador/frontend
test -f fp-cercador/frontend/index.html
test -f fp-cercador/frontend/admin.html

# HTML vàlid amb lang="ca"
grep -q 'lang="ca"' fp-cercador/frontend/index.html
grep -q 'lang="ca"' fp-cercador/frontend/admin.html
grep -q 'charset="UTF-8"' fp-cercador/frontend/index.html
grep -q 'charset="UTF-8"' fp-cercador/frontend/admin.html

# Comentaris TODO correctes (no intercanviats)
grep -q 'TODO: Phase 5' fp-cercador/frontend/index.html
grep -q 'TODO: Phase 6' fp-cercador/frontend/admin.html

# ofertes.json: JSON vàlid + 5 Grados + schema correcte
python3 -c "
import json
d = json.load(open('fp-cercador/backend/data/ofertes.json'))
grads = set(r['grado'] for r in d)
assert grads == {'A','B','C','D','E'}, f'Missing grados: {grads}'
assert len(d) >= 5, f'Need at least 5 records, got {len(d)}'
required_keys = {'id','grado','nivel','familia','codigo','denominacion','plan_antiguo','observaciones'}
for r in d:
    assert set(r.keys()) == required_keys, f'Schema error in record {r[\"id\"]}: {set(r.keys())}'
print('ofertes.json OK:', len(d), 'records, grados:', sorted(grads))
"

# README documenta ADMIN_TOKEN i .env.example
grep -q 'ADMIN_TOKEN' fp-cercador/README.md
grep -q '\.env\.example' fp-cercador/README.md
```

## Verificació global de la Fase 1 (Plans 01 + 02 combinats)

```bash
# Tots els fitxers esperats de la Fase 1
for f in \
    fp-cercador/.gitignore \
    fp-cercador/README.md \
    fp-cercador/backend/app.py \
    fp-cercador/backend/requirements.txt \
    fp-cercador/backend/.env.example \
    fp-cercador/backend/scrapers/__init__.py \
    fp-cercador/backend/data/ofertes.json \
    fp-cercador/frontend/index.html \
    fp-cercador/frontend/admin.html; do
  test -f "$f" && echo "OK: $f" || echo "MISSING: $f"
done

# PROJ-03: exactament 6 dependències
[ "$(grep -c . fp-cercador/backend/requirements.txt)" = "6" ] && echo "OK: 6 deps" || echo "FAIL: wrong dep count"

# PROJ-02: .env exclòs del .gitignore
grep -q '^\.env$' fp-cercador/.gitignore && echo "OK: .env excluded" || echo "FAIL: .env not in .gitignore"
```
</verification>

<success_criteria>
- [ ] `fp-cercador/frontend/` existeix amb `index.html` i `admin.html`
- [ ] `index.html` i `admin.html` son HTML vàlids amb `lang="ca"` i `charset="UTF-8"`
- [ ] `index.html` té el comentari `TODO: Phase 5`; `admin.html` té `TODO: Phase 6`
- [ ] Cap fitxer HTML conté `&lt;script`, `&lt;link` ni `&lt;style` en fase 1
- [ ] `fp-cercador/backend/data/ofertes.json` és un array JSON vàlid amb 7 registres
- [ ] `ofertes.json` cobreix els 5 Grados: A, B, C, D, E
- [ ] Cada registre d'`ofertes.json` conté exactament els 8 camps del schema DATA-01
- [ ] `fp-cercador/README.md` existeix i documenta els 4 passos de setup (D-08)
- [ ] `README.md` menciona `ADMIN_TOKEN` i `\.env\.example` explícitament
</success_criteria>

<output>
Després de completar, crear `.planning/phases/01-project-setup/01-02-SUMMARY.md`
seguint la plantilla `summary.md`. Incloure:
- Llista dels 4 fitxers creats (index.html, admin.html, ofertes.json, README.md)
- Nombre de registres a ofertes.json (7) i confirmació de cobertura dels 5 Grados
- Confirmació que cap fitxer HTML conté CSS/JS (stubs nets per Fases 5/6)
- Confirmació que ofertes.json NO està al .gitignore (decisió D-09 respectada)
</output>
