---
phase: 05-frontend-cercador
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - fp-cercador/frontend/index.html
autonomous: false
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
  - SRCH-08
  - SRCH-09
  - SRCH-10

must_haves:
  truths:
    - "La pàgina mostra 7.244 resultats amb hideOld activat per defecte (no 12.374)"
    - "Escriure 'administracio' (sense accent) troba registres amb 'Administración'"
    - "El dropdown Grado=B mostra NOMÉS registres amb grado B"
    - "El dropdown Família pobla dinàmicament 29 opcions ordenades alfabèticament"
    - "El dropdown Nivell=1 no mostra cap registre Grado A (nivel null)"
    - "Desactivar hideOld mostra badges 'Pla antic' a les files plan_antiguo=true"
    - "La paginació mostra màxim 7 botons; ellipsis apareix quan hi ha gaps de >1"
    - "El botó Anterior és inaccessible (aria-disabled) a la pàgina 1"
    - "El botó Següent és inaccessible (aria-disabled) a l'última pàgina"
    - "Canviar qualsevol filtre reseteja a la pàgina 1"
    - "Buscar 'ZZZZZ' mostra el missatge d'empty state dins la taula (no banner)"
    - "Aturar Flask i recarregar mostra el banner d'error 503 (no crash)"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: "SPA completament verificada i funcional"
      min_lines: 250
  key_links:
    - from: "Carrega inicial (hideOld=true)"
      to: "Comptador mostra 7.244"
      via: "filteredRecords getter exclou plan_antiguo=true → filteredCount"
      pattern: "7.244"
    - from: "DevTools Elements > tbody"
      to: "Màxim 50 <tr>"
      via: "pagedRecords.slice(start, start + 50)"
      pattern: "SRCH-09"
---

<objective>
Verificació funcional completa de fp-cercador/frontend/index.html al navegador real. Aquest pla conté un checkpoint humà estructurat que cobreix tots els requisits SRCH-01 a SRCH-10 i les decisions D-02 a D-12.

Purpose: Confirmar que la implementació del Plan 01 funciona correctament contra el backend real i detectar qualsevol desviació abans de tancar la fase.
Output: index.html verificat i aprovat. Si s'identifiquen defectes, el checkpoint inclou les instruccions per corregir-los.
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rogermasellas/AI/Cercador Graus/.planning/ROADMAP.md
@/Users/rogermasellas/AI/Cercador Graus/.planning/phases/05-frontend-cercador/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificació estructural automatitzada pre-checkpoint</name>
  <files>fp-cercador/frontend/index.html</files>

  <read_first>
    - fp-cercador/frontend/index.html (llegir el fitxer complet per confirmar estructura)
    - .planning/phases/05-frontend-cercador/05-01-SUMMARY.md (resum del que es va implementar)
  </read_first>

  <action>
Executar les verificacions grep automatitzades sobre el fitxer implementat al Plan 01. Reportar resultat de cada comprovació (PASS / FAIL):

```bash
cd /Users/rogermasellas/AI/Cercador\ Graus

echo "=== Verificació estructural index.html ==="

echo "1. Longitud del fitxer (mínim 250 línies):"
wc -l fp-cercador/frontend/index.html

echo "2. Ordre de scripts (alpine:init ha d'aparèixer ABANS de alpinejs@3.15.11):"
grep -n "alpine:init\|alpinejs@3.15.11" fp-cercador/frontend/index.html

echo "3. Constants crítiques:"
grep -c "const API_BASE = 'http://localhost:5000'" fp-cercador/frontend/index.html
grep -c "hideOld: true" fp-cercador/frontend/index.html
grep -c "parseInt(this.filterNivel)" fp-cercador/frontend/index.html
grep -c "normalize('NFD')" fp-cercador/frontend/index.html

echo "4. Renderització correcta:"
grep -c "x-for=\"row in pagedRecords\"" fp-cercador/frontend/index.html
grep -c "x-show=\"row.plan_antiguo\"" fp-cercador/frontend/index.html
grep -c "colspan=\"5\"" fp-cercador/frontend/index.html

echo "5. Seguretat XSS (ha de retornar 0 o buit):"
grep -c "x-html" fp-cercador/frontend/index.html || echo "PASS: cap x-html"

echo "6. Accessibilitat:"
grep -c "role=\"status\"" fp-cercador/frontend/index.html
grep -c "sr-only" fp-cercador/frontend/index.html
grep -c "aria-disabled" fp-cercador/frontend/index.html

echo "7. Estats (loading, error, ready):"
grep -c "state === 'loading'" fp-cercador/frontend/index.html
grep -c "state === 'error'" fp-cercador/frontend/index.html
grep -c "state === 'ready'" fp-cercador/frontend/index.html

echo "8. Capçaleres taula en ordre correcte:"
grep -A 10 "<thead>" fp-cercador/frontend/index.html
```

Si qualsevol comprovació falla (resultat 0 quan s'esperava ≥1, o viceversa), corregir el fitxer ara abans de passar al checkpoint humà. Les correccions han de mantenir totes les decisions D-01 a D-12.
  </action>

  <verify>
    <automated>grep -c "x-html" /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/frontend/index.html || echo "PASS: zero x-html" && wc -l /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/frontend/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `wc -l fp-cercador/frontend/index.html` retorna un número >= 250
    - `grep -c "x-html" index.html` retorna 0 (cap vector XSS)
    - `grep -n "alpine:init" index.html` mostra número de línia MENOR que `grep -n "alpinejs@3.15.11" index.html`
    - `grep -c "hideOld: true" index.html` retorna 1
    - `grep -c "parseInt(this.filterNivel)" index.html` retorna 1
    - `grep -c "x-for=\"row in pagedRecords\"" index.html` retorna 1 (no filteredRecords)
    - `grep -c "role=\"status\"" index.html` retorna 2 (loading + error)
    - `grep -c "colspan=\"5\"" index.html` retorna 1 (empty state)
    - Informe de totes les verificacions generat i cap FAIL sense correcció aplicada
  </acceptance_criteria>

  <done>Totes les comprovacions estructurals passen. El fitxer és complet i correcte estructuralment. Llestos per al checkpoint de verificació visual.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificació funcional al navegador</name>

  <what-built>
Plan 01 ha implementat fp-cercador/frontend/index.html complet: cerca en temps real accent-insensible, dropdowns Grado/Família/Nivell, checkbox Ocultar pla antic (activat per defecte), taula amb badge "Pla antic", paginació clàssica 50/pàgina amb ellipsis, i gestió d'estats loading/error/empty. Task 1 d'aquest pla ha verificat l'estructura automàticament.
  </what-built>

  <how-to-verify>
**Pre-requisit:** Flask ha d'estar corrent al port 5000.
```bash
cd /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/backend
flask run --port 5000
```
Si `flask run` retorna `ADMIN_TOKEN not set`, assegurar que `.env` existeix.

**Obrir el cercador:**
Obrir `fp-cercador/frontend/index.html` al navegador (doble clic o `open fp-cercador/frontend/index.html`).

---

**Checklist de verificació (marcar cada ítem):**

**Càrrega inicial:**
- [ ] Apareix el spinner CSS (cercle giratori blau) mentre carreguen les dades
- [ ] El spinner desapareix i es mostra la taula
- [ ] El comptador mostra **7.244 resultats** (no 12.374) — perquè hideOld=true per defecte (SRCH-05)
- [ ] El checkbox "Ocultar pla antic" apareix ACTIVAT per defecte
- [ ] La taula mostra 5 columnes en ordre: **Denominació | Codi | Família | Grado | Nivell** (D-07)

**Cerca en temps real (SRCH-01):**
- [ ] Escriure "ADG" al camp cerca → la taula filtra per codis que contenen "ADG"
- [ ] Escriure "administracio" (sense accent) → apareixen registres amb "Administración" al nom (cerca accent-insensible)
- [ ] El comptador s'actualitza en temps real mentre s'escriu (SRCH-08)
- [ ] Esborrar la cerca → torna als resultats complets

**Dropdowns (SRCH-02, SRCH-03, SRCH-04):**
- [ ] Dropdown Grado=B → tots els resultats mostren "B" a la columna Grado
- [ ] Obrir dropdown Família → apareixen **30 opcions** (29 famílies + "Totes les famílies"), ordenades alfabèticament
- [ ] Dropdown Família=Sanidad → tots els resultats son de la família Sanidad
- [ ] Dropdown Nivell=1 → cap resultat té "A" a la columna Grado (Grado A té nivel=null)
- [ ] Combinació: Grado=C + Família=Sanidad + Nivell=2 → resultats coherents

**Checkbox i badge (SRCH-05, SRCH-07):**
- [ ] Desactivar checkbox "Ocultar pla antic" → comptador puja a 12.374 resultats
- [ ] Amb checkbox desactivat → apareixen badges **"Pla antic"** a files amb plan_antiguo=true
- [ ] El badge és de color amber clar (fons #fef3c7, text #92400e)
- [ ] El badge és dins la cel·la Denominació, no és una columna separada (D-08)

**Paginació (D-02):**
- [ ] Pàgina 1: el botó "Anterior" apareix desactivat (estil gris, no clicable)
- [ ] Navegar a la pàgina 2: el botó "Anterior" s'activa; la pàgina 1 no està resaltada
- [ ] La pàgina actual es veu resaltada en blau (#2563eb)
- [ ] Amb 145 pàgines (hideOld=true), es veu ellipsis: ex. `1 2 3 … 145`
- [ ] Anar a la pàgina 74 de 145 → el control mostra `1 … 72 73 [74] 75 76 … 145`
- [ ] A l'última pàgina: el botó "Següent" apareix desactivat

**Rendiment (SRCH-09):**
- [ ] Obrir DevTools (F12) → Elements → cercar `<tbody>` → comptar `<tr>` → màxim **50 files**
- [ ] Obrir DevTools → Network → recarregar → un únic fetch a `/api/ofertes` per sessió (D-03)

**Estat buit (D-12):**
- [ ] Escriure "ZZZZZZZZZ" al camp cerca → apareix el missatge dins la taula: "Cap resultat coincideix amb els filtres aplicats."
- [ ] El missatge d'empty state és dins la taula (capçaleres visibles), NO és el banner vermell d'error

**Estat d'error 503 (D-10, D-11):**
- [ ] Aturar Flask (`Ctrl+C`) → recarregar la pàgina → apareix el banner vermell: "⚠️ Les dades del catàleg no estan disponibles. Contacteu l'administrador del sistema."
- [ ] El banner d'error NO té cap botó de reintent (D-11)
- [ ] Tornar a engegar Flask → recarregar → torna al comportament normal

**Reset de pàgina (Pitfall 4):**
- [ ] Navegar a la pàgina 5 → canviar qualsevol filtre → la paginació torna a la pàgina 1 automàticament
  </how-to-verify>

  <resume-signal>
Escriu "aprovat" si tots els ítems del checklist passen.
Si hi ha defectes, descriu quins ítems fallen i el comportament observat. L'executor corregirà i sol·licitarà nova verificació.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Usuari → navegador | Input de cerca lliure; no s'envia al servidor |
| Backend Flask → frontend | Dades de /api/ofertes processades al navegador |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | Verificació manual — possibilitat de marcar ítems com a passats sense verificar-los | accept | Responsabilitat del revisor humà. El plan 01 té verificació grep automatitzada prèvia que cobreix els pitfalls crítics. |
| T-05-07 | Information Disclosure | DevTools Network visible durant verificació | accept | El cercador és públic, sense auth. Les dades de /api/ofertes no contenen PII ni secrets. |
</threat_model>

<verification>
La verificació principal d'aquest pla és el checkpoint humà (Task 2). La Task 1 proporciona verificació grep automatitzada com a pre-requisit.

Totes les comprovacions de Task 1 han de passar (PASS) abans d'iniciar el checkpoint.
</verification>

<success_criteria>
- Task 1: Cap comprovació grep retorna FAIL sense correcció aplicada
- Task 2: L'usuari escriu "aprovat" al checkpoint
- Tots els requisits SRCH-01 a SRCH-10 han estat verificats funcionalment al navegador
- Les decisions D-02 a D-12 han estat confirmades visualment
</success_criteria>

<output>
Després de completar, crear `.planning/phases/05-frontend-cercador/05-02-SUMMARY.md` amb el format estàndard.
</output>
