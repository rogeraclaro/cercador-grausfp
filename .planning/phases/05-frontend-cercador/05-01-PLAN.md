---
phase: 05-frontend-cercador
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/frontend/index.html
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06
  - SRCH-07
  - SRCH-08
  - SRCH-09
  - SRCH-10

must_haves:
  truths:
    - "Obrir index.html al browser mostra un spinner mentre carrega les dades"
    - "Escriure al camp de cerca filtra la taula en temps real per denominacion i codigo"
    - "Els dropdowns de Grado, Família i Nivell filtren els resultats en combinació"
    - "El checkbox Ocultar pla antic és actiu per defecte i mostra 7.244 resultats inicials"
    - "La taula mostra màxim 50 files al DOM en qualsevol moment"
    - "Navegar pàgines canvia les files mostrades sense recàrrega"
    - "Si el backend retorna 503 o no està disponible, apareix un banner d'error"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: "SPA completa amb HTML + CSS + JS Alpine.js inline"
      min_lines: 250
      contains: "x-data=\"cercador\""
    - path: "fp-cercador/frontend/index.html"
      provides: "Constant API_BASE"
      contains: "const API_BASE = 'http://localhost:5000'"
    - path: "fp-cercador/frontend/index.html"
      provides: "CDN Alpine.js 3.15.11 amb defer"
      contains: "alpinejs@3.15.11/dist/cdn.min.js"
  key_links:
    - from: "init() → fetch(API_BASE + '/api/ofertes')"
      to: "allRecords[]"
      via: "data.map() amb _normDen + _normCod"
      pattern: "normalize.*NFD"
    - from: "filteredRecords getter"
      to: "pagedRecords getter"
      via: "filteredRecords.slice(start, start + pageSize)"
      pattern: "slice.*pageSize"
    - from: "x-for en tbody"
      to: "pagedRecords"
      via: "template x-for=\"row in pagedRecords\" :key=\"row.id\""
      pattern: "x-for.*pagedRecords"
---

<objective>
Implementar fp-cercador/frontend/index.html complet: HTML skeleton amb Alpine.js 3.15.11 via CDN, tota la lògica JS (fetch, estat, pre-normalització NFD, filtres en memòria, paginació amb ellipsis), estructura HTML del template (filtres, taula, paginació, estats loading/error/empty) i CSS inline.

Purpose: Crear el fitxer únic que constitueix tota la UI del cercador FP. Sense aquest fitxer, no hi ha frontend.
Output: fp-cercador/frontend/index.html — fitxer HTML complet, funcional, ~300-400 línies.
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rogermasellas/AI/Cercador Graus/.planning/PROJECT.md
@/Users/rogermasellas/AI/Cercador Graus/.planning/ROADMAP.md
@/Users/rogermasellas/AI/Cercador Graus/.planning/STATE.md

<interfaces>
<!-- Resposta de GET /api/ofertes (app.py línies 75-86) -->
<!-- 200 OK → array JSON directe (no objecte wrapper): -->
[
  {
    "id": 1,
    "codigo": "AFD_A_3003_01",
    "denominacion": "Clasificación de tareas administrativas",
    "observaciones": "...",
    "familia": "Actividades Físicas y Deportivas",
    "nivel": null,
    "plan_antiguo": false,
    "grado": "A"
  }
]
<!-- 503 → {"error": "Data not available..."} — NO mostrar missatge del servidor al frontend -->
<!-- plan_antiguo: booleà JSON (true/false), NO string -->
<!-- nivel: null per a 10.027 de 12.374 registres (tots els Grado A + part B/C) -->
<!-- grado: string "A" | "B" | "C" | "D" | "E" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML skeleton + API_BASE + estructura Alpine.js + CSS inline</name>
  <files>fp-cercador/frontend/index.html</files>

  <read_first>
    - fp-cercador/frontend/index.html (stub actual — 10 línies, context de partida)
    - .planning/phases/05-frontend-cercador/05-UI-SPEC.md (design system: colors, spacing, typography, components)
    - .planning/phases/05-frontend-cercador/05-CONTEXT.md (decisions D-01 a D-12 bloquejades)
    - .planning/phases/05-frontend-cercador/05-PATTERNS.md (patró CSS complet extret del UI-SPEC)
  </read_first>

  <action>
Substituir el stub buit amb el document HTML complet. El fitxer ha de tenir aquesta estructura exacta (ordre obligatori):

```
<!DOCTYPE html>
<html lang="ca">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cercador FP Espanya</title>
  <style>
    [CSS complet — veure especificació a continuació]
  </style>
  <script>
    const API_BASE = 'http://localhost:5000';  /* D-09 */
    document.addEventListener('alpine:init', () => {
      Alpine.data('cercador', () => ({ [component JS] }));
    });
  </script>
  <!-- IMPORTANT: CDN Alpine SEMPRE DESPRÉS del script propi (Pitfall 5) -->
  <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.15.11/dist/cdn.min.js"></script>
</head>
<body>
  <div class="page-container" x-data="cercador">
    [HTML template]
  </div>
</body>
</html>
```

**BLOC CSS `<style>` (copiar exactament):**

```css
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
               Helvetica, Arial, sans-serif;
  font-size: 14px;
  color: #111827;
  background: #ffffff;
}

.page-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 32px 24px;
}

h1 {
  font-size: 20px;
  font-weight: 600;
  line-height: 1.2;
  margin-bottom: 24px;
}

.filter-row {
  display: flex;
  flex-wrap: wrap;
  gap: 16px;
  margin-bottom: 8px;
}

input[type="search"], select {
  padding: 8px 16px;
  border: 1px solid #d1d5db;
  border-radius: 4px;
  font-size: 14px;
  background: #ffffff;
  color: #111827;
}
input[type="search"] { flex: 1; min-width: 200px; }
input[type="search"]:focus, select:focus {
  outline: none;
  border-color: #2563eb;
  box-shadow: 0 0 0 2px rgba(37,99,235,0.15);
}
input[type="search"]::placeholder { color: #6b7280; }

.controls-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 16px;
  font-size: 12px;
  font-weight: 600;
  color: #6b7280;
}
.controls-row label {
  display: flex;
  align-items: center;
  gap: 6px;
  cursor: pointer;
}

.results-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 14px;
}
.results-table th {
  background: #f3f4f6;
  text-align: left;
  padding: 8px 12px;
  font-size: 12px;
  font-weight: 600;
  border-bottom: 1px solid #d1d5db;
}
.results-table td {
  padding: 8px 12px;
  border-bottom: 1px solid #f3f4f6;
  vertical-align: middle;
}
.results-table tr:nth-child(even) td { background: #f3f4f6; }

.badge-old {
  display: inline-block;
  background: #fef3c7;
  color: #92400e;
  font-size: 11px;
  font-weight: 600;
  padding: 2px 6px;
  border-radius: 3px;
  margin-left: 6px;
  white-space: nowrap;
}

.empty-row td {
  text-align: center;
  color: #6b7280;
  padding: 32px;
  font-style: italic;
}

.loading-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  padding: 48px;
  color: #6b7280;
}
@keyframes spin { to { transform: rotate(360deg); } }
.spinner {
  border: 3px solid #d1d5db;
  border-top-color: #2563eb;
  border-radius: 50%;
  width: 32px;
  height: 32px;
  animation: spin 0.8s linear infinite;
}

.error-state {
  padding: 16px;
  background: #fee2e2;
  color: #991b1b;
  border-radius: 4px;
  margin: 16px 0;
}

.pagination {
  display: flex;
  align-items: center;
  gap: 4px;
  margin-top: 16px;
  padding: 16px 0;
}
.pagination button {
  padding: 6px 12px;
  border: 1px solid #d1d5db;
  border-radius: 4px;
  background: #ffffff;
  cursor: pointer;
  font-size: 14px;
  color: #111827;
}
.pagination button.active {
  background: #2563eb;
  color: #ffffff;
  border-color: #2563eb;
}
.pagination button.disabled {
  color: #6b7280;
  cursor: default;
  pointer-events: none;
}
.pagination-ellipsis {
  padding: 0 4px;
  color: #6b7280;
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0,0,0,0);
  white-space: nowrap;
  border: 0;
}

@media (max-width: 640px) {
  .filter-row { flex-direction: column; }
}
```

**BLOC JS `<script>` (copiar exactament el component Alpine.data):**

El component ha d'implementar TOTS els camps i mètodes següents:

Propietats d'estat:
- `state: 'loading'`  — valors: 'loading' | 'ready' | 'error'
- `allRecords: []`    — array complet post-fetch amb _normDen i _normCod afegits
- `families: []`      — array de strings, 29 úniques, ordenades alfabèticament

Propietats de filtre (totes buides per defecte excepte hideOld):
- `search: ''`
- `filterGrado: ''`
- `filterFamilia: ''`
- `filterNivel: ''`
- `hideOld: true`     — SRCH-05: ACTIVAT per defecte

Propietats de paginació:
- `currentPage: 1`
- `pageSize: 50`

Mètode `async init()` — ha de fer exactament:
1. `fetch(API_BASE + '/api/ofertes')`
2. Si `!res.ok` → `this.state = 'error'; return;`  (cobreix 503 i errors xarxa)
3. `const data = await res.json()`
4. `this.allRecords = data.map(r => ({ ...r, _normDen: (r.denominacion || '').normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase(), _normCod: (r.codigo || '').normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase() }))`
5. `this.families = [...new Set(data.map(r => r.familia))].sort()`
6. `this.state = 'ready'`
7. `catch(e) { this.state = 'error'; }`

Getter `filteredRecords` — lògica AND exacta:
```javascript
get filteredRecords() {
  const q = this.search.normalize('NFD')
              .replace(/[\u0300-\u036f]/g, '').toLowerCase().trim();
  return this.allRecords.filter(r => {
    if (this.hideOld && r.plan_antiguo) return false;
    if (this.filterGrado && r.grado !== this.filterGrado) return false;
    if (this.filterFamilia && r.familia !== this.filterFamilia) return false;
    if (this.filterNivel && r.nivel !== parseInt(this.filterNivel)) return false;
    if (q && !r._normDen.includes(q) && !r._normCod.includes(q)) return false;
    return true;
  });
},
```

Getter `filteredCount`: `return this.filteredRecords.length;`

Getter `pagedRecords`:
```javascript
get pagedRecords() {
  const start = (this.currentPage - 1) * this.pageSize;
  return this.filteredRecords.slice(start, start + this.pageSize);
},
```

Getter `totalPages`: `return Math.ceil(this.filteredRecords.length / this.pageSize);`

Mètode `buildPagination()` — algorisme kottenator (delta=2):
```javascript
buildPagination() {
  const c = this.currentPage, m = this.totalPages, delta = 2;
  if (m <= 1) return [];
  const left = c - delta, right = c + delta + 1;
  const range = [], result = [];
  let l;
  for (let i = 1; i <= m; i++) {
    if (i === 1 || i === m || (i >= left && i < right)) range.push(i);
  }
  for (const i of range) {
    if (l) {
      if (i - l === 2) result.push({ type: 'page', n: l + 1 });
      else if (i - l !== 1) result.push({ type: 'ellipsis' });
    }
    result.push({ type: 'page', n: i });
    l = i;
  }
  return result;
},
```

Mètode `goToPage(n)`:
```javascript
goToPage(n) {
  if (n < 1 || n > this.totalPages) return;
  this.currentPage = n;
  document.getElementById('results-table')?.scrollIntoView({ behavior: 'smooth' });
},
```

Mètode `resetPage()`: `this.currentPage = 1;`

**BLOC HTML `<body>` (estructura exacta per D-06):**

Dins de `<div class="page-container" x-data="cercador">`:

1. `<h1>Cercador FP Espanya</h1>`

2. Estat loading (D-10):
```html
<div x-show="state === 'loading'" class="loading-state" role="status">
  <div class="spinner"></div>
  <p>Carregant dades del catàleg FP...</p>
</div>
```

3. Estat error 503 (D-11) — sense botó retry:
```html
<div x-show="state === 'error'" class="error-state" role="status">
  <p>⚠️ Les dades del catàleg no estan disponibles. Contacteu l'administrador del sistema.</p>
</div>
```

4. Contingut principal (x-show="state === 'ready'"):
```html
<div x-show="state === 'ready'">
  <!-- Fila de filtres (D-06 ordre: cerca, grado, familia, nivell) -->
  <div class="filter-row">
    <label for="search" class="sr-only">Cerca</label>
    <input type="search" id="search"
      x-model.debounce.250ms="search"
      @input="resetPage()"
      placeholder="Cerca per denominació o codi..."
      autocomplete="off">

    <label for="filter-grado" class="sr-only">Grado</label>
    <select id="filter-grado" x-model="filterGrado" @change="resetPage()">
      <option value="">Tots els grados</option>
      <option value="A">A</option>
      <option value="B">B</option>
      <option value="C">C</option>
      <option value="D">D</option>
      <option value="E">E</option>
    </select>

    <label for="filter-familia" class="sr-only">Família</label>
    <select id="filter-familia" x-model="filterFamilia" @change="resetPage()">
      <option value="">Totes les famílies</option>
      <template x-for="fam in families" :key="fam">
        <option :value="fam" x-text="fam"></option>
      </template>
    </select>

    <label for="filter-nivel" class="sr-only">Nivell</label>
    <select id="filter-nivel" x-model="filterNivel" @change="resetPage()">
      <option value="">Tots els nivells</option>
      <option value="1">1</option>
      <option value="2">2</option>
      <option value="3">3</option>
    </select>
  </div>

  <!-- Fila checkbox + comptador (D-06) -->
  <div class="controls-row">
    <label for="hide-old">
      <input type="checkbox" id="hide-old" x-model="hideOld" @change="resetPage()">
      Ocultar pla antic
    </label>
    <span class="counter" x-text="filteredCount === 1 ? '1 resultat' : (filteredCount === 0 ? 'Cap resultat' : filteredCount + ' resultats')"></span>
  </div>

  <!-- Taula (D-07: columnes Denominació | Codi | Família | Grado | Nivell) -->
  <table class="results-table" id="results-table">
    <caption class="sr-only">Resultats del cercador FP Espanya</caption>
    <thead>
      <tr>
        <th scope="col">Denominació</th>
        <th scope="col">Codi</th>
        <th scope="col">Família</th>
        <th scope="col">Grado</th>
        <th scope="col">Nivell</th>
      </tr>
    </thead>
    <tbody>
      <!-- Estat buit dins la taula (D-12) — diferenciat de l'error 503 -->
      <template x-if="filteredCount === 0">
        <tr class="empty-row">
          <td colspan="5">Cap resultat coincideix amb els filtres aplicats.</td>
        </tr>
      </template>
      <!-- Files de dades: x-for sobre pagedRecords (màx 50 <tr> al DOM — SRCH-09) -->
      <template x-for="row in pagedRecords" :key="row.id">
        <tr>
          <td>
            <!-- x-text escapa HTML — mai x-html (seguretat XSS) -->
            <span x-text="row.denominacion"></span>
            <!-- Badge Pla antic (D-08): inline a Denominació, no columna separada -->
            <span x-show="row.plan_antiguo" class="badge-old">Pla antic</span>
          </td>
          <td x-text="row.codigo ?? '—'"></td>
          <td x-text="row.familia"></td>
          <td x-text="row.grado"></td>
          <td x-text="row.nivel ?? '—'"></td>
        </tr>
      </template>
    </tbody>
  </table>

  <!-- Paginació (D-02) -->
  <nav class="pagination" aria-label="Paginació de resultats" x-show="totalPages > 1">
    <button
      @click="goToPage(currentPage - 1)"
      :aria-disabled="currentPage === 1"
      :class="{ disabled: currentPage === 1 }">Anterior</button>

    <template x-for="item in buildPagination()" :key="JSON.stringify(item)">
      <template x-if="item.type === 'page'">
        <button
          @click="goToPage(item.n)"
          :aria-current="item.n === currentPage ? 'page' : false"
          :aria-label="'Pàgina ' + item.n"
          :class="{ active: item.n === currentPage }"
          x-text="item.n"></button>
      </template>
      <template x-if="item.type === 'ellipsis'">
        <span class="pagination-ellipsis">…</span>
      </template>
    </template>

    <button
      @click="goToPage(currentPage + 1)"
      :aria-disabled="currentPage === totalPages"
      :class="{ disabled: currentPage === totalPages }">Següent</button>
  </nav>
</div>
```

NOTA sobre el port: D-09 especifica `http://localhost:5000`. `app.py` usa `port=5001` en `__main__` però `flask run` per defecte usa 5000. Usar 5000 tal com especifica D-09.
  </action>

  <verify>
    <automated>grep -c "x-data=\"cercador\"" /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/frontend/index.html && grep -c "const API_BASE" /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/frontend/index.html && grep -c "alpinejs@3.15.11" /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/frontend/index.html && grep -c "alpine:init" /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/frontend/index.html</automated>
  </verify>

  <acceptance_criteria>
    - index.html conté `x-data="cercador"` (exactament)
    - index.html conté `const API_BASE = 'http://localhost:5000'` (D-09)
    - index.html conté `alpinejs@3.15.11/dist/cdn.min.js` amb atribut `defer`
    - index.html conté `document.addEventListener('alpine:init'`
    - El script propi (`alpine:init`) apareix ABANS del tag CDN Alpine al fitxer (grep -n per verificar línies)
    - index.html conté `normalize('NFD')` (pre-normalització NFD a init())
    - index.html conté `hideOld: true` (SRCH-05: activat per defecte)
    - index.html conté `parseInt(this.filterNivel)` (Pitfall 2: tipus mixt nivel)
    - index.html conté `x-for="row in pagedRecords"` (no filteredRecords — D-03/SRCH-09)
    - index.html conté `x-show="row.plan_antiguo"` (D-08: badge inline)
    - index.html conté `class="badge-old"` amb text "Pla antic"
    - index.html conté `colspan="5"` (empty state dins la taula — D-12)
    - index.html conté `state === 'error'` (D-11: banner 503)
    - index.html conté `state === 'loading'` (D-10: spinner)
    - index.html conté `class="spinner"` (spinner CSS)
    - index.html conté les 5 capçaleres en ordre: Denominació, Codi, Família, Grado, Nivell (D-07)
    - index.html conté `@input="resetPage()"` al camp de cerca (Pitfall 4)
    - index.html conté `@change="resetPage()"` als dropdowns i checkbox (Pitfall 4)
    - index.html NO conté `x-html` en cap lloc (seguretat XSS)
    - index.html conté `role="status"` als estats loading i error (accessibilitat)
    - index.html conté `<caption class="sr-only">` a la taula (accessibilitat)
    - index.html té almenys 250 línies (fitxer complet, no stub)
  </acceptance_criteria>

  <done>index.html és un fitxer HTML complet i funcional. Obrint-lo al browser amb Flask corrent al port 5000 es mostra el spinner, es carreguen les dades i es mostren 7.244 resultats (hideOld=true per defecte) paginats a 50 per pàgina.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN jsDelivr → browser | Alpine.js carregat de tercers via CDN sense SRI hash |
| API Flask → browser | JSON de /api/ofertes processa contingut de les denominacions i codis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | x-for loop → `row.denominacion` | mitigate | Usar `x-text` exclusivament (mai `x-html`) per a tots els camps del JSON. Alpine.js escapa HTML automàticament amb `x-text`. Verificable: `grep -r "x-html" index.html` ha de retornar buit. |
| T-05-02 | Tampering | `row.codigo`, `row.familia`, `row.grado`, `row.nivel` | mitigate | Tots els camps renderitzats amb `x-text` o `x-text="row.nivel ?? '—'"`. Cap interpolació d'HTML manual. |
| T-05-03 | Information Disclosure | CDN Alpine.js sense SRI | accept | ASVS L1 no requereix SRI per a CDN de tercers amb reputació alta (jsDelivr). Risc baix: cap dada sensible al frontend del cercador (cerca pública, sense auth). Mitigation futura (L2): afegir `integrity` hash al tag CDN. |
| T-05-04 | Denial of Service | fetch únic de 3.8MB JSON | accept | El payload és gran però fix. El backend ja gestiona la càrrega. El frontend no pot controlar el volum del JSON. Spinner cobreix la latència de càrrega. |
| T-05-05 | Tampering | input `search` — text lliure usuari | accept | El camp de cerca NO s'envia al servidor (D-03: filtratge en memòria). La normalització NFD és operació de lectura sobre el valor local. Cap risc d'injeccions al backend. |
</threat_model>

<verification>
Verificació estructural (automatitzable amb grep):

```bash
cd /Users/rogermasellas/AI/Cercador\ Graus

# 1. Estructura Alpine.js correcta (ordre obligatori: script propi ABANS del CDN)
grep -n "alpine:init\|alpinejs@3.15.11" fp-cercador/frontend/index.html
# → alpine:init ha d'aparèixer a una línia MENOR que alpinejs@3.15.11

# 2. Constants i propietats crítiques
grep -c "const API_BASE = 'http://localhost:5000'" fp-cercador/frontend/index.html  # → 1
grep -c "hideOld: true" fp-cercador/frontend/index.html                              # → 1
grep -c "parseInt(this.filterNivel)" fp-cercador/frontend/index.html                 # → 1
grep -c "x-for=\"row in pagedRecords\"" fp-cercador/frontend/index.html              # → 1
grep -c "normalize('NFD')" fp-cercador/frontend/index.html                           # → >=2 (init + getter)

# 3. Seguretat XSS: cap x-html
grep "x-html" fp-cercador/frontend/index.html  # → cap resultat

# 4. Completesa del fitxer
wc -l fp-cercador/frontend/index.html  # → >= 250 línies
```
</verification>

<success_criteria>
- index.html té almenys 250 línies (no stub)
- Tots els acceptance_criteria de la Task 1 passen
- Cap `x-html` al fitxer (zero XSS vectors)
- L'ordre dels scripts és correcte: `alpine:init` handler apareix abans del CDN tag `alpinejs@3.15.11`
- Les 5 columnes apareixen en ordre exacte: Denominació | Codi | Família | Grado | Nivell
</success_criteria>

<output>
Després de completar, crear `.planning/phases/05-frontend-cercador/05-01-SUMMARY.md` amb el format estàndard.
</output>
