---
phase: 03-html-scrapers-data-pipeline-grados-d-e
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - fp-cercador/backend/tests/conftest.py
  - fp-cercador/backend/tests/test_html_scraper.py
  - fp-cercador/backend/.env.example
autonomous: true
requirements: [HTML-01, HTML-02, HTML-03, HTML-04, HTML-05, HTML-06]
must_haves:
  truths:
    - "La suite pytest existent (test_pdf_scraper.py + test_pipeline.py) segueix passant"
    - "test_html_scraper.py existeix amb tests RED que descriuen el comportament esperat del scraper HTML"
    - "Les fixtures HTML a conftest.py reprodueixen l'estructura real verificada contra todofp.es"
    - ".env.example conté les 4 noves variables URL_GRADO_D_BASICO/MEDIO/SUPERIOR i URL_GRADO_E"
  artifacts:
    - path: "fp-cercador/backend/tests/test_html_scraper.py"
      provides: "Suite de tests RED que cobreix HTML-01 a HTML-06"
      contains: "def test_parse_grado_d_basico_single_title"
    - path: "fp-cercador/backend/tests/conftest.py"
      provides: "Fixtures HTML reutilitzables per als tests del html_scraper"
      contains: "minimal_html_grado_d_one_record"
    - path: "fp-cercador/backend/.env.example"
      provides: "Documentació de les 4 noves variables d'entorn URL"
      contains: "URL_GRADO_D_BASICO"
  key_links:
    - from: "tests/test_html_scraper.py"
      to: "scrapers/html_scraper.py"
      via: "import (mòdul encara no existent — tests RED)"
      pattern: "from scrapers.html_scraper import"
    - from: ".env.example"
      to: "pipeline.py (Plan 03)"
      via: "variables d'entorn llegides amb os.getenv fallback"
      pattern: "URL_GRADO_D_BASICO="
---

<objective>
Establir els contractes de test del `html_scraper` (fase RED del TDD) i preparar les fixtures HTML i la configuració d'entorn abans d'implementar el mòdul. Aquest Plan NO crea `html_scraper.py` — els tests fallaran amb ImportError, i això és el resultat esperat al finalitzar el Plan.

Purpose: Fixa el comportament esperat del scraper HTML abans d'escriure-la implementació. Els tests defineixen el contracte (HTML-01 a HTML-06) i serviran de guió per al Plan 02 (GREEN). També afegeix les variables d'entorn al `.env.example` perquè el Plan 03 pugui integrar-les al `pipeline.py` amb `os.getenv` + fallback.

Output:
- `fp-cercador/backend/tests/conftest.py` ampliat amb 4 fixtures HTML minimalistes
- `fp-cercador/backend/tests/test_html_scraper.py` nou amb ~15 tests RED
- `fp-cercador/backend/.env.example` ampliat amb 4 noves variables URL
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-CONTEXT.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md
@fp-cercador/backend/tests/conftest.py
@fp-cercador/backend/tests/test_pdf_scraper.py
@fp-cercador/backend/tests/test_pipeline.py
@fp-cercador/backend/.env.example

<interfaces>
<!-- Contractes que el Plan 02 implementarà a `html_scraper.py`. Els tests d'aquest Plan els usaran com a imports que inicialment fallaran. -->

API pública de `scrapers.html_scraper` (a crear al Plan 02):
```python
# Constants públiques
HTML_FAMILY_ALIASES: dict[str, str]  # Mapeig de noms de família HTML no canònics a valors de PREFIX_MAP

# Funcions privades (usades pels tests unitaris)
_build_fam_map(soup: BeautifulSoup) -> dict[str, str]
_extract_titols(soup: BeautifulSoup, fam_map: dict, nivel: int | None, grado: str) -> list[dict]
_parse_grado_d(url: str, nivel: int) -> list[dict]

# Funcions públiques (criden requests.get i deleguen a _parse_grado_d/_extract_titols)
parse_grado_d_basico(url: str) -> list[dict]    # nivel=1
parse_grado_d_medio(url: str) -> list[dict]     # nivel=2
parse_grado_d_superior(url: str) -> list[dict]  # nivel=3
parse_grado_e(url: str) -> list[dict]           # nivel=None
```

Schema de cada registre retornat (D-10 de CONTEXT):
```python
{
    "denominacion": str,      # text del <a id="tit-...">
    "familia": str,           # nom canònic (o "Desconeguda")
    "nivel": int | None,      # 1/2/3 (D) o None (E)
    "codigo": None,           # sempre None per D/E (HTML-06)
    "plan_antiguo": False,    # sempre False per D/E (HTML-06)
    "observaciones": "",      # sempre buit per D/E
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ampliar conftest.py amb fixtures HTML minimalistes verificades</name>
  <files>fp-cercador/backend/tests/conftest.py</files>
  <read_first>
    - fp-cercador/backend/tests/conftest.py (fitxer a ampliar — mai eliminar fixtures existents)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §Estructura HTML Verificada (font de veritat de l'estructura)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md §conftest.py (patrons a replicar)
  </read_first>
  <behavior>
    - Fixture `minimal_html_grado_d_one_record` retorna un string HTML amb exactament 1 títol i 1 família canònica ("Administración y Gestión")
    - Fixture `minimal_html_grado_d_two_records_same_family` retorna HTML amb 2 títols de la mateixa família (rowspan=2)
    - Fixture `minimal_html_alias_imagen_y_sonido` retorna HTML amb família "Imagen y Sonido" (requerirà HTML_FAMILY_ALIASES)
    - Fixture `minimal_html_unknown_family` retorna HTML amb família no canònica ("Mantenimiento y Servicios a la Producción")
    - Les fixtures existents (sample_table_grado_*) es mantenen intactes
  </behavior>
  <action>
Afegir AL FINAL del fitxer `fp-cercador/backend/tests/conftest.py` (sense tocar les fixtures existents) les 4 noves fixtures HTML següents, EXACTAMENT amb aquests noms i retornant exactament aquests strings:

```python


# ---------------------------------------------------------------------------
# Fixtures HTML per a tests del html_scraper (Fase 3)
# Estructura verificada contra todofp.es — vegeu 03-RESEARCH.md §Estructura HTML Verificada
# ---------------------------------------------------------------------------


@pytest.fixture
def minimal_html_grado_d_one_record():
    """HTML mínim amb 1 títol d'una família canònica ('Administración y Gestión')."""
    return """
    <html><body><table>
      <thead><tr class="cols">
        <th id="familia">Familia</th>
        <th id="titulacion">Titulación</th>
      </tr></thead>
      <tbody>
        <tr class="fpb">
          <th rowspan="1" headers="familia" id="fam0">
            <img alt="Logotipo Administración y Gestión" src="#">
          </th>
          <td headers="titulacion fam0">
            <p><a id="tit-gestion-administrativa" href="#">Gestión Administrativa</a></p>
          </td>
        </tr>
      </tbody>
    </table></body></html>
    """


@pytest.fixture
def minimal_html_grado_d_two_records_same_family():
    """HTML mínim amb 2 títols de la mateixa família (rowspan=2, segona fila sense <th>)."""
    return """
    <html><body><table>
      <thead><tr class="cols">
        <th id="familia">Familia</th>
        <th id="titulacion">Titulación</th>
      </tr></thead>
      <tbody>
        <tr class="fpb">
          <th rowspan="2" headers="familia" id="fam0">
            <img alt="Logotipo Administración y Gestión" src="#">
          </th>
          <td headers="titulacion fam0">
            <p><a id="tit-gestion-administrativa" href="#">Gestión Administrativa</a></p>
          </td>
        </tr>
        <tr class="fpb">
          <td headers="titulacion fam0">
            <p><a id="tit-servicios-administrativos" href="#">Servicios Administrativos</a></p>
          </td>
        </tr>
      </tbody>
    </table></body></html>
    """


@pytest.fixture
def minimal_html_alias_imagen_y_sonido():
    """HTML mínim amb família 'Imagen y Sonido' — requereix HTML_FAMILY_ALIASES per mapar-la a 'Imagen y Espectáculos'."""
    return """
    <html><body><table>
      <thead><tr class="cols">
        <th id="familia">Familia</th>
        <th id="titulacion">Titulación</th>
      </tr></thead>
      <tbody>
        <tr class="fpgm">
          <th rowspan="1" headers="familia" id="fam1">
            <img alt="Logotipo Imagen y Sonido" src="#">
          </th>
          <td headers="titulacion fam1">
            <p><a id="tit-realizacion-audiovisuales" href="#">Realización de Audiovisuales</a></p>
          </td>
        </tr>
      </tbody>
    </table></body></html>
    """


@pytest.fixture
def minimal_html_unknown_family():
    """HTML mínim amb família no canònica ('Mantenimiento y Servicios a la Producción') — produirà familia='Desconeguda' + warning."""
    return """
    <html><body><table>
      <thead><tr class="cols">
        <th id="familia">Familia</th>
        <th id="titulacion">Titulación</th>
      </tr></thead>
      <tbody>
        <tr class="fpgs">
          <th rowspan="1" headers="familia" id="fam99">
            <img alt="Logotipo Mantenimiento y Servicios a la Producción" src="#">
          </th>
          <td headers="titulacion fam99">
            <p><a id="tit-prevencion-riesgos" href="#">Prevención de Riesgos Profesionales</a></p>
          </td>
        </tr>
      </tbody>
    </table></body></html>
    """
```

NO tocar cap fixture existent. NO introduir noves importacions (pytest ja està importat al fitxer actual).
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -m pytest tests/test_pdf_scraper.py tests/test_pipeline.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^@pytest.fixture" fp-cercador/backend/tests/conftest.py` retorna 11 (7 existents + 4 noves)
    - `grep -q "def minimal_html_grado_d_one_record" fp-cercador/backend/tests/conftest.py` surt 0
    - `grep -q "def minimal_html_grado_d_two_records_same_family" fp-cercador/backend/tests/conftest.py` surt 0
    - `grep -q "def minimal_html_alias_imagen_y_sonido" fp-cercador/backend/tests/conftest.py` surt 0
    - `grep -q "def minimal_html_unknown_family" fp-cercador/backend/tests/conftest.py` surt 0
    - `grep -q "Logotipo Administración y Gestión" fp-cercador/backend/tests/conftest.py` surt 0
    - `grep -q "Logotipo Imagen y Sonido" fp-cercador/backend/tests/conftest.py` surt 0
    - `grep -q "Logotipo Mantenimiento y Servicios a la Producción" fp-cercador/backend/tests/conftest.py` surt 0
    - La suite existent (tests PDF + pipeline) continua passant: `pytest tests/test_pdf_scraper.py tests/test_pipeline.py -q` acaba amb 0 errors
  </acceptance_criteria>
  <done>Les 4 noves fixtures existeixen a conftest.py amb els strings HTML exactes descrits. Cap test existent s'ha trencat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Crear test_html_scraper.py (RED) amb tests que cobreixen HTML-01 a HTML-06</name>
  <files>fp-cercador/backend/tests/test_html_scraper.py</files>
  <read_first>
    - fp-cercador/backend/tests/conftest.py (fixtures creades a Task 1)
    - fp-cercador/backend/tests/test_pdf_scraper.py (patró de tests existents per mantenir coherència)
    - fp-cercador/backend/tests/test_pipeline.py (patró de mock requests.get)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §Code Examples, §Test Framework
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md §test_html_scraper.py
  </read_first>
  <behavior>
    - Fase RED: el fitxer importa de `scrapers.html_scraper` — com que encara no existeix, els tests fallen amb ImportError en la collecció (aquesta és la condició RED)
    - Cobreix HTML-01 (compte de títols), HTML-02 (parse_grado_e), HTML-03 (identificació id="tit-*"), HTML-04 (família correcta), HTML-05 (nivel per subtipus), HTML-06 (codigo=None, plan_antiguo=False, observaciones="")
    - Usa les 4 fixtures creades al Task 1
    - Mocks `scrapers.html_scraper.requests.get` — cap test fa xarxa real
  </behavior>
  <action>
Crear el fitxer NOU `fp-cercador/backend/tests/test_html_scraper.py` amb EXACTAMENT aquest contingut:

```python
"""
test_html_scraper.py — Tests unitaris del html_scraper amb fixtures HTML minimalistes.

Tots els tests són sense xarxa real: scrapers.html_scraper.requests.get està mockat.
Cobreix els requisits HTML-01 a HTML-06 de REQUIREMENTS.md.
"""
import logging
import unittest.mock as mock

import pytest

# Els següents imports fallaran fins que el Plan 02 creï scrapers/html_scraper.py.
# Aquesta és la condició RED del TDD.
from scrapers.html_scraper import (
    HTML_FAMILY_ALIASES,
    _build_fam_map,
    _extract_titols,
    parse_grado_d_basico,
    parse_grado_d_medio,
    parse_grado_d_superior,
    parse_grado_e,
)

from bs4 import BeautifulSoup


# ---------------------------------------------------------------------------
# Helpers
# ---------------------------------------------------------------------------

def _mock_response(html: str) -> mock.Mock:
    """Crea un mock de requests.Response amb .text i .raise_for_status()."""
    resp = mock.Mock()
    resp.text = html
    resp.raise_for_status = mock.Mock()
    return resp


PATCH_REQUESTS_GET = 'scrapers.html_scraper.requests.get'


# ---------------------------------------------------------------------------
# HTML-01, HTML-03: compte de títols i identificació per id="tit-*"
# ---------------------------------------------------------------------------

def test_parse_grado_d_basico_single_title(minimal_html_grado_d_one_record):
    """parse_grado_d_basico retorna 1 registre amb denominació extreta de l'<a id='tit-*'>."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_basico('http://fake')
    assert len(result) == 1
    assert result[0]['denominacion'] == 'Gestión Administrativa'


def test_parse_grado_d_medio_single_title(minimal_html_grado_d_one_record):
    """parse_grado_d_medio retorna 1 registre en un HTML d'1 títol."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_medio('http://fake')
    assert len(result) == 1


def test_parse_grado_d_superior_single_title(minimal_html_grado_d_one_record):
    """parse_grado_d_superior retorna 1 registre en un HTML d'1 títol."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_superior('http://fake')
    assert len(result) == 1


def test_parse_grado_e_single_title(minimal_html_grado_d_one_record):
    """parse_grado_e retorna 1 registre en un HTML d'1 títol (estructura idèntica)."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_e('http://fake')
    assert len(result) == 1


def test_parse_grado_d_two_records_same_family(minimal_html_grado_d_two_records_same_family):
    """2 títols de la mateixa família (rowspan=2) s'extreuen correctament."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_two_records_same_family)):
        result = parse_grado_d_basico('http://fake')
    assert len(result) == 2
    # Ambdós han de tenir la mateixa família
    assert result[0]['familia'] == result[1]['familia']
    assert result[0]['familia'] == 'Administración y Gestión'


# ---------------------------------------------------------------------------
# HTML-04: família correcta per cada registre
# ---------------------------------------------------------------------------

def test_family_mapping_direct(minimal_html_grado_d_one_record):
    """Una família canònica (sense alias) es propaga tal qual al registre."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_basico('http://fake')
    assert result[0]['familia'] == 'Administración y Gestión'


def test_family_mapping_alias_imagen_y_sonido(minimal_html_alias_imagen_y_sonido):
    """'Imagen y Sonido' del HTML es mapa a 'Imagen y Espectáculos' via HTML_FAMILY_ALIASES."""
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_alias_imagen_y_sonido)):
        result = parse_grado_d_medio('http://fake')
    assert len(result) == 1
    assert result[0]['familia'] == 'Imagen y Espectáculos'


def test_html_family_aliases_contains_known_exceptions():
    """HTML_FAMILY_ALIASES conté com a mínim les 2 entrades detectades en recerca."""
    assert 'Imagen y Sonido' in HTML_FAMILY_ALIASES
    assert HTML_FAMILY_ALIASES['Imagen y Sonido'] == 'Imagen y Espectáculos'
    assert 'Artes y Artesanias' in HTML_FAMILY_ALIASES
    assert HTML_FAMILY_ALIASES['Artes y Artesanias'] == 'Artesanía'


def test_family_unknown_becomes_desconeguda_and_warns(minimal_html_unknown_family, caplog):
    """Una família no canònica produeix familia='Desconeguda' i warning al log (D-08)."""
    caplog.set_level(logging.WARNING, logger='scrapers.html_scraper')
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_unknown_family)):
        result = parse_grado_d_superior('http://fake')
    assert len(result) == 1
    assert result[0]['familia'] == 'Desconeguda'
    # Warning emès
    assert any('Desconeguda' in rec.message or 'fam99' in rec.message for rec in caplog.records)


# ---------------------------------------------------------------------------
# HTML-05: nivel correcte per subtipus (Básico→1, Medio→2, Superior→3, E→None)
# ---------------------------------------------------------------------------

def test_nivel_basico_is_1(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_basico('http://fake')
    assert result[0]['nivel'] == 1


def test_nivel_medio_is_2(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_medio('http://fake')
    assert result[0]['nivel'] == 2


def test_nivel_superior_is_3(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_superior('http://fake')
    assert result[0]['nivel'] == 3


def test_nivel_grado_e_is_none(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_e('http://fake')
    assert result[0]['nivel'] is None


# ---------------------------------------------------------------------------
# HTML-06: codigo=None, plan_antiguo=False, observaciones=""
# ---------------------------------------------------------------------------

def test_record_fields_codigo_none(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_basico('http://fake')
    assert result[0]['codigo'] is None


def test_record_fields_plan_antiguo_false(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_basico('http://fake')
    assert result[0]['plan_antiguo'] is False


def test_record_fields_observaciones_empty(minimal_html_grado_d_one_record):
    with mock.patch(PATCH_REQUESTS_GET, return_value=_mock_response(minimal_html_grado_d_one_record)):
        result = parse_grado_d_basico('http://fake')
    assert result[0]['observaciones'] == ''


# ---------------------------------------------------------------------------
# Tests unitaris de helpers privats
# ---------------------------------------------------------------------------

def test_build_fam_map_ignores_header_logo():
    """_build_fam_map NO ha d'incloure l'alt del logo de TodoFP (<img alt='Logotipo de TodoFP'>) perquè no està dins un <th headers='familia'>."""
    html = """
    <html><body>
      <img alt="Logotipo de TodoFP">
      <table><tbody>
        <tr><th headers="familia" id="fam0"><img alt="Logotipo Administración y Gestión"></th></tr>
      </tbody></table>
    </body></html>
    """
    soup = BeautifulSoup(html, 'html.parser')
    fam_map = _build_fam_map(soup)
    assert fam_map == {'fam0': 'Administración y Gestión'}


def test_extract_titols_skips_td_without_fam_id():
    """_extract_titols ignora <td> amb headers que NO continguin cap fam_id."""
    html = """
    <html><body><table><tbody>
      <tr><td headers="titulacion"><a id="tit-x" href="#">Sense família</a></td></tr>
    </tbody></table></body></html>
    """
    soup = BeautifulSoup(html, 'html.parser')
    records = _extract_titols(soup, fam_map={}, nivel=1, grado='D')
    assert records == []


# ---------------------------------------------------------------------------
# Fail fast (D-01): raise_for_status propaga excepció
# ---------------------------------------------------------------------------

def test_parse_grado_d_basico_fail_fast_on_http_error():
    """Si requests.get torna 4xx/5xx, l'excepció es propaga (D-01 fail fast)."""
    import requests as req_lib
    fail_resp = mock.Mock()
    fail_resp.raise_for_status = mock.Mock(
        side_effect=req_lib.exceptions.HTTPError("404 Not Found")
    )
    with mock.patch(PATCH_REQUESTS_GET, return_value=fail_resp):
        with pytest.raises(req_lib.exceptions.HTTPError):
            parse_grado_d_basico('http://fake')
```

NO afegir tests més enllà dels llistats. Aquesta és la suite completa de la fase RED; el Plan 02 implementarà perquè passin.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -m pytest tests/test_html_scraper.py --collect-only 2>&1 | grep -E "(error|ERROR|Error)" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - El fitxer `fp-cercador/backend/tests/test_html_scraper.py` existeix
    - `grep -c "^def test_" fp-cercador/backend/tests/test_html_scraper.py` retorna 18 (o més — no menys)
    - `grep -q "from scrapers.html_scraper import" fp-cercador/backend/tests/test_html_scraper.py` surt 0
    - `grep -q "HTML_FAMILY_ALIASES" fp-cercador/backend/tests/test_html_scraper.py` surt 0
    - `grep -q "def test_parse_grado_d_basico_single_title" fp-cercador/backend/tests/test_html_scraper.py` surt 0
    - `grep -q "def test_nivel_grado_e_is_none" fp-cercador/backend/tests/test_html_scraper.py` surt 0
    - `grep -q "def test_family_mapping_alias_imagen_y_sonido" fp-cercador/backend/tests/test_html_scraper.py` surt 0
    - `grep -q "def test_parse_grado_d_basico_fail_fast_on_http_error" fp-cercador/backend/tests/test_html_scraper.py` surt 0
    - Executant `pytest tests/test_html_scraper.py --collect-only` la sortida CONTÉ la paraula `error` o `ModuleNotFoundError` o `ImportError` (fase RED confirmada — el mòdul `scrapers.html_scraper` no existeix encara)
    - La suite existent passa: `pytest tests/test_pdf_scraper.py tests/test_pipeline.py -q` acaba amb 0 errors
  </acceptance_criteria>
  <done>El fitxer test_html_scraper.py existeix amb ≥18 tests. La col·lecció falla amb ImportError perquè `scrapers.html_scraper` encara no existeix. Els tests PDF/pipeline existents continuen passant.</done>
</task>

<task type="auto">
  <name>Task 3: Actualitzar .env.example amb les 4 noves variables URL</name>
  <files>fp-cercador/backend/.env.example</files>
  <read_first>
    - fp-cercador/backend/.env.example (fitxer a ampliar)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §URLs Verificades
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-CONTEXT.md D-05 (URLs via .env amb fallback hardcode)
  </read_first>
  <action>
Ampliar el fitxer `fp-cercador/backend/.env.example` afegint EXACTAMENT aquestes 5 línies al final (1 línia en blanc + 4 variables URL). El contingut final del fitxer ha de ser:

```
ADMIN_TOKEN=canvia-aquest-token-per-un-de-segur

URL_GRADO_D_BASICO=https://www.todofp.es/que-estudiar/grados-d/fp-grado-basico.html
URL_GRADO_D_MEDIO=https://www.todofp.es/que-estudiar/grados-d/grado-medio.html
URL_GRADO_D_SUPERIOR=https://www.todofp.es/que-estudiar/grados-d/grado-superior.html
URL_GRADO_E=https://www.todofp.es/que-estudiar/grados-e/curso-especializacion.html
```

NO tocar la línia `ADMIN_TOKEN=` existent. Les URLs han de ser literalment les verificades a 03-RESEARCH.md §URLs Verificades (curl HTTP 200 confirmat 2026-04-17).
  </action>
  <verify>
    <automated>grep -c "^URL_GRADO_" "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/.env.example"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^URL_GRADO_" fp-cercador/backend/.env.example` retorna exactament `4`
    - `grep -q "^ADMIN_TOKEN=" fp-cercador/backend/.env.example` surt 0 (línia existent no tocada)
    - `grep -q "^URL_GRADO_D_BASICO=https://www.todofp.es/que-estudiar/grados-d/fp-grado-basico.html$" fp-cercador/backend/.env.example` surt 0
    - `grep -q "^URL_GRADO_D_MEDIO=https://www.todofp.es/que-estudiar/grados-d/grado-medio.html$" fp-cercador/backend/.env.example` surt 0
    - `grep -q "^URL_GRADO_D_SUPERIOR=https://www.todofp.es/que-estudiar/grados-d/grado-superior.html$" fp-cercador/backend/.env.example` surt 0
    - `grep -q "^URL_GRADO_E=https://www.todofp.es/que-estudiar/grados-e/curso-especializacion.html$" fp-cercador/backend/.env.example` surt 0
  </acceptance_criteria>
  <done>.env.example conté la línia ADMIN_TOKEN original + les 4 noves variables URL amb els valors literals verificats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desenvolupador → repositori | `.env.example` és checked-in; ha de contenir NOMÉS valors d'exemple, mai secrets reals |
| Tests → mòdul sota test | Els tests importen `scrapers.html_scraper` que encara no existeix; fase RED controlada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I (Information Disclosure) | `.env.example` | mitigate | Conté NOMÉS URLs públiques (todofp.es) i el placeholder `canvia-aquest-token-per-un-de-segur`. Cap secret real introduït. |
| T-03-02 | T (Tampering) | fixtures HTML a conftest.py | accept | Fixtures contenen només HTML inert (text literal). No s'executa JavaScript ni es fa xarxa. Risc nul. |
| T-03-03 | D (Denial of Service) | tests amb pytest | accept | Els tests són purament unitaris amb `requests.get` mockat. Cap trucada de xarxa real. |
</threat_model>

<verification>
Execució de la suite completa de tests + verificació manual dels artefactes:

```bash
cd fp-cercador/backend

# 1. La suite existent (PDF + pipeline) passa
python -m pytest tests/test_pdf_scraper.py tests/test_pipeline.py -q
# Esperat: 0 errors

# 2. La col·lecció de tests/test_html_scraper.py falla amb ImportError (fase RED)
python -m pytest tests/test_html_scraper.py --collect-only 2>&1 | tail -20
# Esperat: sortida conté "ModuleNotFoundError: No module named 'scrapers.html_scraper'" o equivalent

# 3. Fixtures HTML presents a conftest.py
grep -c "^@pytest.fixture" tests/conftest.py
# Esperat: 11 (7 existents + 4 noves)

# 4. .env.example amb les 4 noves URLs
grep -c "^URL_GRADO_" .env.example
# Esperat: 4
```
</verification>

<success_criteria>
- Tests existents (PDF + pipeline) passen sense modificacions: 0 errors a `pytest tests/test_pdf_scraper.py tests/test_pipeline.py -q`
- `tests/test_html_scraper.py` existeix amb ≥18 tests
- La col·lecció falla amb ImportError de `scrapers.html_scraper` (fase RED confirmada)
- conftest.py té 4 noves fixtures HTML + les 7 fixtures existents
- .env.example té les 4 noves variables URL_GRADO_* amb valors literalment idèntics a les URLs verificades
- Cap altre fitxer del repo tocat
</success_criteria>

<output>
After completion, create `.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-01-SUMMARY.md`
</output>
