---
phase: 03-html-scrapers-data-pipeline-grados-d-e
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - fp-cercador/backend/scrapers/pipeline.py
  - fp-cercador/backend/tests/test_pipeline.py
  - fp-cercador/backend/data/ofertes.json
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "pipeline.run() retorna by_grado amb les 5 claus: A, B, C, D, E"
    - "ofertes.json conté tots els registres dels 5 Grados amb id seqüencial 1..N sense buits"
    - "L'ordre d'IDs és A → B → C → D → E (D-03)"
    - "Si qualsevol URL HTML falla, ofertes.json no s'actualitza (D-01 fail fast, D-02 tot o res)"
    - "La suite completa de tests unitaris (pdf, html, pipeline) passa"
    - "L'execució real contra todofp.es produeix un ofertes.json vàlid amb ~12.374 registres (12.143 PDF + ~231 HTML)"
  artifacts:
    - path: "fp-cercador/backend/scrapers/pipeline.py"
      provides: "pipeline.run() estès amb HTML_URLS + loop HTML D/E"
      contains:
        - "from scrapers.html_scraper import"
        - "HTML_URLS"
        - "html_parsers"
    - path: "fp-cercador/backend/tests/test_pipeline.py"
      provides: "Tests actualitzats per incloure mocks D/E"
      contains: "set(result['by_grado'].keys()) == {'A', 'B', 'C', 'D', 'E'}"
    - path: "fp-cercador/backend/data/ofertes.json"
      provides: "Dataset complet dels 5 Grados"
      contains: "\"grado\": \"E\""
  key_links:
    - from: "scrapers.pipeline"
      to: "scrapers.html_scraper"
      via: "import de les 4 funcions parse_grado_d_*/parse_grado_e"
      pattern: "from scrapers.html_scraper import"
    - from: "scrapers.pipeline.run"
      to: "fp-cercador/backend/data/ofertes.json"
      via: "_write_atomic existent"
      pattern: "_write_atomic\\(all_records, DATA_PATH\\)"
    - from: "HTML_URLS"
      to: ".env.example (URL_GRADO_D_*/URL_GRADO_E)"
      via: "os.getenv amb fallback hardcode"
      pattern: "os.getenv\\('URL_GRADO_"
---

<objective>
Estendre `pipeline.run()` per orquestrar també els scrapers HTML (Grados D i E) i verificar amb execució real contra todofp.es que el pipeline complet produeix un `ofertes.json` vàlid amb els 5 Grados. Inclou un checkpoint humà obligatori per validar el volum, la distribució per grado i l'absència de `familia='Desconeguda'` inesperada.

Purpose: Compleix els requisits DATA-01 a DATA-04. Finalitza la Fase 3: `ofertes.json` conté tot el catàleg FP espanyol (A, B, C, D_Básico, D_Medio, D_Superior, E). Prepara el terreny per la Fase 4 (API Flask).

Nota crítica sobre DATA-04: El requirement diu "800–900 registres totals". La Fase 2 ja va generar 12.143 registres per A/B/C (incloent pla antic). Afegir D/E dóna ~12.374 totals. El checkpoint humà inclou explícitament una pregunta per a l'usuari sobre si actualitzar REQUIREMENTS.md i ROADMAP.md a la nova xifra (~12.000-12.500) o deixar-los stale. Aquesta decisió NO bloqueja el Plan si l'usuari aprova el volum obtingut; només afecta documents de planificació.

Output:
- `pipeline.py` amb HTML_URLS i loop HTML afegit
- `test_pipeline.py` amb mocks D/E i assertions actualitzades
- `ofertes.json` regenerat amb ~12.374 registres dels 5 Grados
- Checkpoint humà aprovat
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-CONTEXT.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md
@.planning/phases/02-pdf-scrapers-grados-a-b-c/02-03-SUMMARY.md
@fp-cercador/backend/scrapers/pipeline.py
@fp-cercador/backend/scrapers/html_scraper.py
@fp-cercador/backend/tests/test_pipeline.py

<interfaces>
<!-- Contractes rellevants per a l'execució d'aquest Plan. -->

API pública de scrapers.html_scraper (creada al Plan 02):
```
HTML_FAMILY_ALIASES: dict[str, str]
parse_grado_d_basico(url)   -> list[dict]   # nivel=1
parse_grado_d_medio(url)    -> list[dict]   # nivel=2
parse_grado_d_superior(url) -> list[dict]   # nivel=3
parse_grado_e(url)          -> list[dict]   # nivel=None
```

API pública actual de scrapers.pipeline:
```
HEADERS: dict                   (NO tocar)
PDF_URLS: dict                  (NO tocar)
DATA_PATH: str                  (NO tocar)
_download_pdf(url) -> str       (NO tocar)
_write_atomic(data, path)       (NO tocar)
run() -> dict                   (AMPLIAR per incloure D i E)
```

Schema dels registres D/E (retornats per html_scraper — camp 'grado' i 'id' afegits per pipeline):
```
{
  "denominacion": str, "familia": str, "nivel": int|None,
  "codigo": None, "plan_antiguo": False, "observaciones": "",
  # + "grado": "D" o "E" (afegit a pipeline)
  # + "id": int (afegit a pipeline, seqüencial A→B→C→D→E)
}
```

URLs oficials verificades (CONTEXT D-05 + RESEARCH §URLs Verificades):
- URL_GRADO_D_BASICO   → https://www.todofp.es/que-estudiar/grados-d/fp-grado-basico.html
- URL_GRADO_D_MEDIO    → https://www.todofp.es/que-estudiar/grados-d/grado-medio.html
- URL_GRADO_D_SUPERIOR → https://www.todofp.es/que-estudiar/grados-d/grado-superior.html
- URL_GRADO_E          → https://www.todofp.es/que-estudiar/grados-e/curso-especializacion.html

Volums esperats de l'execució real (RESEARCH.md):
- Grado D Básico: 34 | Grado D Medio: 67 | Grado D Superior: 94 | Grado E: 36
- Total D: 195 | Total E: 36 | Total D+E: 231
- Total 5 Grados: 12.143 (A+B+C actual) + 231 = ~12.374
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estendre pipeline.py amb HTML_URLS i loop HTML dins run()</name>
  <files>fp-cercador/backend/scrapers/pipeline.py</files>
  <read_first>
    - fp-cercador/backend/scrapers/pipeline.py (fitxer a modificar — estructura actual)
    - fp-cercador/backend/scrapers/html_scraper.py (mòdul a importar)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md §pipeline.py (extensió in-place)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §Patró 2
  </read_first>
  <action>
Modificar `fp-cercador/backend/scrapers/pipeline.py` amb les 4 edicions següents:

**Edit 1 — Afegir import de html_scraper just després de la línia 24 (`from scrapers.pdf_scraper import parse_grado_a, parse_grado_b, parse_grado_c`):**

Afegir el bloc:
```
from scrapers.html_scraper import (
    parse_grado_d_basico,
    parse_grado_d_medio,
    parse_grado_d_superior,
    parse_grado_e,
)
```

**Edit 2 — Afegir HTML_URLS just després del dict PDF_URLS existent (després de la línia 48 que tanca el dict):**

Afegir una línia en blanc i després:
```
HTML_URLS = {
    'D_BASICO':   os.getenv('URL_GRADO_D_BASICO',   'https://www.todofp.es/que-estudiar/grados-d/fp-grado-basico.html'),
    'D_MEDIO':    os.getenv('URL_GRADO_D_MEDIO',    'https://www.todofp.es/que-estudiar/grados-d/grado-medio.html'),
    'D_SUPERIOR': os.getenv('URL_GRADO_D_SUPERIOR', 'https://www.todofp.es/que-estudiar/grados-d/grado-superior.html'),
    'E':          os.getenv('URL_GRADO_E',          'https://www.todofp.es/que-estudiar/grados-e/curso-especializacion.html'),
}
```

**Edit 3 — Actualitzar el docstring de run() (línies ~98-116):**

Canviar dins del docstring:
- `"Executa el pipeline complet per Grados A, B, C."` → `"Executa el pipeline complet per Grados A, B, C, D i E."`
- `"by_grado": {"A": int, "B": int, "C": int}` → `"by_grado": {"A": int, "B": int, "C": int, "D": int, "E": int}`

Si trobes una altra referència a "Grados A, B, C" al docstring, també canvia-la a "Grados A, B, C, D, E".

**Edit 4 — Afegir bloc HTML dins run() entre el final del loop PDF i l'assignació d'IDs (just abans de `for i, record in enumerate(all_records, start=1):` — aprox. línia 146):**

Inserir aquest bloc nou:
```

    # -----------------------------------------------------------------------
    # Bloc HTML: Grados D (Basico/Medio/Superior) i E
    # Ordre D-03: IDs seqüencials A → B → C → D → E (garantit per l'ordre d'aquest
    # bloc DESPRÉS del loop PDF i ABANS de l'enumerate d'IDs).
    # Fail fast D-01: qualsevol excepció de parse_grado_* propaga i _write_atomic
    # NO es crida (ofertes.json roman intacte).
    # -----------------------------------------------------------------------
    html_parsers = [
        (HTML_URLS['D_BASICO'],   parse_grado_d_basico,   'D'),
        (HTML_URLS['D_MEDIO'],    parse_grado_d_medio,    'D'),
        (HTML_URLS['D_SUPERIOR'], parse_grado_d_superior, 'D'),
        (HTML_URLS['E'],          parse_grado_e,          'E'),
    ]

    html_by_grado: dict = {'D': 0, 'E': 0}
    for url, parser_fn, grado_letter in html_parsers:
        records = parser_fn(url)  # fail fast: excepció propagada (D-01)
        for r in records:
            r['grado'] = grado_letter
        html_by_grado[grado_letter] += len(records)
        all_records.extend(records)

    by_grado['D'] = html_by_grado['D']
    by_grado['E'] = html_by_grado['E']
```

Aquest bloc s'executa NOMÉS si el loop PDF ha acabat sense excepcions. L'ordre d'aparició (D→E després d'A→B→C) + l'`enumerate` d'IDs posterior garanteix l'ordre D-03.

NO tocar res més: HEADERS, PDF_URLS, DATA_PATH, `_download_pdf`, `_write_atomic`, la signatura de `run()`, el bloc final d'`enumerate`, ni el `return`. El `return` ja serveix per als 5 grados perquè `by_grado` és el mateix dict que ara té claus A/B/C/D/E.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -c "from scrapers.pipeline import run, HTML_URLS, HEADERS, PDF_URLS; assert set(HTML_URLS.keys()) == {'D_BASICO', 'D_MEDIO', 'D_SUPERIOR', 'E'}; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "from scrapers.html_scraper import" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "parse_grado_d_basico" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "parse_grado_d_medio" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "parse_grado_d_superior" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "parse_grado_e" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "^HTML_URLS = {" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "'URL_GRADO_D_BASICO'" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "'URL_GRADO_E'" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "html_parsers" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `grep -q "html_by_grado" fp-cercador/backend/scrapers/pipeline.py` surt 0
    - `python -c "from scrapers.pipeline import HTML_URLS; assert set(HTML_URLS.keys()) == {'D_BASICO', 'D_MEDIO', 'D_SUPERIOR', 'E'}"` retorna exit code 0 des de `fp-cercador/backend`
    - `grep -q "PDF_URLS = {" fp-cercador/backend/scrapers/pipeline.py` surt 0 (dict PDF_URLS no tocat)
    - `grep -q "^HEADERS = {" fp-cercador/backend/scrapers/pipeline.py` surt 0 (HEADERS no tocat)
    - `grep -q "def _write_atomic" fp-cercador/backend/scrapers/pipeline.py` surt 0 (helper existent intacte)
    - `grep -q "def _download_pdf" fp-cercador/backend/scrapers/pipeline.py` surt 0 (helper existent intacte)
  </acceptance_criteria>
  <done>pipeline.py importa les 4 funcions de html_scraper, defineix HTML_URLS amb os.getenv + fallback literal, i el loop HTML es troba dins run() entre el loop PDF i l'enumerate d'IDs. Cap helper existent modificat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Actualitzar test_pipeline.py amb mocks D/E i 2 tests nous</name>
  <files>fp-cercador/backend/tests/test_pipeline.py</files>
  <read_first>
    - fp-cercador/backend/tests/test_pipeline.py (fitxer a modificar)
    - fp-cercador/backend/scrapers/pipeline.py (referència de patch paths)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md §Tests existents que cal modificar
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §Pitfall 5
  </read_first>
  <behavior>
    - Els mocks dels 4 parsers HTML s'afegeixen a tots els tests existents que criden pipeline.run()
    - El test `test_pipeline_run_returns_correct_schema` verifica que by_grado té les 5 claus
    - Tests nous cobreixen: (a) ordre d'IDs A→B→C→D→E, (b) fail fast si una URL HTML falla
  </behavior>
  <action>
Modificar `fp-cercador/backend/tests/test_pipeline.py` amb les 4 edicions següents:

**Edit 1 — Afegir nous patch paths just després de la línia 41 (`PATCH_TEMPFILE_NTF = ...`):**

```
PATCH_PARSE_D_BASICO   = 'scrapers.pipeline.parse_grado_d_basico'
PATCH_PARSE_D_MEDIO    = 'scrapers.pipeline.parse_grado_d_medio'
PATCH_PARSE_D_SUPERIOR = 'scrapers.pipeline.parse_grado_d_superior'
PATCH_PARSE_E          = 'scrapers.pipeline.parse_grado_e'
```

**Edit 2 — Afegir 4 mocks HTML addicionals a CADA test existent que ja fa `mock.patch(PATCH_PARSE_A, ...)` + `PATCH_PARSE_B` + `PATCH_PARSE_C`.**

Tests afectats (7 en total):
- `test_pipeline_run_returns_correct_schema`
- `test_pipeline_adds_id_and_grado`
- `test_pipeline_fail_fast_on_download_error`
- `test_pipeline_deletes_pdf_on_success`
- `test_pipeline_deletes_pdf_on_error`
- `test_pipeline_atomic_write`
- `test_pipeline_headers_used`

A cada un, dins del `with mock.patch(...):` agregat, afegir les següents 4 línies `mock.patch` amb `return_value=[]` just abans del `mock.patch(PATCH_OS_UNLINK)`:

```
         mock.patch(PATCH_PARSE_D_BASICO, return_value=[]), \
         mock.patch(PATCH_PARSE_D_MEDIO, return_value=[]), \
         mock.patch(PATCH_PARSE_D_SUPERIOR, return_value=[]), \
         mock.patch(PATCH_PARSE_E, return_value=[]), \
```

Excepció — test `test_pipeline_fail_fast_on_download_error` (línies ~149-181): aquest test interromp el pipeline al Grado B (abans d'arribar a HTML). Els mocks HTML es poden afegir però no s'arribaran a cridar — això és correcte i no canvia el comportament del test. Afegeix-los igualment per consistència.

**Edit 3 — Actualitzar l'assertion de `by_grado` al test `test_pipeline_run_returns_correct_schema`:**

Buscar la línia:
```
assert set(result['by_grado'].keys()) == {'A', 'B', 'C'}
```

Reemplaçar per:
```
assert set(result['by_grado'].keys()) == {'A', 'B', 'C', 'D', 'E'}
# D i E valen 0 (mocks retornen []); el total A+B+C ha de coincidir amb result['total']
assert result['by_grado']['D'] == 0
assert result['by_grado']['E'] == 0
```

També cal buscar i actualitzar la línia següent (aprox. línia 90):
```
assert result['total'] == result['by_grado']['A'] + result['by_grado']['B'] + result['by_grado']['C']
```

Reemplaçar per:
```
assert result['total'] == sum(result['by_grado'].values())
```

**Edit 4 — Afegir 2 tests nous al FINAL del fitxer:**

```


# ---------------------------------------------------------------------------
# Test 8: pipeline inclou D/E i manté l'ordre d'IDs A→B→C→D→E
# ---------------------------------------------------------------------------

def test_pipeline_id_order_a_b_c_d_e(tmp_path):
    """Els IDs assignats segueixen l'ordre A, B, C, D, E (D-03)."""
    record_a = _make_record('01', grado_letter='A')
    record_b = _make_record('02', grado_letter='B')
    record_c = _make_record('03', grado_letter='C')
    # Registres D/E simulats (schema de html_scraper — sense 'grado' encara)
    record_d_basico = {'denominacion': 'D-Basico', 'familia': 'X', 'nivel': 1,
                       'codigo': None, 'plan_antiguo': False, 'observaciones': ''}
    record_d_medio = {'denominacion': 'D-Medio', 'familia': 'X', 'nivel': 2,
                      'codigo': None, 'plan_antiguo': False, 'observaciones': ''}
    record_d_superior = {'denominacion': 'D-Superior', 'familia': 'X', 'nivel': 3,
                         'codigo': None, 'plan_antiguo': False, 'observaciones': ''}
    record_e = {'denominacion': 'E-Curso', 'familia': 'X', 'nivel': None,
                'codigo': None, 'plan_antiguo': False, 'observaciones': ''}

    mock_response = mock.Mock()
    mock_response.content = b'%PDF fake'
    mock_response.raise_for_status = mock.Mock()

    written_data = []
    def fake_json_dump(data, f, **kwargs):
        written_data.extend(data)

    output_file = tmp_path / 'ofertes.json'

    with mock.patch(PATCH_REQUESTS_GET, return_value=mock_response), \
         mock.patch(PATCH_PARSE_A, return_value=[record_a]), \
         mock.patch(PATCH_PARSE_B, return_value=[record_b]), \
         mock.patch(PATCH_PARSE_C, return_value=[record_c]), \
         mock.patch(PATCH_PARSE_D_BASICO, return_value=[record_d_basico]), \
         mock.patch(PATCH_PARSE_D_MEDIO, return_value=[record_d_medio]), \
         mock.patch(PATCH_PARSE_D_SUPERIOR, return_value=[record_d_superior]), \
         mock.patch(PATCH_PARSE_E, return_value=[record_e]), \
         mock.patch(PATCH_OS_UNLINK), \
         mock.patch(PATCH_OS_PATH_EXISTS, return_value=True), \
         mock.patch('scrapers.pipeline.DATA_PATH', str(output_file)), \
         mock.patch('scrapers.pipeline.json.dump', side_effect=fake_json_dump), \
         mock.patch('scrapers.pipeline.tempfile.NamedTemporaryFile') as mock_ntf, \
         mock.patch(PATCH_OS_REPLACE):

        tmp_file = tmp_path / 'tmp.json'
        mock_context = mock.MagicMock()
        mock_context.__enter__ = mock.Mock(return_value=mock_context)
        mock_context.__exit__ = mock.Mock(return_value=False)
        mock_context.name = str(tmp_file)
        mock_ntf.return_value = mock_context

        import scrapers.pipeline as pipeline_mod
        result = pipeline_mod.run()

    assert len(written_data) == 7  # A + B + C + 3*D + E
    # Ordre: 3 primers A/B/C (PDF), 4 ultims D/D/D/E (HTML)
    assert written_data[0]['grado'] == 'A' and written_data[0]['id'] == 1
    assert written_data[1]['grado'] == 'B' and written_data[1]['id'] == 2
    assert written_data[2]['grado'] == 'C' and written_data[2]['id'] == 3
    assert written_data[3]['grado'] == 'D' and written_data[3]['id'] == 4
    assert written_data[4]['grado'] == 'D' and written_data[4]['id'] == 5
    assert written_data[5]['grado'] == 'D' and written_data[5]['id'] == 6
    assert written_data[6]['grado'] == 'E' and written_data[6]['id'] == 7
    assert result['by_grado'] == {'A': 1, 'B': 1, 'C': 1, 'D': 3, 'E': 1}


# ---------------------------------------------------------------------------
# Test 9: fail fast si una URL HTML falla — ofertes.json NO s'escriu
# ---------------------------------------------------------------------------

def test_pipeline_fail_fast_on_html_error(tmp_path):
    """Si parse_grado_d_basico llanca excepcio, os.replace NO es crida (D-01, D-02)."""
    mock_response = mock.Mock()
    mock_response.content = b'%PDF fake'
    mock_response.raise_for_status = mock.Mock()

    output_file = tmp_path / 'ofertes.json'

    with mock.patch(PATCH_REQUESTS_GET, return_value=mock_response), \
         mock.patch(PATCH_PARSE_A, return_value=[_make_record('01', grado_letter='A')]), \
         mock.patch(PATCH_PARSE_B, return_value=[_make_record('01', grado_letter='B')]), \
         mock.patch(PATCH_PARSE_C, return_value=[_make_record('01', grado_letter='C')]), \
         mock.patch(PATCH_PARSE_D_BASICO, side_effect=Exception("HTML parse error")), \
         mock.patch(PATCH_PARSE_D_MEDIO, return_value=[]), \
         mock.patch(PATCH_PARSE_D_SUPERIOR, return_value=[]), \
         mock.patch(PATCH_PARSE_E, return_value=[]), \
         mock.patch(PATCH_OS_UNLINK), \
         mock.patch(PATCH_OS_PATH_EXISTS, return_value=True), \
         mock.patch('scrapers.pipeline.DATA_PATH', str(output_file)), \
         mock.patch(PATCH_OS_REPLACE) as mock_replace:

        import scrapers.pipeline as pipeline_mod

        with pytest.raises(Exception, match="HTML parse error"):
            pipeline_mod.run()

        mock_replace.assert_not_called()
```

NO eliminar cap test existent. NO canviar els identifiers `PATCH_PARSE_A/B/C`. L'ordre dels mocks dins el `with` és irrellevant per a pytest.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "PATCH_PARSE_D_BASICO" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -q "PATCH_PARSE_D_MEDIO" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -q "PATCH_PARSE_D_SUPERIOR" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -q "PATCH_PARSE_E" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -q "{'A', 'B', 'C', 'D', 'E'}" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -q "def test_pipeline_id_order_a_b_c_d_e" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -q "def test_pipeline_fail_fast_on_html_error" fp-cercador/backend/tests/test_pipeline.py` surt 0
    - `grep -c "^def test_" fp-cercador/backend/tests/test_pipeline.py` retorna ≥9 (7 anteriors + 2 nous)
    - La suite completa passa: `pytest tests/ -q` acaba amb `0 failed` i exit code 0
    - `pytest tests/test_pipeline.py::test_pipeline_run_returns_correct_schema -v` exit code 0
    - `pytest tests/test_pipeline.py::test_pipeline_id_order_a_b_c_d_e -v` exit code 0
    - `pytest tests/test_pipeline.py::test_pipeline_fail_fast_on_html_error -v` exit code 0
  </acceptance_criteria>
  <done>test_pipeline.py té ≥9 tests (7 existents adaptats + 2 nous), amb assertions {'A','B','C','D','E'} i verificació explícita d'ordre d'IDs A→B→C→D→E. Tota la suite (pdf + html + pipeline) passa.</done>
</task>

<task type="auto">
  <name>Task 3: Execució real del pipeline i generació de ofertes.json</name>
  <files>fp-cercador/backend/data/ofertes.json</files>
  <read_first>
    - fp-cercador/backend/scrapers/pipeline.py (estès a Task 1)
    - fp-cercador/backend/data/ofertes.json (fitxer actual — serà sobreescrit)
    - .planning/phases/02-pdf-scrapers-grados-a-b-c/02-03-SUMMARY.md (referència dels volums A/B/C)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §Nota Crítica DATA-04
  </read_first>
  <action>
Executar el pipeline real contra todofp.es i generar `fp-cercador/backend/data/ofertes.json` amb els 5 Grados. Passos exactes:

**Pas 1 — Executar el pipeline amb logging actiu:**

```bash
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend"
python -c "
import logging, json
logging.basicConfig(level=logging.INFO, format='%(levelname)s %(name)s: %(message)s')
from scrapers.pipeline import run
result = run()
print('--- RESULT ---')
print(json.dumps(result, indent=2, ensure_ascii=False))
" 2>&1 | tee /tmp/fp_pipeline_run.log
```

Durada esperada: 60-90 segons (55s confirmats per A/B/C al Plan 02-03, + 4 requests HTML).

**Pas 2 — Capturar les observacions següents de la sortida:**
- `result['total']` (esperat: 12.000-12.500)
- `result['by_grado']` (esperat: A~8537, B~2786, C~820, D~195, E~36)
- `result['duration_seconds']`
- Warnings de `Família desconeguda` al log (si n'hi ha, apuntar fam_id i Grado afectats)

**Pas 3 — Verificar el fitxer generat amb un script de self-check:**

```bash
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend"
python -c "
import json
from collections import Counter
with open('data/ofertes.json') as f:
    data = json.load(f)
print(f'Total registres: {len(data)}')

by_grado = Counter(r['grado'] for r in data)
print(f'Grados: {sorted(by_grado.keys())}')
for g in ['A','B','C','D','E']:
    print(f'  {g}: {by_grado.get(g,0)}')

fam = Counter(r['familia'] for r in data)
print(f'familia=Desconeguda: {fam.get(\"Desconeguda\",0)}')

ids = sorted(r['id'] for r in data)
print(f'IDs: min={ids[0]} max={ids[-1]} len={len(ids)} seqüencial={ids==list(range(1,len(ids)+1))}')

# Verificar ordre A→B→C→D→E
abc_ids = [r['id'] for r in data if r['grado'] in ('A','B','C')]
de_ids  = [r['id'] for r in data if r['grado'] in ('D','E')]
if abc_ids and de_ids:
    print(f'Ordre OK: max(ABC)={max(abc_ids)} < min(DE)={min(de_ids)} ? {max(abc_ids) < min(de_ids)}')

# Sample D i E (3 primers de cada)
print('--- Sample Grado D ---')
for r in [x for x in data if x['grado']=='D'][:3]:
    print(f'  id={r[\"id\"]} familia={r[\"familia\"]} nivel={r[\"nivel\"]} denominacion={r[\"denominacion\"][:60]}')
print('--- Sample Grado E ---')
for r in [x for x in data if x['grado']=='E'][:3]:
    print(f'  id={r[\"id\"]} familia={r[\"familia\"]} nivel={r[\"nivel\"]} denominacion={r[\"denominacion\"][:60]}')

# Families desconegudes especifiques (si n'hi ha)
desc = [r for r in data if r['familia']=='Desconeguda']
if desc:
    print(f'--- {len(desc)} registres amb familia=Desconeguda ---')
    for r in desc[:10]:
        print(f'  id={r[\"id\"]} grado={r[\"grado\"]} codigo={r[\"codigo\"]} denominacion={r[\"denominacion\"][:60]}')
"
```

**Pas 4 — Documentar les observacions en una variable temporal (per al checkpoint):** recollir total, by_grado, desconeguda_count, durada, llista de famílies desconegudes (si >0).

**Pas 5 — NO fer commit encara** — el checkpoint humà (Task 4) decideix.

**Condicions de fallada:**
- Si `total < 12000` o `total > 13000`: investigar. Pot indicar error en el scraping o canvi al lloc origen.
- Si `grados != {'A','B','C','D','E'}`: un grado no s'ha afegit. Investigar logs.
- Si `familia=Desconeguda > 5` per a D/E: hi ha famílies no mapejades a HTML_FAMILY_ALIASES que caldrà afegir en un fix. Documentar.
- Si IDs no seqüencials: bug lògic al pipeline. Aturar i investigar.

Si es donen les condicions de fallada, NO seguir al Task 4 — reportar-ho a l'usuari.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -c "import json; d=json.load(open('data/ofertes.json')); assert 12000<=len(d)<=13000; g=sorted(set(r['grado'] for r in d)); assert g==['A','B','C','D','E']; ids=sorted(r['id'] for r in d); assert ids==list(range(1,len(ids)+1)); print('SELF-CHECK OK', len(d), 'registres')"</automated>
  </verify>
  <acceptance_criteria>
    - `ls -la fp-cercador/backend/data/ofertes.json` mostra mida > 4 MB
    - `python -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); print(len(d))"` retorna valor entre 12.000 i 13.000
    - `python -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); print(sorted(set(r['grado'] for r in d)))"` retorna `['A', 'B', 'C', 'D', 'E']`
    - `python -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); ids=sorted(r['id'] for r in d); print(ids==list(range(1,len(ids)+1)))"` retorna `True`
    - `python -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); abc=max(r['id'] for r in d if r['grado'] in ('A','B','C')); de=min(r['id'] for r in d if r['grado'] in ('D','E')); print(abc < de)"` retorna `True`
    - `python -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); dr=[r for r in d if r['grado']=='D']; assert all(r['codigo'] is None and r['plan_antiguo'] is False for r in dr); print('D OK', len(dr))"` surt OK
    - `python -c "import json; d=json.load(open('fp-cercador/backend/data/ofertes.json')); er=[r for r in d if r['grado']=='E']; assert all(r['codigo'] is None and r['nivel'] is None and r['plan_antiguo'] is False for r in er); print('E OK', len(er))"` surt OK
  </acceptance_criteria>
  <done>ofertes.json regenerat. Total entre 12.000 i 13.000 registres. Els 5 Grados presents. IDs seqüencials amb ordre A→B→C→D→E. Observacions capturades per al checkpoint humà.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint humà — validació del volum i decisió sobre DATA-04</name>
  <files>fp-cercador/backend/data/ofertes.json</files>
  <read_first>
    - fp-cercador/backend/data/ofertes.json (artefacte a validar)
    - .planning/REQUIREMENTS.md §DATA-04 (requirement potencialment stale)
    - .planning/ROADMAP.md §Phase 3 Success Criterion 5
  </read_first>
  <action>
Aquest és un checkpoint humà — l'executor ha de pausar i demanar a l'usuari que verifiqui el volum de `ofertes.json` i prengui 2 decisions. L'executor NO modifica cap fitxer durant aquest task; només mostra a l'usuari els comandos a executar i captura la resposta.

**Passos que l'executor ha d'executar i mostrar a l'usuari** (són informatius, no modifiquen res):

```bash
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend"

# 1. Volums i distribució
python -c "
import json
from collections import Counter
d = json.load(open('data/ofertes.json'))
print(f'TOTAL: {len(d)}')
g = Counter(r['grado'] for r in d)
for k in ['A','B','C','D','E']:
    print(f'  {k}: {g.get(k,0)}')
"

# 2. Famílies desconegudes
python -c "
import json
from collections import Counter
d = json.load(open('data/ofertes.json'))
desc = [r for r in d if r['familia']=='Desconeguda']
print(f'familia=Desconeguda: {len(desc)}')
for r in desc[:20]:
    print(f'  grado={r[\"grado\"]} codigo={r[\"codigo\"]} denominacion={r[\"denominacion\"][:60]}')
"

# 3. Samples D i E
python -c "
import json
d = json.load(open('data/ofertes.json'))
print('--- Grado D primer ---')
print(json.dumps([r for r in d if r['grado']=='D'][0], ensure_ascii=False, indent=2))
print('--- Grado E primer ---')
print(json.dumps([r for r in d if r['grado']=='E'][0], ensure_ascii=False, indent=2))
"

# 4. Tests
python -m pytest tests/ -q
```

Després mostrar les 2 preguntes següents a l'usuari i capturar les seves respostes.

**Pregunta 1 — DATA-04 / Success Criterion 5 (valors stale):**

El requirement DATA-04 actual diu "~800-900 registres totals". La realitat verificada és ~12.374. Opcions:
- Opció A (recomanada): Actualitzar REQUIREMENTS.md §DATA-04 a "~12.000-12.500" i ROADMAP.md §Phase 3 SC-5 en conseqüència.
- Opció B: Deixar stale i afegir una nota al SC-5 explicant la discrepància.
- Opció C: Cap canvi (documentar a STATE.md §Blockers).

**Pregunta 2 — Famílies desconegudes (si n'han aparegut):**

Si el Pas 2 mostra registres amb familia='Desconeguda', decidir:
- Opció A: Afegir el nom a HTML_FAMILY_ALIASES amb un mapping apropiat (exemple: "Mantenimiento y Servicios a la Producción" → "Instalación y Mantenimiento"). Requereix modificar html_scraper.py i afegir test.
- Opció B: Deixar 'Desconeguda' (consistent amb D-08).
- Opció C: Preservar el nom literal (afegir al dict amb el seu propi valor).

Si cap família és 'Desconeguda' (cas més probable), aquest pas és informatiu.

**Format de la resposta esperada de l'usuari:**
- `approved` — tot OK, posposar decisions (Opció C per defecte)
- `approved, aplicar Opció A a DATA-04` — actualitzar requirements
- `approved, aplicar Opció A a DATA-04 i Opció B a famílies desconegudes` — combinació
- `blocked: [descripció del problema]` — el volum no quadra o un Grado falta

Si l'usuari respon `blocked`, l'executor no continua al Task 5 — reporta el problema i el replanificador (si es crida) diagnosticarà.
  </action>
  <verify>
    <automated>echo "MANUAL: checkpoint humà — cap verificació automàtica possible fins que l'usuari respongui"; test -f "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/data/ofertes.json" && echo "ofertes.json present"</automated>
  </verify>
  <acceptance_criteria>
    - L'usuari ha respost amb un dels formats indicats a `<action>`
    - Si la resposta és `approved` o variants amb opcions: Task 5 s'executa
    - Si la resposta és `blocked`: Task 5 NO s'executa; es reporta el problema
    - El fitxer `fp-cercador/backend/data/ofertes.json` existeix
  </acceptance_criteria>
  <done>L'usuari ha aprovat (o bloquejat) el checkpoint i ha comunicat les seves decisions sobre DATA-04 i famílies desconegudes en el format indicat.</done>
  <what-built>
    - pipeline.py estès amb loop HTML + HTML_URLS
    - test_pipeline.py amb 2 tests nous (ordre D-03 i fail fast HTML) i mocks D/E
    - ofertes.json regenerat amb ~12.374 registres dels 5 Grados (A/B/C/D/E)
    - Tota la suite de tests unitaris passa
  </what-built>
  <how-to-verify>
Vegeu comandos al bloc `<action>`. L'usuari executa els 4 passos (volums, famílies desconegudes, samples D/E, tests) i reporta els resultats.
  </how-to-verify>
  <resume-signal>
Format de resposta esperat:
- `approved`
- `approved, aplicar Opció A a DATA-04`
- `approved, aplicar Opció A a DATA-04 i Opció B a famílies desconegudes`
- `blocked: [descripció del problema]`
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Aplicar decisions del checkpoint i commitejar</name>
  <files>
    - fp-cercador/backend/scrapers/pipeline.py
    - fp-cercador/backend/scrapers/html_scraper.py
    - fp-cercador/backend/tests/test_pipeline.py
    - fp-cercador/backend/tests/test_html_scraper.py
    - fp-cercador/backend/tests/conftest.py
    - fp-cercador/backend/.env.example
    - fp-cercador/backend/data/ofertes.json
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </files>
  <read_first>
    - Decisions del checkpoint (Task 4) — reportades per l'usuari
    - .planning/REQUIREMENTS.md (si s'escull Opció A per DATA-04)
    - .planning/ROADMAP.md (si s'escull Opció A per DATA-04)
    - fp-cercador/backend/scrapers/html_scraper.py (si s'escull Opció A/C per famílies desconegudes)
  </read_first>
  <action>
Aplicar les decisions del checkpoint segons el format exacte reportat per l'usuari al Task 4.

**Accions condicionals:**

**Si l'usuari ha escollit Opció A per DATA-04:**
1. Editar `.planning/REQUIREMENTS.md` §DATA-04:
   - Buscar: `**DATA-04**: El volum generat és d'aproximadament 800–900 registres totals`
   - Reemplaçar per: `**DATA-04**: El volum generat és d'aproximadament 12.000–12.500 registres totals (incloent registres de pla antic per als Grados A, B, C)`
2. Editar `.planning/ROADMAP.md` §Phase 3 Success Criterion 5:
   - Buscar: `The file contains 800–900 total records with unique, sequential IDs spanning all 5 Grados`
   - Reemplaçar per: `The file contains 12.000–12.500 total records (incluent registres de pla antic) with unique, sequential IDs spanning all 5 Grados`

**Si l'usuari ha escollit Opció B per DATA-04:**
1. Afegir a `.planning/STATE.md` §Blockers/Concerns una entrada: `- DATA-04 stale: el requirement esperava 800-900, la realitat és ~12.374. Es manté com a documentació de la intenció original.`

**Si l'usuari ha escollit Opció A per famílies desconegudes (amb un mapping concret):**
1. Editar `fp-cercador/backend/scrapers/html_scraper.py` afegint una línia al `HTML_FAMILY_ALIASES` amb el mapping aprovat.
2. Afegir un test a `tests/test_html_scraper.py` que verifiqui el nou alias.
3. Executar `pytest tests/ -q` per verificar que tots els tests passen.
4. Re-executar el pipeline (com al Task 3) i re-verificar que `familia=Desconeguda = 0`.

**Si l'usuari ha escollit Opció B per famílies desconegudes:** cap canvi de codi; només documentar a STATE.md §Decisions.

**Si l'usuari ha escollit Opció C per famílies desconegudes (preservar nom):**
1. Editar `fp-cercador/backend/scrapers/html_scraper.py` afegint la línia: `"Mantenimiento y Servicios a la Producción": "Mantenimiento y Servicios a la Producción"` al dict.
2. Afegir un test que verifiqui que el nom es preserva.
3. Executar `pytest tests/ -q`.
4. Re-executar el pipeline i re-verificar.

**Commit final (sempre):**

```bash
cd "/Users/rogermasellas/AI/Cercador Graus"
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" commit "feat(03): HTML scrapers D/E + pipeline extension, 5 Grados consolidated" --files fp-cercador/backend/scrapers/pipeline.py fp-cercador/backend/scrapers/html_scraper.py fp-cercador/backend/tests/test_pipeline.py fp-cercador/backend/tests/test_html_scraper.py fp-cercador/backend/tests/conftest.py fp-cercador/backend/.env.example fp-cercador/backend/data/ofertes.json .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md
```

Ajustar la llista de fitxers als que realment s'hagin modificat segons les decisions del checkpoint.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -m pytest tests/ -q && python -c "import json; d=json.load(open('data/ofertes.json')); assert 12000<=len(d)<=13000; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` a `fp-cercador/backend` retorna exit code 0
    - Si s'ha escollit Opció A per DATA-04: `grep -q "12.000" .planning/REQUIREMENTS.md` surt 0 i `grep -q "12.000" .planning/ROADMAP.md` surt 0
    - Si s'ha escollit Opció A/C per famílies desconegudes: `grep -q "Mantenimiento y Servicios" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - El commit git existeix amb el missatge indicat (`git log -1 --oneline | grep "feat(03)"`)
    - El fitxer `fp-cercador/backend/data/ofertes.json` està present a l'HEAD del commit
  </acceptance_criteria>
  <done>Decisions del checkpoint aplicades. Commit final creat incloent tots els fitxers modificats d'aquesta fase. La suite de tests passa. ofertes.json està committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| todofp.es → pipeline | HTML i PDFs descarregats d'un tercer. Fail fast garanteix que un resposta malformada no corrompi ofertes.json. |
| pipeline → ofertes.json | Escriptura atòmica via `_write_atomic` existent. os.replace és atòmic POSIX. |
| pipeline → test_pipeline (mocks) | Els tests mockejen xarxa; cap trucada real. |
| Developer → git repo | Commit final de ofertes.json (~4+ MB JSON sense PII). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | T (Tampering) | pipeline.run() loop HTML | mitigate | Fail fast D-01: qualsevol excepció de parse_grado_* propaga i `_write_atomic` no es crida. ofertes.json roman intacte. Verificat per `test_pipeline_fail_fast_on_html_error`. |
| T-03-11 | I (Information Disclosure) | logging.warning al loop HTML | accept | Els warnings contenen només fam_id i Grado (valors tècnics públics). Cap PII ni secret. |
| T-03-12 | D (Denial of Service) | loop HTML (4 requests) | mitigate | Cada parse_grado_* usa `timeout=30` a `requests.get`. Màxim 4×30 = 120s en pitjor cas. |
| T-03-13 | T (Tampering) | `ofertes.json` committed | accept | Dades públiques (catàleg FP ministerial). Cap PII, cap secret. Commit explícit per reproductibilitat. |
| T-03-14 | S (Spoofing) | HTML_URLS amb fallback hardcode | mitigate | `os.getenv` amb fallback: si `.env` conté URLs falses, sobreescriuen; si no, s'usen les verificades per l'agent de recerca. L'usuari pot blindar a `.env` si cal. |
| T-03-15 | E (Elevation of Privilege) | requests.get | accept | `requests` és l'única biblioteca de xarxa usada; surfície mínima. Cap execució de codi remot. |
</threat_model>

<verification>
```bash
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend"

# 1. Suite completa passa (cap regressió)
python -m pytest tests/ -q
# Esperat: 0 errors, 0 failures

# 2. Pipeline estès correctament importable
python -c "from scrapers.pipeline import HTML_URLS, run; print(sorted(HTML_URLS.keys()))"
# Esperat: ['D_BASICO', 'D_MEDIO', 'D_SUPERIOR', 'E']

# 3. ofertes.json regenerat amb volum esperat
python -c "
import json
from collections import Counter
d = json.load(open('data/ofertes.json'))
print(f'TOTAL: {len(d)}')
g = Counter(r['grado'] for r in d)
print(f'by_grado: {dict(g)}')
ids = sorted(r['id'] for r in d)
print(f'IDs seqüencials: {ids == list(range(1, len(ids)+1))}')
"
# Esperat: TOTAL entre 12000 i 13000, by_grado amb claus {A,B,C,D,E}, IDs seqüencials True
```
</verification>

<success_criteria>
- pipeline.py importa i orquestra les 4 funcions HTML
- HTML_URLS dict amb 4 claus i fallbacks literals verificats
- test_pipeline.py té ≥9 tests, tots passen
- ofertes.json conté 12.000-12.500 registres amb els 5 Grados, IDs seqüencials A→B→C→D→E
- La suite completa de pytest passa a `fp-cercador/backend`
- Checkpoint humà aprovat
- Decisions del checkpoint aplicades (DATA-04 update si escull Opció A, famílies desconegudes resoltes si escullen A/C)
- Commit final creat amb tots els fitxers modificats de la Fase 3
</success_criteria>

<output>
After completion, create `.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-03-SUMMARY.md`
</output>
