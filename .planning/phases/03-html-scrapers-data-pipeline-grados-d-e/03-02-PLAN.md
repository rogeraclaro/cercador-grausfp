---
phase: 03-html-scrapers-data-pipeline-grados-d-e
plan: "02"
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - fp-cercador/backend/scrapers/html_scraper.py
autonomous: true
requirements: [HTML-01, HTML-02, HTML-03, HTML-04, HTML-05, HTML-06]
must_haves:
  truths:
    - "El mòdul scrapers.html_scraper existeix i és importable des dels tests"
    - "Les 4 funcions públiques parse_grado_d_basico/medio/superior/parse_grado_e retornen llistes de registres amb el schema D-10"
    - "La família professional s'infereix correctament usant el mètode B (headers del <td>) de 03-RESEARCH.md"
    - "Les 2 anomalies conegudes de famílies HTML es mapen via HTML_FAMILY_ALIASES"
    - "Una família no canònica produeix familia='Desconeguda' + warning al logger sense aturar el parse"
    - "requests.raise_for_status() propaga HTTPError (fail fast D-01)"
    - "Tota la suite test_html_scraper.py passa (GREEN)"
  artifacts:
    - path: "fp-cercador/backend/scrapers/html_scraper.py"
      provides: "4 funcions parse_grado_* + HTML_FAMILY_ALIASES + helpers _build_fam_map, _extract_titols, _parse_grado_d"
      exports:
        - "HTML_FAMILY_ALIASES"
        - "parse_grado_d_basico"
        - "parse_grado_d_medio"
        - "parse_grado_d_superior"
        - "parse_grado_e"
        - "_build_fam_map"
        - "_extract_titols"
  key_links:
    - from: "scrapers.html_scraper"
      to: "scrapers.pdf_scraper.PREFIX_MAP"
      via: "reutilització del mapeig de famílies canòniques"
      pattern: "from scrapers.pdf_scraper import PREFIX_MAP"
    - from: "scrapers.html_scraper"
      to: "bs4 (BeautifulSoup)"
      via: "parsing HTML"
      pattern: "from bs4 import BeautifulSoup"
    - from: "scrapers.html_scraper"
      to: "requests"
      via: "descàrrega HTTP amb HEADERS"
      pattern: "requests.get(url, headers=HEADERS"
---

<objective>
Implementar `scrapers/html_scraper.py` perquè la suite RED del Plan 01 passi (fase GREEN del TDD). El mòdul conté 4 funcions públiques (`parse_grado_d_basico/medio/superior`, `parse_grado_e`), 3 helpers privats (`_build_fam_map`, `_extract_titols`, `_parse_grado_d`), el dict `HTML_FAMILY_ALIASES` i el dict `HEADERS` requerit per todofp.es.

Purpose: Compleix els requisits HTML-01 a HTML-06. Usa el Mètode B (headers del `<td>`) per a la inferència de família, consistent amb la recomanació de 03-RESEARCH.md. Evita `difflib` i `rapidfuzz` (determinisme + zero dependències noves). Tracta correctament la peculiaritat de BeautifulSoup4 que retorna l'atribut `headers` com a `AttributeValueList` (Pitfall 1).

Output: Un únic fitxer `fp-cercador/backend/scrapers/html_scraper.py` autocontinguts (sense dependències circulars amb `pipeline.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-CONTEXT.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md
@.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-01-PLAN.md
@fp-cercador/backend/scrapers/pdf_scraper.py
@fp-cercador/backend/scrapers/pipeline.py
@fp-cercador/backend/tests/test_html_scraper.py

<interfaces>
<!-- Contractes que aquesta implementació ha de satisfer. Extrets de test_html_scraper.py creat al Plan 01. -->

Imports que han de funcionar (test_html_scraper.py línies 15-22):
```python
from scrapers.html_scraper import (
    HTML_FAMILY_ALIASES,
    _build_fam_map,
    _extract_titols,
    parse_grado_d_basico,
    parse_grado_d_medio,
    parse_grado_d_superior,
    parse_grado_e,
)
```

Schema de sortida per cada registre (CONTEXT.md D-10):
```python
{
    "denominacion": str,      # text de <a id="tit-..."> (strip)
    "familia": str,           # nom canònic o "Desconeguda"
    "nivel": int | None,      # 1/2/3 per D, None per E
    "codigo": None,
    "plan_antiguo": False,
    "observaciones": "",
}
```

HTML_FAMILY_ALIASES (RESEARCH.md §Mapeig de Famílies):
```python
HTML_FAMILY_ALIASES = {
    "Imagen y Sonido": "Imagen y Espectáculos",
    "Artes y Artesanias": "Artesanía",
}
```

HEADERS (font pipeline.py línies 32-42, duplicat NO importat per evitar cicle):
```python
HEADERS = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36",
    "Referer": "https://www.todofp.es/catalogos-registros-sistema-fp/catalogo-nacional-ofertas-sistema.html",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Crear scrapers/html_scraper.py — GREEN de la suite test_html_scraper.py</name>
  <files>fp-cercador/backend/scrapers/html_scraper.py</files>
  <read_first>
    - fp-cercador/backend/tests/test_html_scraper.py (contracte de comportament)
    - fp-cercador/backend/tests/conftest.py (fixtures: estructura HTML exacta esperada)
    - fp-cercador/backend/scrapers/pdf_scraper.py (patró d'imports/logger/docstring a replicar)
    - fp-cercador/backend/scrapers/pipeline.py (font literal del dict HEADERS a duplicar — línies 32-42)
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-RESEARCH.md §Code Examples, §Common Pitfalls
    - .planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-PATTERNS.md §html_scraper.py
  </read_first>
  <behavior>
    - `parse_grado_d_basico(url)` retorna llista de dicts amb `nivel=1`
    - `parse_grado_d_medio(url)` retorna llista de dicts amb `nivel=2`
    - `parse_grado_d_superior(url)` retorna llista de dicts amb `nivel=3`
    - `parse_grado_e(url)` retorna llista de dicts amb `nivel=None`
    - Totes 4 funcions criden `requests.get(url, headers=HEADERS, timeout=30)` i `resp.raise_for_status()`
    - `_build_fam_map(soup)` retorna `{fam_id: nom_canonic}` només per `<th headers='familia'>` (ignora altres `<img alt="Logotipo ...">`)
    - `_extract_titols(soup, fam_map, nivel, grado)` itera per cada `<td>` amb `'titulacion'` al seu atribut `headers`, extreu el fam_id, busca `<a id="tit-*">` i construeix el dict complet
    - Família no mapable → `'Desconeguda'` + `logger.warning(...)`
    - HTTPError de `raise_for_status()` → propaga (fail fast D-01)
  </behavior>
  <action>
Crear el fitxer NOU `fp-cercador/backend/scrapers/html_scraper.py` amb EXACTAMENT aquest contingut:

```python
"""
html_scraper.py — Parsing de pàgines HTML dels Grados D i E del sistema FP espanyol.

Exposa:
  parse_grado_d_basico(url: str)   -> list[dict]   (nivel=1)
  parse_grado_d_medio(url: str)    -> list[dict]   (nivel=2)
  parse_grado_d_superior(url: str) -> list[dict]   (nivel=3)
  parse_grado_e(url: str)          -> list[dict]   (nivel=None)
  HTML_FAMILY_ALIASES: dict[str, str]  -- mapeig de famílies HTML no canòniques

Cada registre retornat té exactament:
  {denominacion, familia, nivel, codigo, plan_antiguo, observaciones}

Camps 'id' i 'grado' els afegeix pipeline.py, NO aquest mòdul.

Decisions aplicades:
  D-01: Fail fast — resp.raise_for_status() propaga HTTPError
  D-04: Funcions per subtipus amb helper intern _parse_grado_d compartit
  D-07: Detecció de família via Mètode B (headers del <td>) — recomanat a 03-RESEARCH.md
  D-08: Família no mapable → familia='Desconeguda' + logger.warning (consistent Fase 2)
  D-10: Schema fix dels registres (codigo=None, plan_antiguo=False, observaciones="")

IMPORTANT — Pitfall 1 (03-RESEARCH.md): BeautifulSoup4 retorna l'atribut `headers`
com a AttributeValueList (llista), no com a string. Cal isinstance(hv, list) sempre.

IMPORTANT — HEADERS duplicat: Es duplica el dict HEADERS de pipeline.py (comptem
com a dues constants independents) per evitar dependència circular, ja que pipeline.py
importarà funcions d'aquest mòdul al Plan 03. Font: fp-cercador/backend/scrapers/pipeline.py
línies 32-42. Si canvia pipeline.HEADERS, cal sincronitzar aquí.
"""
import logging

import requests
from bs4 import BeautifulSoup

logger = logging.getLogger(__name__)


# ---------------------------------------------------------------------------
# Constants
# ---------------------------------------------------------------------------

# Font: pipeline.py línies 32-42 (duplicat intencionat — vegeu docstring).
HEADERS = {
    "User-Agent": (
        "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
        "AppleWebKit/537.36 (KHTML, like Gecko) "
        "Chrome/124.0.0.0 Safari/537.36"
    ),
    "Referer": (
        "https://www.todofp.es/catalogos-registros-sistema-fp/"
        "catalogo-nacional-ofertas-sistema.html"
    ),
}

# Mapeig de noms de família HTML que NO coincideixen literalment amb PREFIX_MAP
# de pdf_scraper.py. Detectats durant recerca (03-RESEARCH.md §Mapeig de Famílies).
# difflib produeix matches incorrectes en aquests 2 casos; el dict explícit és
# determinista. Si apareixen més anomalies en execució real, el warning de
# 'Desconeguda' a _extract_titols ho evidenciarà als logs.
HTML_FAMILY_ALIASES: dict[str, str] = {
    "Imagen y Sonido": "Imagen y Espectáculos",
    "Artes y Artesanias": "Artesanía",
}


# ---------------------------------------------------------------------------
# Funcions privades
# ---------------------------------------------------------------------------


def _build_fam_map(soup: BeautifulSoup) -> dict:
    """
    Retorna {fam_id: nom_canonic} per a cada <th headers='familia'> amb <img alt='Logotipo ...'>.

    Construeix el map UNICAMENT de <th headers='familia'>. Això ignora
    automàticament el logo de capçalera <img alt='Logotipo de TodoFP'> i
    qualsevol altra imatge fora de la taula principal.
    """
    fam_map: dict = {}
    for th in soup.find_all('th', attrs={'headers': 'familia'}):
        fam_id = th.get('id', '')
        img = th.find('img')
        if not img:
            continue
        alt = img.get('alt', '')
        if not alt.startswith('Logotipo '):
            continue
        raw_name = alt[len('Logotipo '):].strip()
        fam_map[fam_id] = HTML_FAMILY_ALIASES.get(raw_name, raw_name)
    return fam_map


def _extract_titols(soup: BeautifulSoup, fam_map: dict, nivel, grado: str) -> list[dict]:
    """
    Extreu tots els títols d'una pàgina HTML del ministeri.

    Recorre els <td> amb 'titulacion' al seu atribut headers. Per cada un:
      1. Localitza el fam_id dins l'atribut headers (la paraula que comença per 'fam').
      2. Cerca el primer <a id='tit-*'> dins el <td>.
      3. Mapeja fam_id → nom de família via fam_map (o 'Desconeguda' amb warning).
      4. Construeix el dict del registre amb el schema fix D-10.

    IMPORTANT: td.get('headers') retorna AttributeValueList (llista) en BeautifulSoup4.
    L'isinstance(hv, list) és obligatori (Pitfall 1 03-RESEARCH.md).
    """
    records: list = []
    for td in soup.find_all('td', attrs={'headers': True}):
        hv = td.get('headers', [])
        hl = hv if isinstance(hv, list) else hv.split()
        if 'titulacion' not in hl:
            continue
        fam_id = next((p for p in hl if p.startswith('fam')), None)
        if not fam_id:
            continue
        a = td.find('a', id=lambda x: x and x.startswith('tit-'))
        if not a:
            continue

        familia = fam_map.get(fam_id)
        if not familia:
            logger.warning(
                f"Família desconeguda per fam_id='{fam_id}' al Grado {grado}"
            )
            familia = 'Desconeguda'

        records.append({
            'denominacion': a.get_text(strip=True),
            'familia': familia,
            'nivel': nivel,
            'codigo': None,
            'plan_antiguo': False,
            'observaciones': '',
        })
    return records


def _parse_grado_d(url: str, nivel: int) -> list[dict]:
    """
    Helper intern compartit pels 3 subtipus Grado D (estructura HTML idèntica).

    nivel: 1 (Básico), 2 (Medio), 3 (Superior).
    Fail fast D-01: raise_for_status() propaga HTTPError.
    """
    resp = requests.get(url, headers=HEADERS, timeout=30)
    resp.raise_for_status()
    soup = BeautifulSoup(resp.text, 'html.parser')
    fam_map = _build_fam_map(soup)
    return _extract_titols(soup, fam_map, nivel=nivel, grado='D')


# ---------------------------------------------------------------------------
# API pública
# ---------------------------------------------------------------------------


def parse_grado_d_basico(url: str) -> list[dict]:
    """Parseja la pàgina HTML de Grado D Básico (nivel=1) i retorna llista de registres."""
    return _parse_grado_d(url, nivel=1)


def parse_grado_d_medio(url: str) -> list[dict]:
    """Parseja la pàgina HTML de Grado D Medio (nivel=2) i retorna llista de registres."""
    return _parse_grado_d(url, nivel=2)


def parse_grado_d_superior(url: str) -> list[dict]:
    """Parseja la pàgina HTML de Grado D Superior (nivel=3) i retorna llista de registres."""
    return _parse_grado_d(url, nivel=3)


def parse_grado_e(url: str) -> list[dict]:
    """
    Parseja la pàgina HTML de Grado E (Cursos d'Especialització) i retorna llista de registres.

    nivel=None per a tots els registres Grado E (HTML-05).
    """
    resp = requests.get(url, headers=HEADERS, timeout=30)
    resp.raise_for_status()
    soup = BeautifulSoup(resp.text, 'html.parser')
    fam_map = _build_fam_map(soup)
    return _extract_titols(soup, fam_map, nivel=None, grado='E')
```

NO crear cap altre fitxer. NO modificar `pipeline.py`, `pdf_scraper.py`, `__init__.py` ni cap test — aquesta és la fase GREEN focalitzada en fer passar la suite RED.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -m pytest tests/test_html_scraper.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - El fitxer `fp-cercador/backend/scrapers/html_scraper.py` existeix
    - `grep -q "^HTML_FAMILY_ALIASES" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "\"Imagen y Sonido\": \"Imagen y Espectáculos\"" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "\"Artes y Artesanias\": \"Artesanía\"" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "^HEADERS = {" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def _build_fam_map" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def _extract_titols" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def _parse_grado_d" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def parse_grado_d_basico" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def parse_grado_d_medio" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def parse_grado_d_superior" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "def parse_grado_e" fp-cercador/backend/scrapers/html_scraper.py` surt 0
    - `grep -q "isinstance(hv, list)" fp-cercador/backend/scrapers/html_scraper.py` surt 0 (Pitfall 1 gestionat)
    - `grep -q "raise_for_status()" fp-cercador/backend/scrapers/html_scraper.py` surt 0 (fail fast D-01)
    - `pytest tests/test_html_scraper.py -q` acaba amb 0 errors i 0 failures
    - La suite completa passa: `pytest tests/ -q` acaba amb 0 errors
  </acceptance_criteria>
  <done>`scrapers/html_scraper.py` existeix amb els elements exportats exactes. Els ≥18 tests de test_html_scraper.py passen (GREEN). Els tests de test_pdf_scraper.py i test_pipeline.py segueixen passant (cap regressió).</done>
</task>

<task type="auto">
  <name>Task 2: REFACTOR opcional — revisió de duplicació i neteja</name>
  <files>fp-cercador/backend/scrapers/html_scraper.py</files>
  <read_first>
    - fp-cercador/backend/scrapers/html_scraper.py (fitxer recent creat)
    - fp-cercador/backend/scrapers/pdf_scraper.py (referència d'estil)
  </read_first>
  <action>
Revisar `scrapers/html_scraper.py` buscant duplicacions no trivials, codi mort o inconsistències d'estil respecte a `pdf_scraper.py`. Criteris explícits:

1. **Duplicació HEADERS:** Acceptada intencionadament (comentat al docstring del fitxer). NO extreure a un mòdul utils.py — el principi del projecte és "simple over clever" (CLAUDE.md). Deixa-ho així.
2. **parse_grado_e vs _parse_grado_d:** parse_grado_e duplica 4 línies del helper però amb `nivel=None` i `grado='E'`. NO refactoritzar a un helper compartit `_parse_html_page(url, nivel, grado)`: `_parse_grado_d` és específic del grado D per claredat semàntica. 4 línies de duplicació < cost d'abstracció.
3. **Imports:** Ordenar stdlib → third-party → local si no ho estan ja. L'ordre actual ja és correcte (logging → requests → bs4).
4. **Docstrings:** Verificar que totes les funcions públiques tenen docstring d'una línia amb el nivel.
5. **Noms de variables:** `hv`, `hl` al `_extract_titols` són curts però context clar (header value, header list). Acceptable; sinònim directe de `headers_val`/`headers_list` no afegeix valor.

Si després de la revisió no hi ha res a canviar (cas esperat), NO fer cap modificació — deixar el commit de GREEN com a final. Documenta "REFACTOR omès (sense duplicació significativa)" al SUMMARY del plan (consistent amb Fase 2 plans 01/02).

Si detectes un problema real (no un gust estètic), aplica el canvi mínim i torna a executar la suite.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - La suite completa continua passant després de la revisió: `pytest tests/ -q` acaba amb 0 errors
    - Si s'han fet canvis: `grep -q "def parse_grado_d_basico" fp-cercador/backend/scrapers/html_scraper.py` segueix sortint 0 (la signatura pública no canvia)
    - El fitxer `scrapers/html_scraper.py` continua exportant els símbols exigits pels tests (HTML_FAMILY_ALIASES, _build_fam_map, _extract_titols, parse_grado_d_basico, parse_grado_d_medio, parse_grado_d_superior, parse_grado_e)
  </acceptance_criteria>
  <done>Tota la suite passa. S'ha documentat al SUMMARY si s'ha refactoritzat o si s'ha omès (recomanat: omès, igual que Fase 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| todofp.es → `html_scraper._parse_grado_d` | HTML descarregat d'un tercer (ministeri) és parsed per BeautifulSoup4 |
| BeautifulSoup4 → `_extract_titols` | El parser normalitza HTML malformat; entrades inesperades poden produir dicts `headers` no-list |
| HTML → `_build_fam_map` → usuari final | Els noms de família són renderitzats més endavant a la UI (Fase 5). Cap sanitizació XSS aquí (HTML rebut és text pla per a camps JSON). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T (Tampering) | `_parse_grado_d`, `parse_grado_e` | mitigate | `resp.raise_for_status()` propaga HTTPError per a 4xx/5xx. Fail fast D-01 garanteix que no s'escriu ofertes.json amb dades parcials. |
| T-03-05 | I (Information Disclosure) | `logger.warning(f"fam_id='{fam_id}'...")` | accept | El log conté només fam_id (valor tècnic públic de l'HTML). Cap PII ni secret. |
| T-03-06 | D (Denial of Service) | `requests.get(url, timeout=30)` | mitigate | Timeout de 30s per request. Si todofp.es és lent, una pàgina no pot bloquejar el pipeline més de 30s. 4 pàgines × 30s = 120s màxim. |
| T-03-07 | E (Elevation of Privilege) | BeautifulSoup4 parsing | accept | BS4 no executa JavaScript ni fa HTTP requests addicionals. Parser `'html.parser'` stdlib (no `lxml` extern). Surface d'atac mínim. |
| T-03-08 | S (Spoofing) | `HEADERS['Referer']` | accept | El Referer està duplicat de pipeline.py per necessitat de todofp.es. No és una mesura de seguretat; és requisit de l'API externa. |
| T-03-09 | I (Information Disclosure) | HTML malformat → `AttributeValueList` | mitigate | `isinstance(hv, list)` garanteix que no llancem AttributeError en entrades on l'atribut sigui un string inesperat (Pitfall 1). |
</threat_model>

<verification>
```bash
cd fp-cercador/backend

# 1. Suite test_html_scraper.py passa completament (GREEN)
python -m pytest tests/test_html_scraper.py -v
# Esperat: 18+ tests, 0 errors, 0 failures

# 2. Suite completa passa (cap regressió)
python -m pytest tests/ -q
# Esperat: 0 errors (tots els tests de pdf_scraper, pipeline i html_scraper)

# 3. Smoke-check manual del fitxer
python -c "from scrapers.html_scraper import parse_grado_d_basico, parse_grado_e, HTML_FAMILY_ALIASES; print('OK', list(HTML_FAMILY_ALIASES.keys()))"
# Esperat: OK ['Imagen y Sonido', 'Artes y Artesanias']
```
</verification>

<success_criteria>
- `scrapers/html_scraper.py` existeix amb 4 funcions públiques, 3 helpers privats, HTML_FAMILY_ALIASES i HEADERS
- Els ≥18 tests de test_html_scraper.py passen (GREEN confirmat)
- La suite completa de pytest acaba amb 0 errors i 0 failures
- Cap modificació de pdf_scraper.py, pipeline.py ni cap altre fitxer
- El mòdul és importable independentment (no hi ha dependència circular amb pipeline.py)
</success_criteria>

<output>
After completion, create `.planning/phases/03-html-scrapers-data-pipeline-grados-d-e/03-02-SUMMARY.md`
</output>
