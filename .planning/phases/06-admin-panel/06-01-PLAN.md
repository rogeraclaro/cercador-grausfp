---
phase: 06-admin-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/backend/app.py
  - fp-cercador/backend/scrapers/pipeline.py
  - fp-cercador/backend/requirements.txt
  - fp-cercador/backend/scheduler_service.py
autonomous: true
requirements:
  - ADMN-04
  - ADMN-05
  - ADMN-06
  - ADMN-07
user_setup: []

must_haves:
  truths:
    - "POST /api/admin/refresh respon 200 a la primera crida amb un token vàlid (bug doble crida resolt)"
    - "POST /api/admin/update-cookies amb Bearer vàlid escriu BUSCADOR_COOKIES al .env i actualitza os.environ en viu"
    - "pipeline.run() recarrega .env a cada execució (load_dotenv override=True), de manera que cookies noves prenen efecte sense reiniciar"
    - "GET /api/admin/scheduler retorna l'estat persistit a backend/data/scheduler.json (o defaults si no existeix)"
    - "POST /api/admin/scheduler activa/actualitza un job APScheduler cron amb day_of_week + hour + minute i el persisteix"
    - "DELETE /api/admin/scheduler atura el job actiu i marca enabled=false al fitxer"
    - "Tots els nous endpoints admin retornen 401 sense token Bearer correcte"
    - "Flask llegeix scheduler.json a l'arrencada i reprograma el job si enabled=true"
  artifacts:
    - path: "fp-cercador/backend/app.py"
      provides: "endpoints update-cookies + scheduler GET/POST/DELETE; bug fix _lock release"
      contains: "/api/admin/update-cookies"
    - path: "fp-cercador/backend/scheduler_service.py"
      provides: "wrapper APScheduler: load_config, save_config, apply_config, init"
      exports: ["init_scheduler", "load_config", "save_config", "apply_config", "DEFAULT_CONFIG"]
    - path: "fp-cercador/backend/scrapers/pipeline.py"
      provides: "load_dotenv(override=True) al principi de run()"
      contains: "load_dotenv(override=True)"
    - path: "fp-cercador/backend/requirements.txt"
      provides: "apscheduler com a nova dependència"
      contains: "apscheduler"
  key_links:
    - from: "app.py admin_refresh"
      to: "refresh_state._lock"
      via: "release al except del bloc thread.start()"
      pattern: "refresh_state\\._lock\\.release\\(\\)"
    - from: "app.py admin_update_cookies"
      to: ".env file"
      via: "_write_env_value atomic"
      pattern: "BUSCADOR_COOKIES"
    - from: "pipeline.run"
      to: "os.environ"
      via: "load_dotenv(override=True)"
      pattern: "load_dotenv\\(override=True\\)"
    - from: "app.py startup"
      to: "scheduler_service.init_scheduler"
      via: "crida a nivell de mòdul"
      pattern: "init_scheduler\\("
---

<objective>
Backend complet del panell admin (Phase 6): corregir el bug de la doble crida a /api/admin/refresh, afegir endpoints per gestionar les cookies del buscador i el scheduler periòdic, i fer que pipeline.run() recarregui .env cada vegada.

Purpose: Permetre que l'operador gestioni el sistema sense SSH (D-01, D-02, D-06, D-12 del CONTEXT) i que el bug del refresh manual quedi resolt abans de construir el frontend que el consumirà.

Output:
- app.py amb 4 nous endpoints (update-cookies, scheduler GET/POST/DELETE) i el bug fix.
- scheduler_service.py: wrapper APScheduler.
- pipeline.py amb load_dotenv(override=True).
- requirements.txt amb apscheduler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-PATTERNS.md

@fp-cercador/backend/app.py
@fp-cercador/backend/refresh_state.py
@fp-cercador/backend/scrapers/pipeline.py
@fp-cercador/backend/requirements.txt

<interfaces>
<!-- Contractes que els executors necessiten directament. Extrets del codebase. -->

From fp-cercador/backend/refresh_state.py:
```python
_lock = threading.Lock()        # mutex de concurrència del refresh
_state_lock = threading.Lock()  # mutex de _state

def get_state() -> dict          # retorna còpia profunda thread-safe
def set_state(**kwargs) -> None  # actualitza camps thread-safe
```

From fp-cercador/backend/app.py:
```python
ADMIN_TOKEN: str                          # llegit de os.environ["ADMIN_TOKEN"]
DATA_PATH: str                            # backend/data/ofertes.json
def _check_auth(req) -> bool              # Bearer + hmac.compare_digest
app: Flask                                # instància Flask
```

From fp-cercador/backend/scrapers/pipeline.py:
```python
def run() -> dict
# retorna {"total": int, "by_grado": {"A":int,"B":int,"C":int,"D":int,"E":int},
#          "errors": [], "duration_seconds": float}
```

APScheduler API (versió no fixada, però l'API és estable des de la 3.x):
```python
from apscheduler.schedulers.background import BackgroundScheduler
sched = BackgroundScheduler()
sched.start()
sched.add_job(func=callable, trigger='cron',
              day_of_week='mon|tue|wed|thu|fri|sat|sun|*',
              hour=int, minute=int,
              id='weekly_refresh', replace_existing=True)
sched.get_job('weekly_refresh')   # retorna Job o None
sched.remove_job('weekly_refresh')
job.next_run_time   # datetime amb tz, format ISO via .isoformat()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bug fix refresh + endpoint update-cookies + load_dotenv a pipeline</name>
  <files>fp-cercador/backend/app.py, fp-cercador/backend/scrapers/pipeline.py</files>
  <read_first>
    - fp-cercador/backend/app.py (línies 95-134, lògica de admin_refresh i el bug)
    - fp-cercador/backend/refresh_state.py (entendre _lock i quan s'allibera)
    - fp-cercador/backend/scrapers/pipeline.py (línies 78-97, capçalera de run())
    - .planning/phases/06-admin-panel/06-PATTERNS.md (secció "Bug doble crida (D-12)" i "_write_env_value")
    - .planning/phases/06-admin-panel/06-CONTEXT.md (D-01, D-02, D-03, D-12)
  </read_first>
  <behavior>
    - El primer POST /api/admin/refresh amb token vàlid amb refresh_state idle ha de retornar 200 + {"status":"started"} (NO 409 espuri).
    - Després d'una excepció abans de t.start(), _lock queda alliberat (no “stuck”).
    - POST /api/admin/update-cookies amb Bearer vàlid i body {"cookies":"JSESSIONID=X; __Host-todofp.es=Y"} retorna 200 i el .env conté la línia exacta `BUSCADOR_COOKIES=JSESSIONID=X; __Host-todofp.es=Y`.
    - POST /api/admin/update-cookies amb body sense JSESSIONID retorna 400 amb {"error":"Invalid cookies format"}.
    - POST /api/admin/update-cookies sense Bearer correcte retorna 401.
    - pipeline.run() crida load_dotenv(override=True) abans de qualsevol scraping; canviar BUSCADOR_COOKIES al .env i tornar a cridar run() fa que el nou valor sigui visible a os.environ.
  </behavior>
  <action>
    1. **Bug fix doble crida (D-12) a `app.py`**: localitzar `admin_refresh` (al voltant de la línia 95). Investigar l'arrel: actualment, si `t.start()` reïx i `_run` allibera el lock al `finally`, però `_run` també crida `set_state(status="running",...)` ABANS del pipeline. Hipòtesi confirmada al PATTERNS: si `_run` triga a executar-se i el client torna a fer POST molt aviat, troba lock adquirit i 409. Però el bug reportat és "mai funciona a la primera". Revisar si `refresh_state.set_state(status="error", errors=[str(exc)])` al `except` deixa el lock adquirit perquè el `finally` el deixa anar — això no és el bug. **El bug real**: després d'una excepció `_run`, el lock queda alliberat però l'estat queda en "error". A la SEGONA crida, `set_state(status="running",...)` sobreescriu i funciona. A la PRIMERA crida amb estat inicial idle el comportament hauria de ser idèntic. Cal **afegir logging** per diagnosticar si t.start() falla silenciosament. **Implementació mínima**: (a) afegir logging detallat a `admin_refresh` (`logger.info("admin_refresh: lock acquired=%s", acquired)` i `logger.info("admin_refresh: thread started id=%s", t.ident)`) ABANS del return; (b) garantir que el lock s'allibera en TOTS els camins d'error del bloc try/except de t.start() (ja ho fa); (c) reordenar `set_state(status="running",...)` perquè s'executi DINS de `admin_refresh` (síncron) ABANS de llançar el thread, no dins de `_run`. Així el client mai veu un transitori "idle" entre `started` i el primer poll. **Codi exacte a substituir** (línies ~95-134 senceres):

```python
@app.route("/api/admin/refresh", methods=["POST"])
def admin_refresh():
    """API-03 / API-04 / API-05: Llança el pipeline en background si el token és vàlid."""
    if not _check_auth(request):
        return jsonify({"error": "Unauthorized"}), 401

    acquired = refresh_state._lock.acquire(blocking=False)
    logger.info("admin_refresh: lock acquired=%s", acquired)
    if not acquired:
        return jsonify({"error": "Refresh already running"}), 409

    # Marcar estat "running" SÍNCRON ABANS de retornar, perquè el polling immediat
    # del client no vegi un transitori "idle" o l'estat "error" anterior (fix D-12).
    refresh_state.set_state(
        status="running",
        last_run=datetime.now(timezone.utc).isoformat(),
        total=None,
        by_grado=None,
        duration_seconds=None,
        errors=[],
    )

    def _run():
        try:
            result = pipeline.run()
            refresh_state.set_state(
                status="done",
                total=result["total"],
                by_grado=result["by_grado"],
                duration_seconds=result["duration_seconds"],
                errors=result["errors"],
            )
        except Exception as exc:
            logger.error("Pipeline refresh failed: %s", exc)
            refresh_state.set_state(status="error", errors=[str(exc)])
        finally:
            refresh_state._lock.release()

    try:
        t = threading.Thread(target=_run, daemon=True)
        t.start()
        logger.info("admin_refresh: thread started ident=%s", t.ident)
    except Exception as exc:
        # Restaurar estat i alliberar lock si no podem llançar el thread
        refresh_state.set_state(status="error", errors=[f"thread start failed: {exc}"])
        refresh_state._lock.release()
        logger.error("Could not start refresh thread: %s", exc)
        return jsonify({"error": "Could not start refresh"}), 500

    return jsonify({"status": "started"}), 200
```

    2. **Helper `_write_env_value`** (afegir a `app.py`, després de `_check_auth`, abans de les rutes). Codi exacte (D-02, patró de PATTERNS):

```python
ENV_PATH = os.path.normpath(
    os.path.join(os.path.dirname(__file__), ".env")
)


def _write_env_value(key: str, value: str, env_path: str = ENV_PATH) -> None:
    """Actualitza o afegeix KEY=value al .env de forma atòmica (os.replace)."""
    import tempfile
    lines = []
    if os.path.exists(env_path):
        with open(env_path, "r", encoding="utf-8") as f:
            lines = f.readlines()
    new_line = f"{key}={value}\n"
    found = False
    for i, line in enumerate(lines):
        if line.startswith(f"{key}="):
            lines[i] = new_line
            found = True
            break
    if not found:
        lines.append(new_line)
    dir_path = os.path.dirname(env_path) or "."
    with tempfile.NamedTemporaryFile(
        mode="w", encoding="utf-8", dir=dir_path, delete=False
    ) as tmp:
        tmp.writelines(lines)
        tmp_path = tmp.name
    os.replace(tmp_path, env_path)
```

    3. **Endpoint `update-cookies`** (afegir DESPRÉS de `admin_refresh`). Codi exacte (D-02, D-03):

```python
@app.route("/api/admin/update-cookies", methods=["POST"])
def admin_update_cookies():
    """D-02: Actualitza BUSCADOR_COOKIES al .env sense restart."""
    if not _check_auth(request):
        return jsonify({"error": "Unauthorized"}), 401
    body = request.get_json(silent=True) or {}
    cookies = (body.get("cookies") or "").strip()
    if "JSESSIONID=" not in cookies:
        return jsonify({"error": "Invalid cookies format"}), 400
    try:
        _write_env_value("BUSCADOR_COOKIES", cookies)
        os.environ["BUSCADOR_COOKIES"] = cookies
    except OSError as exc:
        logger.error("update-cookies write failed: %s", exc)
        return jsonify({"error": "Could not write .env"}), 500
    return jsonify({"status": "ok"}), 200
```

    4. **Modificació `pipeline.py`** (D-01): a `fp-cercador/backend/scrapers/pipeline.py`, afegir import al capçal i crida al principi de `run()`. **Imports** — afegir després de `import time`:

```python
from dotenv import load_dotenv
```

       Després, dins de `run()`, com a PRIMERA línia després del docstring (just abans de `start = time.time()`):

```python
    # D-01 (Phase 6): Recarrega .env perquè un BUSCADOR_COOKIES actualitzat
    # via /api/admin/update-cookies prengui efecte sense reiniciar el servei.
    load_dotenv(override=True)
```

  </action>
  <verify>
    <automated>cd fp-cercador/backend && python -c "import ast,sys; t=ast.parse(open('app.py').read()); names=[n.name for n in ast.walk(t) if isinstance(n,(ast.FunctionDef,))]; assert 'admin_update_cookies' in names and '_write_env_value' in names, names; print('OK app.py')" && python -c "import ast; src=open('scrapers/pipeline.py').read(); assert 'load_dotenv(override=True)' in src, 'missing load_dotenv override'; assert 'from dotenv import load_dotenv' in src; print('OK pipeline.py')" && grep -n "logger.info(\"admin_refresh: lock acquired" app.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "admin_update_cookies" fp-cercador/backend/app.py` retorna ≥ 1.
    - `grep -c "_write_env_value" fp-cercador/backend/app.py` retorna ≥ 2 (definició + crida).
    - `grep -c "load_dotenv(override=True)" fp-cercador/backend/scrapers/pipeline.py` retorna 1.
    - `grep -n "from dotenv import load_dotenv" fp-cercador/backend/scrapers/pipeline.py` retorna 1 línia.
    - `grep -n "set_state(" fp-cercador/backend/app.py | head -1` mostra que `set_state(status="running"` és cridat dins `admin_refresh` (no només dins `_run`).
    - `python -c "import ast; ast.parse(open('fp-cercador/backend/app.py').read())"` no llança SyntaxError.
    - `python -c "import ast; ast.parse(open('fp-cercador/backend/scrapers/pipeline.py').read())"` no llança SyntaxError.
  </acceptance_criteria>
  <done>
    Bug fix aplicat amb logging diagnòstic, endpoint update-cookies funcional, _write_env_value escriu de forma atòmica, pipeline.run() recarrega .env. Cap regressió a admin_refresh existent.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mòdul scheduler_service.py + apscheduler a requirements</name>
  <files>fp-cercador/backend/scheduler_service.py, fp-cercador/backend/requirements.txt</files>
  <read_first>
    - fp-cercador/backend/scrapers/pipeline.py (per entendre què cridarà el job)
    - fp-cercador/backend/refresh_state.py (per coordinar l'estat quan el job es dispari)
    - .planning/phases/06-admin-panel/06-CONTEXT.md (D-06, D-07, D-09)
    - .planning/phases/06-admin-panel/06-PATTERNS.md (secció APScheduler)
    - fp-cercador/backend/requirements.txt
  </read_first>
  <behavior>
    - `load_config()` retorna un dict amb claus `enabled` (bool), `day_of_week` (str), `hour` (int 0-23), `minute` (int 0-59); si `scheduler.json` no existeix, retorna DEFAULT_CONFIG (`{"enabled": False, "day_of_week": "mon", "hour": 3, "minute": 0}`).
    - `save_config(cfg)` escriu `backend/data/scheduler.json` de forma atòmica (tempfile + os.replace).
    - `apply_config(cfg)` afegeix/reemplaça el job `weekly_refresh` si `enabled=True`, l'elimina si `enabled=False`.
    - `init_scheduler(app_logger)` arrenca el BackgroundScheduler i aplica la config persistida.
    - El job, quan es dispari, ha de invocar el pipeline en un thread separat amb la mateixa lògica de lock que `admin_refresh` (no executar dos refreshos simultanis).
    - `get_next_run_iso()` retorna `next_run_time.isoformat()` o `None` si no hi ha job.
  </behavior>
  <action>
    1. **Crear `fp-cercador/backend/scheduler_service.py`** amb el contingut següent (no abreviar):

```python
"""
scheduler_service.py — Wrapper APScheduler per al refresh periòdic (Fase 6, D-06/D-07/D-09).

Persisteix la config a backend/data/scheduler.json. Flask la llegeix a l'arrencada
i reprograma el job. Reutilitza refresh_state._lock per evitar concurrència amb
/api/admin/refresh manual.
"""
import json
import logging
import os
import tempfile
import threading
from datetime import datetime, timezone
from typing import Optional

from apscheduler.schedulers.background import BackgroundScheduler

import refresh_state
from scrapers import pipeline

logger = logging.getLogger(__name__)

CONFIG_PATH = os.path.normpath(
    os.path.join(os.path.dirname(__file__), "data", "scheduler.json")
)

JOB_ID = "weekly_refresh"

VALID_DAYS = {"mon", "tue", "wed", "thu", "fri", "sat", "sun", "*"}

DEFAULT_CONFIG = {
    "enabled": False,
    "day_of_week": "mon",
    "hour": 3,
    "minute": 0,
}

_scheduler: Optional[BackgroundScheduler] = None


def _write_atomic(data: dict, path: str) -> None:
    dir_path = os.path.dirname(path) or "."
    os.makedirs(dir_path, exist_ok=True)
    with tempfile.NamedTemporaryFile(
        mode="w", encoding="utf-8", suffix=".json", dir=dir_path, delete=False
    ) as tmp:
        json.dump(data, tmp, ensure_ascii=False, indent=2)
        tmp_path = tmp.name
    os.replace(tmp_path, path)


def load_config() -> dict:
    """Llegeix scheduler.json o retorna DEFAULT_CONFIG si no existeix / és invàlid."""
    if not os.path.exists(CONFIG_PATH):
        return dict(DEFAULT_CONFIG)
    try:
        with open(CONFIG_PATH, "r", encoding="utf-8") as f:
            cfg = json.load(f)
    except (OSError, json.JSONDecodeError) as exc:
        logger.warning("scheduler.json invalid (%s); usant defaults", exc)
        return dict(DEFAULT_CONFIG)
    # Validació + defaults
    out = dict(DEFAULT_CONFIG)
    out["enabled"] = bool(cfg.get("enabled", False))
    dow = str(cfg.get("day_of_week", "mon")).lower()
    out["day_of_week"] = dow if dow in VALID_DAYS else "mon"
    try:
        out["hour"] = max(0, min(23, int(cfg.get("hour", 3))))
        out["minute"] = max(0, min(59, int(cfg.get("minute", 0))))
    except (TypeError, ValueError):
        out["hour"], out["minute"] = 3, 0
    return out


def save_config(cfg: dict) -> dict:
    """Valida i persisteix la config; retorna la config validada."""
    validated = dict(DEFAULT_CONFIG)
    validated["enabled"] = bool(cfg.get("enabled", False))
    dow = str(cfg.get("day_of_week", "mon")).lower()
    if dow not in VALID_DAYS:
        raise ValueError(f"day_of_week ha de ser un de {sorted(VALID_DAYS)}")
    validated["day_of_week"] = dow
    try:
        h = int(cfg.get("hour", 3))
        m = int(cfg.get("minute", 0))
    except (TypeError, ValueError):
        raise ValueError("hour i minute han de ser enters")
    if not (0 <= h <= 23 and 0 <= m <= 59):
        raise ValueError("hour 0-23, minute 0-59")
    validated["hour"] = h
    validated["minute"] = m
    _write_atomic(validated, CONFIG_PATH)
    return validated


def _scheduled_refresh() -> None:
    """Funció invocada per APScheduler. Reutilitza el mateix lock que admin_refresh."""
    acquired = refresh_state._lock.acquire(blocking=False)
    if not acquired:
        logger.info("scheduled_refresh: refresh ja en curs, omès")
        return
    refresh_state.set_state(
        status="running",
        last_run=datetime.now(timezone.utc).isoformat(),
        total=None,
        by_grado=None,
        duration_seconds=None,
        errors=[],
    )
    try:
        result = pipeline.run()
        refresh_state.set_state(
            status="done",
            total=result["total"],
            by_grado=result["by_grado"],
            duration_seconds=result["duration_seconds"],
            errors=result["errors"],
        )
    except Exception as exc:
        logger.error("scheduled_refresh failed: %s", exc)
        refresh_state.set_state(status="error", errors=[str(exc)])
    finally:
        refresh_state._lock.release()


def apply_config(cfg: dict) -> None:
    """Programa o desprograma el job segons cfg.enabled."""
    if _scheduler is None:
        raise RuntimeError("scheduler not initialized; call init_scheduler() first")
    existing = _scheduler.get_job(JOB_ID)
    if cfg.get("enabled"):
        _scheduler.add_job(
            func=_scheduled_refresh,
            trigger="cron",
            day_of_week=cfg["day_of_week"],
            hour=cfg["hour"],
            minute=cfg["minute"],
            id=JOB_ID,
            replace_existing=True,
        )
        logger.info("scheduler job programat: %s %02d:%02d",
                    cfg["day_of_week"], cfg["hour"], cfg["minute"])
    elif existing is not None:
        _scheduler.remove_job(JOB_ID)
        logger.info("scheduler job eliminat")


def get_next_run_iso() -> Optional[str]:
    if _scheduler is None:
        return None
    job = _scheduler.get_job(JOB_ID)
    if job is None or job.next_run_time is None:
        return None
    return job.next_run_time.isoformat()


def init_scheduler() -> None:
    """Arrenca el BackgroundScheduler i aplica la config persistida."""
    global _scheduler
    if _scheduler is not None:
        return
    _scheduler = BackgroundScheduler()
    _scheduler.start()
    cfg = load_config()
    apply_config(cfg)
```

    2. **Modificar `requirements.txt`** afegint `apscheduler` com a última línia (sense pinning, consistent amb la resta). El fitxer ha de quedar:

```
flask
flask-cors
pdfplumber
requests
beautifulsoup4
python-dotenv
gunicorn
apscheduler
```

  </action>
  <verify>
    <automated>cd fp-cercador/backend && python -c "import ast; t=ast.parse(open('scheduler_service.py').read()); names=[n.name for n in ast.walk(t) if hasattr(n,'name')]; assert all(x in names for x in ['load_config','save_config','apply_config','init_scheduler','get_next_run_iso','_scheduled_refresh']), names; print('OK')" && grep -x "apscheduler" requirements.txt</automated>
  </verify>
  <acceptance_criteria>
    - `fp-cercador/backend/scheduler_service.py` existeix i conté funcions `load_config`, `save_config`, `apply_config`, `init_scheduler`, `get_next_run_iso`, `_scheduled_refresh`.
    - `python -c "import ast; ast.parse(open('fp-cercador/backend/scheduler_service.py').read())"` no llança SyntaxError.
    - `grep -c "DEFAULT_CONFIG" fp-cercador/backend/scheduler_service.py` retorna ≥ 2.
    - `grep -c "VALID_DAYS" fp-cercador/backend/scheduler_service.py` retorna ≥ 2.
    - `grep -x "apscheduler" fp-cercador/backend/requirements.txt` retorna `apscheduler` (línia exacta).
    - `wc -l fp-cercador/backend/requirements.txt` retorna 8 línies.
    - El job ID `weekly_refresh` és l'única constant de jobs (`grep -c "weekly_refresh" fp-cercador/backend/scheduler_service.py` ≥ 2).
  </acceptance_criteria>
  <done>
    Mòdul scheduler_service.py creat amb API completa (load/save/apply/init/get_next_run); apscheduler afegit a requirements; res s'integra encara a app.py (Task 3).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Endpoints scheduler a app.py + init a l'arrencada</name>
  <files>fp-cercador/backend/app.py</files>
  <read_first>
    - fp-cercador/backend/app.py (estat post-Task 1)
    - fp-cercador/backend/scheduler_service.py (estat creat a Task 2)
    - .planning/phases/06-admin-panel/06-CONTEXT.md (D-08, D-09)
  </read_first>
  <behavior>
    - GET /api/admin/scheduler amb Bearer vàlid retorna 200 amb `{"enabled": bool, "day_of_week": str, "hour": int, "minute": int, "next_run": str|null}`.
    - POST /api/admin/scheduler amb Bearer vàlid i body vàlid persisteix la config i programa el job; retorna 200 amb la config validada + next_run.
    - POST /api/admin/scheduler amb day_of_week invàlid (p.ex. "xxx") retorna 400.
    - POST /api/admin/scheduler amb hour=25 retorna 400.
    - DELETE /api/admin/scheduler amb Bearer vàlid posa enabled=false, elimina el job, retorna 200.
    - Tots tres endpoints retornen 401 sense Bearer correcte.
    - A l'arrencada de Flask (import time o abans del primer request), `scheduler_service.init_scheduler()` es crida una sola vegada i, si scheduler.json té enabled=true, programa el job.
  </behavior>
  <action>
    1. **Import** a la part superior de `fp-cercador/backend/app.py` (després dels imports existents, abans de `# Inicialització`):

```python
import scheduler_service
```

    2. **Inicialitzar el scheduler** just després de la línia `CORS(app)` (al voltant de la línia 46 actual):

```python
# Phase 6 (D-06/D-07): Arrenca APScheduler i programa el job persistit (si enabled).
scheduler_service.init_scheduler()
```

    3. **Endpoints** — afegir al final del fitxer, ABANS del bloc `if __name__ == "__main__":`. Codi exacte:

```python
@app.route("/api/admin/scheduler", methods=["GET"])
def scheduler_get():
    """D-08: Retorna l'estat actual del scheduler periòdic."""
    if not _check_auth(request):
        return jsonify({"error": "Unauthorized"}), 401
    cfg = scheduler_service.load_config()
    cfg["next_run"] = scheduler_service.get_next_run_iso()
    return jsonify(cfg), 200


@app.route("/api/admin/scheduler", methods=["POST"])
def scheduler_set():
    """D-08: Actualitza la config del scheduler i la persisteix."""
    if not _check_auth(request):
        return jsonify({"error": "Unauthorized"}), 401
    body = request.get_json(silent=True) or {}
    try:
        validated = scheduler_service.save_config(body)
        scheduler_service.apply_config(validated)
    except ValueError as exc:
        return jsonify({"error": str(exc)}), 400
    except OSError as exc:
        logger.error("scheduler save failed: %s", exc)
        return jsonify({"error": "Could not write scheduler.json"}), 500
    validated["next_run"] = scheduler_service.get_next_run_iso()
    return jsonify(validated), 200


@app.route("/api/admin/scheduler", methods=["DELETE"])
def scheduler_delete():
    """D-08: Desactiva el scheduler i elimina el job."""
    if not _check_auth(request):
        return jsonify({"error": "Unauthorized"}), 401
    cfg = scheduler_service.load_config()
    cfg["enabled"] = False
    try:
        scheduler_service.save_config(cfg)
        scheduler_service.apply_config(cfg)
    except (ValueError, OSError) as exc:
        logger.error("scheduler delete failed: %s", exc)
        return jsonify({"error": "Could not disable scheduler"}), 500
    return jsonify({"status": "disabled"}), 200
```

    4. Verificar que el fitxer parseja amb `python -c "import ast; ast.parse(open('fp-cercador/backend/app.py').read())"`.

    5. **Smoke test backend** (sense fer crides reals al ministeri): instal·lar deps i carregar app.py per assegurar que l'init del scheduler no peta.

```bash
cd fp-cercador/backend && pip install -q apscheduler && \
  ADMIN_TOKEN=test python -c "import app; print('app loaded; routes:', sorted(r.rule for r in app.app.url_map.iter_rules()))"
```

       La sortida ha de contenir `/api/admin/scheduler`, `/api/admin/update-cookies`, `/api/admin/refresh`.
  </action>
  <verify>
    <automated>cd fp-cercador/backend && pip install -q apscheduler && ADMIN_TOKEN=test python -c "import app; rules=sorted(r.rule for r in app.app.url_map.iter_rules()); assert '/api/admin/scheduler' in rules and '/api/admin/update-cookies' in rules and '/api/admin/refresh' in rules, rules; print('routes OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scheduler_service.init_scheduler" fp-cercador/backend/app.py` retorna 1.
    - `grep -c "@app.route(\"/api/admin/scheduler\"" fp-cercador/backend/app.py` retorna 3 (GET, POST, DELETE).
    - `grep -c "import scheduler_service" fp-cercador/backend/app.py` retorna 1.
    - El smoke test del verify imprimeix "routes OK".
    - Cap altre endpoint admin trenca: `grep -c "_check_auth(request)" fp-cercador/backend/app.py` retorna ≥ 5 (refresh + update-cookies + 3 scheduler).
  </acceptance_criteria>
  <done>
    Endpoints scheduler GET/POST/DELETE registrats, init_scheduler cridat a l'arrencada, smoke test confirma totes les rutes carregables sense errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/admin/* | Operador autenticat amb ADMIN_TOKEN; tot input és untrusted |
| Flask process → .env | Escriptura a fitxer al filesystem (BUSCADOR_COOKIES) |
| Flask process → scheduler.json | Escriptura a fitxer de config |
| APScheduler job → pipeline.run | Invocació autònoma sense Bearer (interna) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing / Elevation | /api/admin/update-cookies | mitigate | `_check_auth(request)` com a primera línia + `hmac.compare_digest` (ja existent); 401 si falla. Cobert per acceptance criteria de Task 1. |
| T-06-02 | Spoofing / Elevation | /api/admin/scheduler GET/POST/DELETE | mitigate | `_check_auth(request)` com a primera línia a tots tres endpoints. Cobert per Task 3. |
| T-06-03 | Tampering | Body POST /api/admin/update-cookies | mitigate | Validació `"JSESSIONID=" not in cookies` → 400. No s'executa el cookies string com a codi; s'escriu literal al .env. |
| T-06-04 | Tampering / Injection | Body POST /api/admin/scheduler | mitigate | `save_config()` valida day_of_week contra whitelist `VALID_DAYS`, hour/minute amb int + range check; ValueError → 400. |
| T-06-05 | Information Disclosure | Errors verbosos al client | mitigate | Endpoints retornen missatges genèrics ("Could not write .env"); detalls al `logger.error` (no al body). |
| T-06-06 | Denial of Service | Refresh manual + scheduled simultanis | mitigate | `_scheduled_refresh` reutilitza `refresh_state._lock` (acquire blocking=False); si manual en curs, scheduler omès amb log. |
| T-06-07 | Repudiation | Canvis de cookies/scheduler sense traça | accept | `logger.info` registra accions; sense audit log persistent. Risc baix per a un sistema d'un sol operador. |
| T-06-08 | Tampering | Path traversal en _write_env_value | accept | ENV_PATH és constant relativa a `__file__`; `key` és constant ("BUSCADOR_COOKIES"); no input controla el path. |
| T-06-09 | Information Disclosure | ADMIN_TOKEN al log | mitigate | Logs no inclouen el token (només `acquired=True/False`, `thread ident`). Auditat al codi de Task 1. |

**Notes ADMN-08 (token storage):** Aquest plan no toca el frontend. La garantia de no-localStorage es valida al Plan 02. Aquí només cal assegurar que el backend no exposa el token en cap response (els logs queden al servidor).
</threat_model>

<verification>
- Tots els endpoints existents (`/health`, `/api/ofertes`, `/api/refresh-status`, `/api/admin/refresh`) continuen carregables.
- Els 4 nous endpoints (`/api/admin/update-cookies`, GET/POST/DELETE `/api/admin/scheduler`) apareixen al `app.url_map`.
- Smoke test imports app.py sense errors amb `ADMIN_TOKEN=test`.
- `apscheduler` instal·lable amb `pip install -r requirements.txt`.
</verification>

<success_criteria>
- `python -c "import ast; ast.parse(open('fp-cercador/backend/app.py').read())"` OK.
- `python -c "import ast; ast.parse(open('fp-cercador/backend/scheduler_service.py').read())"` OK.
- `python -c "import ast; ast.parse(open('fp-cercador/backend/scrapers/pipeline.py').read())"` OK.
- Smoke import de `app` amb `ADMIN_TOKEN=test` exposa les 7 rutes esperades.
- `grep` checks de l'acceptance criteria de cada task tots passen.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-01-SUMMARY.md`.
</output>
