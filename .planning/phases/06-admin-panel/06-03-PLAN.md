---
phase: 06-admin-panel
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - deploy/DEPLOY.md
autonomous: false
requirements:
  - ADMN-01
  - ADMN-02
  - ADMN-03
  - ADMN-04
  - ADMN-05
  - ADMN-06
  - ADMN-07
  - ADMN-08

must_haves:
  truths:
    - "DEPLOY.md indica l'admin panel com a mètode recomanat per renovar cookies (D-04 del CONTEXT)"
    - "DEPLOY.md manté com a alternativa el procés SSH però documenta que ja NO cal systemctl restart"
    - "Operador confirma manualment al navegador que les 3 seccions del admin funcionen end-to-end (refresh, cookies, scheduler)"
    - "Operador confirma manualment que el token NO persisteix en tancar la pestanya (ADMN-08)"
  artifacts:
    - path: "deploy/DEPLOY.md"
      provides: "secció actualitzada de renovació de cookies via admin panel"
      contains: "/admin.html"
  key_links:
    - from: "deploy/DEPLOY.md cookies section"
      to: "/admin.html"
      via: "instruccions textuals"
      pattern: "/admin\\.html"
---

<objective>
Tancar la fase 6 amb (a) actualització de la documentació de deploy perquè reflecteixi el nou flux via admin panel i (b) un checkpoint humà que validi end-to-end les tres seccions del panell amb el backend real.

Purpose: Garantir que el sistema instal·lat al VPS s'opera amb el flux nou i que res ha regredit (refresh, cookies, scheduler).

Output:
- `deploy/DEPLOY.md` actualitzat.
- Checkpoint humà aprovat amb evidència (captura o text descriptiu) del comportament correcte.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-PATTERNS.md
@.planning/phases/06-admin-panel/06-01-PLAN.md
@.planning/phases/06-admin-panel/06-02-PLAN.md

@deploy/DEPLOY.md
@fp-cercador/frontend/admin.html
@fp-cercador/backend/app.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Actualitzar DEPLOY.md amb el nou flux d'admin panel</name>
  <files>deploy/DEPLOY.md</files>
  <read_first>
    - deploy/DEPLOY.md (línies 129-177, secció "Renovar les cookies del buscador")
    - .planning/phases/06-admin-panel/06-PATTERNS.md (secció DEPLOY.md amb el text exacte de substitució)
    - .planning/phases/06-admin-panel/06-CONTEXT.md (D-01, D-02)
  </read_first>
  <behavior>
    - DEPLOY.md té una secció "Renovar les cookies del buscador" que recomana fer-ho des de l'admin panel.
    - El procés SSH segueix documentat com a alternativa, però sense l'ordre `systemctl restart` (perquè ja no cal — D-01).
    - Es menciona explícitament la URL del panell admin (`/admin.html`).
    - S'afegeix una nota breu sobre el refresh periòdic com a opció addicional disponible al mateix admin.
  </behavior>
  <action>
    1. Llegir `deploy/DEPLOY.md` complet i localitzar la secció "Actualitzar al VPS" dins el bloc "Renovar les cookies del buscador" (al voltant de les línies 148-175 segons PATTERNS.md).

    2. Substituir el bloc "### Actualitzar al VPS" + el bloc de codi `nano …` + l'ordre `systemctl restart` per aquest contingut exacte:

```markdown
### Actualitzar des del panell d'administració (recomanat — Fase 6)

1. Obre `https://DOMINI_AQUI/admin.html` al navegador.
2. Introdueix l'`ADMIN_TOKEN` al camp de la secció **Refresh manual**.
3. A la secció **Cookies del buscador**, enganxa el valor `JSESSIONID=…; __Host-todofp.es=…` (les instruccions detallades estan dins el desplegable «Com obtenir les cookies?»).
4. Clica **Guardar**. Veuràs «Cookies guardades correctament». El proper refresh aplicarà el nou valor sense reiniciar el servei.

> El token només viu a la memòria del navegador; en tancar la pestanya s'esborra (no es guarda a localStorage).

### Alternativa: actualitzar manualment per SSH

```bash
nano /home/masellas-grausfp/htdocs/grausfp.masellas.info/fp-cercador/backend/.env
# Substituir la línia BUSCADOR_COOKIES=
```

A partir de la Fase 6 **ja no cal `systemctl restart fp-cercador`** — el pipeline crida `load_dotenv(override=True)` a cada execució i recarrega el `.env` automàticament.

### Refresh periòdic (opcional)

Al mateix `/admin.html`, la secció **Refresh periòdic** permet programar un refresh automàtic recurrent (dia de la setmana + hora). La config es persisteix a `backend/data/scheduler.json` i Flask la reprograma a l'arrencada.
```

    3. Verificar que el fitxer continua sintàcticament vàlid Markdown (els blocs de codi tancats correctament).
  </action>
  <verify>
    <automated>grep -c "/admin.html" deploy/DEPLOY.md && grep -c "load_dotenv(override=True)" deploy/DEPLOY.md && grep -c "Refresh periòdic" deploy/DEPLOY.md && ! grep -q "systemctl restart fp-cercador" <(sed -n '/Renovar les cookies/,/^## /p' deploy/DEPLOY.md | grep -v "ja no cal")</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/admin.html" deploy/DEPLOY.md` retorna ≥ 2 (recomanat + alternativa).
    - `grep -c "Refresh periòdic" deploy/DEPLOY.md` retorna ≥ 1.
    - `grep -c "load_dotenv(override=True)" deploy/DEPLOY.md` retorna ≥ 1.
    - `grep -c "Cookies del buscador" deploy/DEPLOY.md` retorna ≥ 1.
    - El text "ja no cal `systemctl restart`" apareix literalment a la secció.
    - Cap línia de la secció nova obliga a un restart explícit (només se'n parla per dir que JA NO cal).
  </acceptance_criteria>
  <done>
    DEPLOY.md actualitzat amb el flux admin panel com a recomanat, alternativa SSH documentada sense restart, secció scheduler mencionada.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint humà — verificació end-to-end al navegador</name>
  <files>(no file changes — només verificació al navegador per part de l'operador)</files>
  <read_first>
    - fp-cercador/frontend/admin.html (per saber què està implementat)
    - fp-cercador/backend/app.py (per conèixer el comportament dels endpoints)
    - .planning/phases/06-admin-panel/06-CONTEXT.md (D-12, ADMN-08)
  </read_first>
  <action>
    Aquest és un checkpoint humà. L'agent NO modifica cap fitxer. La feina és:
    1. Arrencar backend i frontend localment (vegeu el bloc &lt;how-to-verify&gt;).
    2. Presentar a l'operador les instruccions d'aquest checkpoint i pausar fins que escrigui "approved" o reporti errors.
    3. Si l'operador reporta errors, registrar-los a la SUMMARY del plan; NO intentar arreglar-los aquí (crear gap-closure si cal).
  </action>
  <what-built>
    Backend (Plan 01): bug fix refresh, endpoints update-cookies + scheduler GET/POST/DELETE, scheduler_service.py, load_dotenv a pipeline.
    Frontend (Plan 02): admin.html complet amb 3 seccions.
    Docs (Task 1 d'aquest plan): DEPLOY.md actualitzat.
  </what-built>
  <how-to-verify>
    Abans de començar, arrencar el backend:

    ```bash
    cd fp-cercador/backend
    pip install -r requirements.txt
    python app.py    # arrenca a http://localhost:5001
    ```

    Servir el frontend en un segon terminal:

    ```bash
    cd fp-cercador/frontend
    python3 -m http.server 8000
    ```

    Obrir `http://localhost:8000/admin.html`.

    **Bloc A — Refresh manual (ADMN-01..07, fix D-12):**
    1. Sense token, clica "Actualitzar dades" → ha de mostrar "Introdueix el token".
    2. Introdueix un token INCORRECTE → "Token incorrecte".
    3. Introdueix l'`ADMIN_TOKEN` correcte del `.env` i clica "Actualitzar dades".
       - Verifica que **a la PRIMERA crida** apareix "Actualitzant…" amb spinner (NO "Ja hi ha un refresh en curs").
       - Espera fins que aparegui el resum "Refresh completat. Total: N registres" amb la taula per Grado i la durada.
    4. Si en algun moment veus "Refresh fallat", verifica que els errors es llisten clarament.

    **Bloc B — Cookies (D-02, D-04):**
    1. La secció "Cookies del buscador" té el `<details>` tancat per defecte. Obre'l i comprova que les instruccions són completes.
    2. Sense cookies enganxades, clica "Guardar" → "Enganxa primer les cookies".
    3. Enganxa una cadena INVÀLIDA (p.ex. "foo=bar") i clica "Guardar" → "Format invàlid: ha de contenir JSESSIONID=…".
    4. Enganxa una cadena vàlida (`JSESSIONID=TEST123; __Host-todofp.es=TEST456`) i clica "Guardar" → "Cookies guardades correctament".
    5. Verifica al servidor que el `.env` ha estat actualitzat:
       ```bash
       grep BUSCADOR_COOKIES fp-cercador/backend/.env
       ```

    **Bloc C — Scheduler (D-06..D-09):**
    1. Clica "Carregar config actual" → ha d'aparèixer el formulari amb defaults (enabled=false, dilluns 03:00).
    2. Activa el checkbox, posa Dia=Cada dia, Hora=4, Minut=30. Clica "Desar config" → "Config guardada" + "Propera execució: …" amb data formatejada en català.
    3. Recarrega la pàgina, torna a clicar "Carregar config actual" → els valors persisteixen (llegits de `backend/data/scheduler.json`).
    4. Clica "Desactivar" → confirm dialog → "Scheduler desactivat".
    5. Recarrega → la config queda enabled=false.

    **Bloc D — Seguretat (ADMN-08):**
    1. Obre DevTools → Application → Local Storage / Session Storage → confirma que NO hi ha cap entrada amb el token.
    2. Tanca la pestanya, torna-la a obrir → el camp del token està buit (no recordat).

    **Bloc E — Estil (D-10):**
    1. La pàgina té el topbar fosc amb "GrausFP" igual que `index.html`.
    2. La paleta és warm (fons crema, no blanc pur).
  </how-to-verify>
  <verify>
    <automated>echo "MANUAL VERIFICATION — operador ha de respondre 'approved' al resume-signal després de completar Blocs A-E"</automated>
  </verify>
  <acceptance_criteria>
    - Bloc A: la primera crida a "Actualitzar dades" amb token vàlid retorna "Actualitzant…" (no 409). Quan acaba, mostra Total + per Grado + Durada.
    - Bloc B: les 4 sub-validacions de cookies retornen els missatges esperats; .env conté la cadena guardada.
    - Bloc C: form scheduler es carrega, desa, i la config persisteix entre recàrregues; "Desactivar" funciona.
    - Bloc D: DevTools no mostra el token a localStorage ni sessionStorage; tancar la pestanya esborra el camp.
    - Bloc E: topbar fosc i palette warm coincideixen amb index.html.
    - L'operador escriu "approved" al resume-signal.
  </acceptance_criteria>
  <done>
    Tots cinc blocs (A-E) verificats per l'operador i aprovats explícitament.
  </done>
  <resume-signal>
    Escriu "approved" si tots els blocs A-E passen. Si qualsevol falla, descriu quin bloc, quin pas i el comportament observat (logs del backend i de la consola del navegador són útils).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Aquest plan només toca documentació + verificació humana; no introdueix nova superfície d'atac. Hereta el model dels Plans 01 i 02.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-16 | Information Disclosure | DEPLOY.md amb URL real del VPS | accept | Documentació interna del propietari; el placeholder `DOMINI_AQUI` s'utilitza per evitar fixar el host real al repo. |
| T-06-17 | Information Disclosure | Token visible al checkpoint | accept | El checkpoint demana l'`ADMIN_TOKEN` real al humà; queda a la sessió del navegador del operador. Cap registre persistent. |
| T-06-18 | Tampering | Backdoor "_TEST" cookies escrites al .env durant Bloc B | mitigate | El checkpoint demana al operador que **substitueixi després** les cookies de test per les reals abans de qualsevol refresh contra el ministeri en producció. Documentar a la SUMMARY. |

**ADMN-08 verification:** Bloc D del checkpoint exercita explícitament la garantia de no-persistència del token (DevTools + close-tab test).
</threat_model>

<verification>
- DEPLOY.md actualitzat (acceptance criteria de Task 1).
- Checkpoint humà aprovat (resume signal "approved").
</verification>

<success_criteria>
- DEPLOY.md cobreix admin panel + alternativa SSH sense restart.
- Tots els blocs A-E del checkpoint passen.
- Cap regressió a /api/admin/refresh (primera crida funciona).
- Cookies aplicables sense restart.
- Scheduler persisteix entre arrencades.
- Token mai a localStorage/sessionStorage.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-03-SUMMARY.md`.
</output>
