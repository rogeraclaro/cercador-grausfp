---
phase: 04-flask-api
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - API-09

must_haves:
  truths:
    - "El servidor Flask arrenca localment sense errors"
    - "curl GET /health retorna {\"status\": \"ok\"} (200)"
    - "curl GET /api/ofertes retorna 12.374 registres reals (200)"
    - "curl POST /api/admin/refresh amb token correcte retorna {\"status\": \"started\"} en < 1 segon"
    - "curl POST /api/admin/refresh amb token incorrecte retorna 401"
    - "curl GET /api/refresh-status mostra l'estat correcte (running → done)"
    - "El pipeline real s'executa en background i actualitza /api/refresh-status"
  artifacts:
    - path: "fp-cercador/backend/app.py"
      provides: "Servidor Flask funcional verificat amb curl contra el servidor real"
      contains: "hmac.compare_digest"
    - path: "fp-cercador/backend/.env"
      provides: "ADMIN_TOKEN configurat localment (NO al repo)"
      contains: "ADMIN_TOKEN="
  key_links:
    - from: "curl /api/admin/refresh"
      to: "pipeline.run() en background thread"
      via: "threading.Thread(target=_run, daemon=True).start()"
      pattern: "daemon=True"
    - from: "curl /api/refresh-status"
      to: "refresh_state._state"
      via: "refresh_state.get_state()"
      pattern: "get_state"
---

<objective>
Verificar que l'API Flask funciona correctament en execució real al servidor local. Arrenca el servidor, executa curl per a cada endpoint i confirma el comportament end-to-end incloent el pipeline real en background.

Purpose: Els tests unitaris del pla 01-02 moquen el pipeline. Aquest pla verifica que la integració real (Flask + pipeline real + ofertes.json) funciona correctament. Necessari per passar el phase gate.

Output:
- Servidor Flask arrencat i verificat via curl
- Pipeline real executat en background amb monitorització via /api/refresh-status
- Checkpoint humà per confirmar el comportament del refresh en temps real
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/PROJECT.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/ROADMAP.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-02-SUMMARY.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-RESEARCH.md

<interfaces>
<!-- Estat esperat de l'entorn en arribar a aquest pla -->

De fp-cercador/backend/app.py (implementat al pla 02):
- GET /health → 200 {"status": "ok"}
- GET /api/ofertes → 200 [array] o 503
- GET /api/refresh-status → 200 {status, last_run, total, by_grado, duration_seconds, errors}
- POST /api/admin/refresh → 200 {"status":"started"} o 401 o 409

De fp-cercador/backend/data/ofertes.json:
- 3.7 MB, 12.374 registres reals

Configuració necessària:
- fp-cercador/backend/.env amb ADMIN_TOKEN=<secret> (crear si no existeix)
- .env.example ja existent amb placeholder segur
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verificació automatitzada de tots els endpoints via curl</name>
  <files></files>
  <read_first>
    - fp-cercador/backend/.env (verificar si existeix i té ADMIN_TOKEN configurat)
    - fp-cercador/backend/.env.example (veure el format esperat)
    - fp-cercador/backend/app.py (verificar les rutes implementades i el guard ADMIN_TOKEN)
  </read_first>
  <action>
Executar les verificacions curl següents en ordre. Prerequisit: el servidor Flask ha d'estar arrencat en una terminal separada.

PAS 0 — Verificar que .env existeix amb ADMIN_TOKEN:
```bash
# Si .env no existeix o no té ADMIN_TOKEN, crear-lo:
# echo "ADMIN_TOKEN=local-dev-token-$(openssl rand -hex 8)" >> fp-cercador/backend/.env
ls -la "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/.env" 2>/dev/null || echo "MISSING"
```

PAS 1 — Arrencar el servidor en background (port 5001 per evitar conflictes):
```bash
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  python3 app.py &
SERVER_PID=$!
sleep 2  # Esperar que el servidor arranqui
```

PAS 2 — Verificar /health:
```bash
curl -s http://localhost:5000/health | python3 -m json.tool
# Esperat: {"status": "ok"}
```

PAS 3 — Verificar /api/ofertes (recompte de registres):
```bash
curl -s http://localhost:5000/api/ofertes | python3 -c "import sys,json; d=json.load(sys.stdin); print(f'Total: {len(d)} registres')"
# Esperat: Total: 12374 registres
```

PAS 4 — Verificar /api/ofertes estructura del primer registre:
```bash
curl -s http://localhost:5000/api/ofertes | python3 -c "import sys,json; d=json.load(sys.stdin); print(json.dumps(d[0], indent=2, ensure_ascii=False))"
# Esperat: objecte amb id, grado, nivel, familia, codigo, denominacion, plan_antiguo, observaciones
```

PAS 5 — Verificar /api/admin/refresh amb token incorrecte (401):
```bash
curl -s -X POST http://localhost:5000/api/admin/refresh \
  -H "Authorization: Bearer token-incorrecte" | python3 -m json.tool
# Esperat: {"error": "Unauthorized"} i status 401
```

PAS 6 — Verificar /api/admin/refresh sense header (401):
```bash
curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:5000/api/admin/refresh
# Esperat: 401
```

PAS 7 — Verificar CORS headers:
```bash
curl -s -I http://localhost:5000/health -H "Origin: http://localhost:3000" | grep -i "access-control"
# Esperat: Access-Control-Allow-Origin: *
```

PAS 8 — Verificar /api/refresh-status inicial (idle):
```bash
curl -s http://localhost:5000/api/refresh-status | python3 -m json.tool
# Esperat: {"status": "idle", "last_run": null, "total": null, "by_grado": null, "duration_seconds": null, "errors": []}
```

NOTA: El ADMIN_TOKEN s'ha de llegir del fitxer .env real. Usar el token configurat a .env per al Pas 9 (al checkpoint humà).
  </action>
  <verify>
    <automated>curl -s http://localhost:5000/health 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); assert d=={'status':'ok'}, f'ERROR: {d}'; print('health OK')" || echo "Servidor no disponible — arrencar primer"</automated>
  </verify>
  <acceptance_criteria>
    - GET /health retorna exactament {"status": "ok"} amb status code 200
    - GET /api/ofertes retorna 12.374 registres (len(data) == 12374)
    - GET /api/ofertes primer registre té camps: id, grado, nivel, familia, codigo, denominacion, plan_antiguo, observaciones
    - POST /api/admin/refresh amb "Bearer token-incorrecte" retorna status 401
    - POST /api/admin/refresh sense header retorna status 401
    - GET /api/refresh-status retorna {"status": "idle", "last_run": null, "total": null, "by_grado": null, "duration_seconds": null, "errors": []}
    - Resposta de /health inclou header "Access-Control-Allow-Origin"
  </acceptance_criteria>
  <done>Tots els endpoints responen correctament via curl al servidor Flask real.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificació humana del refresh real en background</name>
  <what-built>
Flask app amb POST /api/admin/refresh que llança el pipeline real (html_scraper + pdf_scraper) en un thread daemon en background i retorna {"status": "started"} immediatament. L'estat es monitoritza via GET /api/refresh-status.
  </what-built>
  <how-to-verify>
Necessites el servidor Flask en execució (arrencat al Task 1) i l'ADMIN_TOKEN del fitxer .env.

PREREQUISIT — Obtenir el token:
```bash
grep ADMIN_TOKEN "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/.env"
# Anota el valor de TOKEN per usar-lo a les comandes de sota
```

PAS 1 — Llançar el refresh i verificar resposta immediata (< 1 segon):
```bash
time curl -s -X POST http://localhost:5000/api/admin/refresh \
  -H "Authorization: Bearer <TOKEN>"
# Esperat: {"status": "started"} en menys d'1 segon
```

PAS 2 — Monitoritzar l'estat immediatament després:
```bash
curl -s http://localhost:5000/api/refresh-status | python3 -m json.tool
# Esperat: {"status": "running", "last_run": "2026-...", ...}
```

PAS 3 — Verificar que l'API segueix responent mentre el pipeline corre (non-blocking):
```bash
# Mentre el pipeline corre (~45s), l'API ha de seguir responent
curl -s http://localhost:5000/health
# Esperat: {"status": "ok"} — sense bloqueig
```

PAS 4 — Esperar que el pipeline acabi i verificar l'estat final:
```bash
# Esperar ~60 segons i tornar a consultar
sleep 65 && curl -s http://localhost:5000/api/refresh-status | python3 -m json.tool
# Esperat: {"status": "done", "total": 12374, "by_grado": {"A": 8537, "B": 2786, "C": 820, "D": 195, "E": 36}, "duration_seconds": <número>, "errors": []}
```

PAS 5 — Verificar 409 quan el pipeline ja ha acabat i es torna a llançar ràpidament:
```bash
# Llançar 2 refresh seguits ràpidament
curl -s -X POST http://localhost:5000/api/admin/refresh -H "Authorization: Bearer <TOKEN>" &
curl -s -X POST http://localhost:5000/api/admin/refresh -H "Authorization: Bearer <TOKEN>"
# El segon ha de retornar {"error": "Refresh already running"} amb status 409
```

CONFIRMA que:
- [ ] {"status": "started"} arriba immediatament (< 1 segon)
- [ ] /api/refresh-status mostra "running" mentre el pipeline s'executa
- [ ] L'API segueix responent durant el refresh (non-blocking confirmat)
- [ ] Quan acaba, status = "done" amb total = 12374
- [ ] by_grado = {"A": 8537, "B": 2786, "C": 820, "D": 195, "E": 36}
  </how-to-verify>
  <resume-signal>Escriu "aprovat" si tots els punts de la llista estan confirmats, o descriu qualsevol problema trobat.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl → Flask (local) | Verificació local; no hi ha xarxa pública exposada en aquest pla |
| Flask thread → todofp.es | El pipeline real fa requests HTTP a l'exterior durant el refresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | .env amb ADMIN_TOKEN real | mitigate | Verificar que .env està a .gitignore (configurat Fase 1). Mai mostrar el token als logs ni als SUMMARY. El checkpoint usa `<TOKEN>` com a placeholder — el token real no es registra en cap fitxer de planificació. |
| T-04-03-02 | Denial of Service | Pipeline real durant la verificació | accept | El pipeline triga ~45s i fa requests reals a todofp.es. Verificació única en entorn local. No hi ha risc de flood ja que el lock retorna 409. |
| T-04-03-03 | Tampering | ofertes.json sobreescrit pel pipeline real | accept | El pipeline escriu atòmicament (temp file + os.replace). Els 12.374 registres existents es reemplaten per dades fresques iguals. Comportament esperat i desitjat. |
</threat_model>

<verification>
```bash
# Verificació final — suite completa ha de seguir en verd
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  ADMIN_TOKEN=test python3 -m pytest -q
# Esperat: 64 passed (o més si s'han afegit tests)

# Verificació d'estructura app.py — guards de seguretat presents
grep -n "hmac.compare_digest\|daemon=True\|finally:\|if not ADMIN_TOKEN" \
  "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/app.py"
```
</verification>

<success_criteria>
- Tots els endpoints responen correctament via curl al servidor real
- POST /api/admin/refresh retorna {"status": "started"} en < 1 segon (non-blocking confirmat)
- /api/refresh-status mostra "done" amb total = 12374 i by_grado correcte
- El checkpoint humà confirma el comportament del refresh end-to-end
- Suite completa de tests segueix en verd (64 tests)
- Fase 4 verificada i preparada per /gsd-verify-work
</success_criteria>

<output>
Després de completar, crear `.planning/phases/04-flask-api/04-03-SUMMARY.md`
</output>
