---
phase: 04-flask-api
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - fp-cercador/backend/app.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - API-09

must_haves:
  truths:
    - "GET /health retorna {\"status\": \"ok\"} (200) sense autenticació"
    - "GET /api/ofertes retorna 200 + array JSON quan ofertes.json existeix"
    - "GET /api/ofertes retorna 503 + {\"error\": ...} quan ofertes.json no existeix"
    - "POST /api/admin/refresh amb token correcte retorna {\"status\": \"started\"} (200) immediatament"
    - "POST /api/admin/refresh amb token incorrecte retorna 401"
    - "POST /api/admin/refresh quan hi ha procés en curs retorna 409"
    - "GET /api/refresh-status retorna status, last_run, total, by_grado, duration_seconds, errors"
    - "Totes les respostes inclouen header Access-Control-Allow-Origin (CORS actiu)"
    - "ADMIN_TOKEN buit a l'inici llança RuntimeError (guard de seguretat)"
  artifacts:
    - path: "fp-cercador/backend/app.py"
      provides: "Flask app amb 4 rutes: /health, /api/ofertes, /api/refresh-status, /api/admin/refresh"
      exports: ["app"]
      contains: "hmac.compare_digest"
  key_links:
    - from: "app.py"
      to: "refresh_state.py"
      via: "import refresh_state; refresh_state.get_state() / set_state() / _lock"
      pattern: "import refresh_state"
    - from: "app.py"
      to: "scrapers/pipeline.py"
      via: "from scrapers import pipeline; pipeline.run()"
      pattern: "from scrapers import pipeline"
    - from: "app.py (_check_auth)"
      to: "ADMIN_TOKEN env var"
      via: "hmac.compare_digest(provided, ADMIN_TOKEN)"
      pattern: "hmac.compare_digest"
---

<objective>
Implementar totes les rutes Flask a app.py per fer GREEN la suite de tests del pla 01. Totes les 9 rutes s'implementen en un únic fitxer (sense Blueprints — 5 endpoints no justifiquen la complexitat addicional).

Purpose: Convertir l'estat RED de test_api.py en GREEN. Quan tots els tests passen, la Fase 4 és funcional excepte la verificació curl en el servidor real.

Output:
- app.py ampliat amb 4 rutes (/health, /api/ofertes, /api/refresh-status, /api/admin/refresh)
- refresh_state.py (creat al pla 01) consumit per app.py
- 9/9 tests de test_api.py en verd
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/PROJECT.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/ROADMAP.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/STATE.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-01-SUMMARY.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-RESEARCH.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-PATTERNS.md

<interfaces>
<!-- Contractes que app.py ha de consumir. Llegir ABANS de tocar res. -->

De fp-cercador/backend/app.py (stub actual — 11 línies, REEMPLAÇAR completament):
```python
from flask import Flask
from flask_cors import CORS
from dotenv import load_dotenv

load_dotenv()

app = Flask(__name__)
CORS(app)

if __name__ == '__main__':
    app.run(debug=True)
```

De fp-cercador/backend/refresh_state.py (creat al pla 01):
```python
import threading

_lock = threading.Lock()
_state = {"status": "idle", "last_run": None, "total": None,
          "by_grado": None, "duration_seconds": None, "errors": []}

def get_state() -> dict: ...    # còpia superficial de _state
def set_state(**kwargs) -> None: ...  # actualitza _state
```

De fp-cercador/backend/scrapers/pipeline.py:
```python
def run() -> dict:
    # Retorna: {"total": int, "by_grado": dict, "duration_seconds": float, "errors": list}
```

Tests que han de passar (noms exactes a tests/test_api.py):
- test_health → GET /health → 200 {"status": "ok"}
- test_ofertes_200 → GET /api/ofertes → 200 llista JSON
- test_ofertes_503_when_no_file → GET /api/ofertes → 503 {"error": str}
- test_cors_headers → qualsevol ruta → Access-Control-Allow-Origin present
- test_refresh_401_wrong_token → POST /api/admin/refresh → 401
- test_refresh_401_no_header → POST /api/admin/refresh → 401
- test_refresh_409_while_running → POST /api/admin/refresh → 409
- test_refresh_started → POST /api/admin/refresh → 200 {"status": "started"}
- test_refresh_status_idle → GET /api/refresh-status → 200 amb tots els camps
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar app.py complet (GREEN)</name>
  <files>fp-cercador/backend/app.py</files>
  <read_first>
    - fp-cercador/backend/app.py (stub actual — llegir primer per preservar load_dotenv() i CORS(app))
    - fp-cercador/backend/refresh_state.py (estructura del mòdul creat al pla 01)
    - fp-cercador/backend/scrapers/pipeline.py (línies 1-70 — imports i signatura de run())
    - fp-cercador/backend/tests/test_api.py (tots els tests — entendre exactament quins comportaments cal implementar)
    - .planning/phases/04-flask-api/04-PATTERNS.md (secció "app.py — patrons de cada endpoint")
    - .planning/phases/04-flask-api/04-RESEARCH.md (secció "Common Pitfalls" — especialment Pitfall 1 ADMIN_TOKEN buit, Pitfall 2 Lock sense finally, Pitfall 3 DATA_PATH)
  </read_first>
  <behavior>
    - test_health: GET /health → 200 {"status": "ok"}
    - test_ofertes_200: GET /api/ofertes quan os.path.exists=True → 200 llista JSON
    - test_ofertes_503_when_no_file: GET /api/ofertes quan os.path.exists=False → 503 {"error": str}
    - test_cors_headers: qualsevol GET → header "Access-Control-Allow-Origin" present
    - test_refresh_401_wrong_token: POST /api/admin/refresh Bearer wrong → 401 {"error": str}
    - test_refresh_401_no_header: POST /api/admin/refresh sense header → 401 {"error": str}
    - test_refresh_409_while_running: POST quan _lock adquirit → 409 {"error": str}
    - test_refresh_started: POST amb token correcte → 200 {"status": "started"}
    - test_refresh_status_idle: GET /api/refresh-status → 200 {"status":"idle","last_run":null,"total":null,"by_grado":null,"duration_seconds":null,"errors":[]}
  </behavior>
  <action>
Reemplaçar el contingut de fp-cercador/backend/app.py completament. L'estructura exacta és:

```python
"""
app.py — Flask application: rutes REST per al Cercador FP España.

Rutes:
  GET  /health                → {"status": "ok"} (sense auth)
  GET  /api/ofertes           → array JSON dels registres (200) o 503 si no hi ha dades
  GET  /api/refresh-status    → estat del darrer refresh (idle/running/done/error)
  POST /api/admin/refresh     → llança el pipeline en background (requereix Bearer token)

Decisions de disseny:
  - Sense Blueprints: 4 rutes no justifiquen la complexitat addicional
  - refresh_state.py: mòdul separat per a l'estat compartit (evita circulars)
  - hmac.compare_digest: comparació constant-time per evitar timing attacks
  - Thread(daemon=True): el pipeline no bloqueja l'aturada del servidor
  - DATA_PATH relatiu a __file__: correcte independentment del cwd de l'execució
"""
import hmac
import json
import logging
import os
import threading
from datetime import datetime, timezone

from dotenv import load_dotenv
from flask import Flask, jsonify, request
from flask_cors import CORS

import refresh_state
from scrapers import pipeline

# ---------------------------------------------------------------------------
# Inicialització
# ---------------------------------------------------------------------------

load_dotenv()

ADMIN_TOKEN = os.environ.get("ADMIN_TOKEN", "")
if not ADMIN_TOKEN:
    raise RuntimeError("ADMIN_TOKEN not set. Create .env from .env.example.")

DATA_PATH = os.path.normpath(
    os.path.join(os.path.dirname(__file__), "data", "ofertes.json")
)

app = Flask(__name__)
CORS(app)

logger = logging.getLogger(__name__)

# ---------------------------------------------------------------------------
# Helper d'autenticació
# ---------------------------------------------------------------------------


def _check_auth(req) -> bool:
    """Verifica el token Bearer amb comparació constant-time (evita timing attacks)."""
    auth = req.headers.get("Authorization", "")
    if not auth.startswith("Bearer "):
        return False
    provided = auth[7:]
    return hmac.compare_digest(provided, ADMIN_TOKEN)


# ---------------------------------------------------------------------------
# Rutes
# ---------------------------------------------------------------------------


@app.route("/health")
def health():
    """API-07: Health check sense autenticació."""
    return jsonify({"status": "ok"}), 200


@app.route("/api/ofertes")
def get_ofertes():
    """API-01 / API-02: Retorna tots els registres de ofertes.json o 503 si no existeix."""
    if not os.path.exists(DATA_PATH):
        return jsonify({"error": "Data not available. Run /api/admin/refresh first."}), 503
    with open(DATA_PATH, "r", encoding="utf-8") as f:
        data = json.load(f)
    return jsonify(data), 200


@app.route("/api/refresh-status")
def refresh_status():
    """API-06: Retorna l'estat del darrer procés de refresh."""
    return jsonify(refresh_state.get_state()), 200


@app.route("/api/admin/refresh", methods=["POST"])
def admin_refresh():
    """API-03 / API-04 / API-05: Llança el pipeline en background si el token és vàlid."""
    if not _check_auth(request):
        return jsonify({"error": "Unauthorized"}), 401

    acquired = refresh_state._lock.acquire(blocking=False)
    if not acquired:
        return jsonify({"error": "Refresh already running"}), 409

    def _run():
        try:
            refresh_state.set_state(
                status="running",
                last_run=datetime.now(timezone.utc).isoformat(),
                errors=[],
            )
            result = pipeline.run()
            refresh_state.set_state(
                status="done",
                total=result["total"],
                by_grado=result["by_grado"],
                duration_seconds=result["duration_seconds"],
                errors=result["errors"],
            )
        except Exception as exc:
            logger.error("Pipeline refresh failed: %s", exc)
            refresh_state.set_state(status="error", errors=[str(exc)])
        finally:
            refresh_state._lock.release()

    t = threading.Thread(target=_run, daemon=True)
    t.start()
    return jsonify({"status": "started"}), 200


# ---------------------------------------------------------------------------
# Punt d'entrada
# ---------------------------------------------------------------------------

if __name__ == "__main__":
    app.run(debug=True)
```

NOTES CRÍTIQUES a verificar durant la implementació:
1. `ADMIN_TOKEN` guard ABANS de `app = Flask(__name__)` — el servidor no ha d'arrencar sense token
2. `DATA_PATH` amb `os.path.dirname(__file__)` (no ruta relativa al cwd)
3. `hmac.compare_digest` a `_check_auth` (no `==` ni `is`)
4. `finally: refresh_state._lock.release()` OBLIGATORI al thread `_run`
5. `daemon=True` al Thread
6. L'ordre dels imports preserva: `load_dotenv()` → guard → `DATA_PATH` → `app = Flask()`
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && ADMIN_TOKEN=test python3 -m pytest tests/test_api.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ADMIN_TOKEN=test python3 -m pytest tests/test_api.py -q` retorna "9 passed" (GREEN)
    - `ADMIN_TOKEN=test python3 -m pytest -q` retorna tots els tests en verd (suite completa, inclosos els 55 tests anteriors)
    - grep "hmac.compare_digest" fp-cercador/backend/app.py retorna resultat
    - grep "daemon=True" fp-cercador/backend/app.py retorna resultat
    - grep "finally:" fp-cercador/backend/app.py retorna resultat (lock release garantit)
    - grep "os.path.dirname(__file__)" fp-cercador/backend/app.py retorna resultat (DATA_PATH correcte)
    - grep "if not ADMIN_TOKEN:" fp-cercador/backend/app.py retorna resultat (guard de seguretat)
    - python3 -c "import app" sense ADMIN_TOKEN → llança RuntimeError (no ImportError ni silenci)
    - El fitxer NO conté cap Blueprint (grep "Blueprint" ha de retornar buit)
  </acceptance_criteria>
  <done>app.py implementat completament. 9/9 tests en verd. Suite completa (55 + 9 = 64 tests) en verd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client HTTP → Flask app | Totes les requests entren per aquí. Input no fiable: headers, body. |
| Flask app → pipeline thread | El thread executa codi que fa network I/O (todofp.es). Errors del pipeline captats amb try/except. |
| Flask app → refresh_state | Memòria compartida entre el thread principal i el thread de refresh. Accés concurrent possible. |
| Flask app → filesystem | Lectura de ofertes.json. Ruta resolta des de __file__, no des d'input de l'usuari. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Spoofing | POST /api/admin/refresh — token comparison | mitigate | `hmac.compare_digest(provided, ADMIN_TOKEN)` en lloc de `==`. Implementat a `_check_auth()`. ASVS V2.1.12. |
| T-04-02-02 | Elevation of Privilege | ADMIN_TOKEN buit | mitigate | Guard `if not ADMIN_TOKEN: raise RuntimeError(...)` a l'inici del mòdul. El servidor no arrenca sense token configurat. |
| T-04-02-03 | Denial of Service | POST /api/admin/refresh — refresh flood | mitigate | `_lock.acquire(blocking=False)` retorna 409 si el pipeline ja s'està executant. Màxim 1 thread concurrent. |
| T-04-02-04 | Denial of Service | Thread de refresh — lock no alliberat | mitigate | Bloc `finally: refresh_state._lock.release()` obligatori. Garantit fins i tot si `pipeline.run()` llança excepció. |
| T-04-02-05 | Information Disclosure | ADMIN_TOKEN al repositori | accept | `.env` exclòs de git (configurat a la Fase 1). `.env.example` conté placeholder `ADMIN_TOKEN=change-me`. Cap token real al repo. |
| T-04-02-06 | Information Disclosure | Error messages del pipeline | accept | `errors=[str(exc)]` exposa el missatge d'excepció a `/api/refresh-status`. Acceptable: endpoint amb autenticació i dades no sensibles. |
| T-04-02-07 | Tampering | DATA_PATH path traversal | accept | `DATA_PATH` és una constant resolta a l'inici del mòdul des de `__file__`, no des d'input de l'usuari. No hi ha risc de path traversal. |
</threat_model>

<verification>
```bash
# 1. Test suite RED → GREEN
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  ADMIN_TOKEN=test python3 -m pytest tests/test_api.py -v

# 2. Suite completa verda (64 tests)
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  ADMIN_TOKEN=test python3 -m pytest -q

# 3. Guard de seguretat — servidor no arrenca sense token
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  python3 -c "import app" 2>&1 | grep "RuntimeError"
# Esperat: RuntimeError: ADMIN_TOKEN not set...

# 4. hmac present
grep "hmac.compare_digest" "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/app.py"

# 5. daemon=True present
grep "daemon=True" "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/app.py"

# 6. finally present
grep "finally:" "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend/app.py"
```
</verification>

<success_criteria>
- `ADMIN_TOKEN=test python3 -m pytest tests/test_api.py -q` → "9 passed"
- `ADMIN_TOKEN=test python3 -m pytest -q` → tota la suite en verd
- `python3 -c "import app"` sense ADMIN_TOKEN → RuntimeError visible
- grep de hmac.compare_digest, daemon=True, finally: tots retornen resultat
- app.py NO conté Blueprints, NO conté token hardcoded
</success_criteria>

<output>
Després de completar, crear `.planning/phases/04-flask-api/04-02-SUMMARY.md`
</output>
