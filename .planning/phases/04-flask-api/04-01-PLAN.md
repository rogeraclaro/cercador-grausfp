---
phase: 04-flask-api
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - fp-cercador/backend/refresh_state.py
  - fp-cercador/backend/tests/test_api.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
  - API-07
  - API-08
  - API-09

must_haves:
  truths:
    - "El fitxer tests/test_api.py existeix amb exactament 9 funcions de test (test_health, test_ofertes_200, test_ofertes_503_when_no_file, test_cors_headers, test_refresh_started, test_refresh_401_wrong_token, test_refresh_401_no_header, test_refresh_409_while_running, test_refresh_status_idle)"
    - "El fitxer refresh_state.py existeix amb _lock, _state, get_state() i set_state()"
    - "Tots els tests fallen (RED) perquè app.py no té cap ruta implementada"
    - "La fixture autouse reset_refresh_state restableix _state i allibera _lock entre cada test"
  artifacts:
    - path: "fp-cercador/backend/refresh_state.py"
      provides: "Estat compartit thread-safe: _lock (threading.Lock), _state dict, get_state(), set_state()"
      exports: ["_lock", "_state", "get_state", "set_state"]
    - path: "fp-cercador/backend/tests/test_api.py"
      provides: "Suite de tests RED per a totes les rutes Flask (API-01 a API-09)"
      contains: "def test_health"
  key_links:
    - from: "tests/test_api.py"
      to: "refresh_state._lock"
      via: "import refresh_state; refresh_state._lock.acquire()"
      pattern: "import refresh_state"
    - from: "tests/test_api.py"
      to: "app.pipeline.run"
      via: "mock.patch(PATCH_PIPELINE_RUN, ...)"
      pattern: "PATCH_PIPELINE_RUN"
---

<objective>
Crear refresh_state.py (mòdul d'estat compartit) i test_api.py (suite RED) que defineix el comportament esperat de totes les rutes Flask de la Fase 4.

Purpose: El TDD RED estableix el contracte de l'API abans de la implementació. Quan el pla 02 fa GREEN, els tests ja cobreixen tots els requisits API-01 a API-09 sense afegir casos nous.

Output:
- refresh_state.py — mòdul nou amb l'estat compartit thread-safe
- tests/test_api.py — 9 tests, tots FAILING (app.py no té rutes)
</objective>

<execution_context>
@/Users/rogermasellas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rogermasellas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/PROJECT.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/ROADMAP.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/STATE.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-RESEARCH.md
@/Users/rogermasellas/AI/Cercador\ Graus/.planning/phases/04-flask-api/04-PATTERNS.md

<interfaces>
<!-- Fitxers que els tests han d'importar. Llegir ABANS de tocar res. -->

De fp-cercador/backend/app.py (stub actual — 11 línies):
```python
from flask import Flask
from flask_cors import CORS
from dotenv import load_dotenv

load_dotenv()

app = Flask(__name__)
CORS(app)

if __name__ == '__main__':
    app.run(debug=True)
```
Cap ruta definida. Tests fallaran amb 404 fins al pla 02.

De fp-cercador/backend/scrapers/pipeline.py (signatura de run()):
```python
def run() -> dict:
    # Retorna: {"total": int, "by_grado": dict, "duration_seconds": float, "errors": list}
```

De fp-cercador/backend/tests/test_pipeline.py (convencions de la suite existent):
```python
# Patch paths com a constants de mòdul
PATCH_REQUESTS_GET = "scrapers.pipeline.requests.get"
# Fixtures amb pytest.fixture
# mock.patch com a context manager
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear refresh_state.py (mòdul d'estat compartit)</name>
  <files>fp-cercador/backend/refresh_state.py</files>
  <read_first>
    - fp-cercador/backend/scrapers/pipeline.py (línies 1-35 — convencions d'imports i constants de mòdul)
    - fp-cercador/backend/app.py (stub actual — verificar que no hi ha cap import circular)
    - .planning/phases/04-flask-api/04-PATTERNS.md (secció "refresh_state.py — mòdul complet a crear")
  </read_first>
  <action>
Crear fp-cercador/backend/refresh_state.py amb el contingut exacte següent. Cap import de Flask ni de app.py (dependència unidireccional: app.py → refresh_state.py).

```python
"""
refresh_state.py — Estat compartit thread-safe per al pipeline de refresh.

Dependència unidireccional: app.py → refresh_state.py
Cap import de Flask ni de app.py per evitar importacions circulars.
"""
import threading

_lock = threading.Lock()

_state = {
    "status": "idle",         # idle | running | done | error
    "last_run": None,         # ISO 8601 string o null
    "total": None,
    "by_grado": None,
    "duration_seconds": None,
    "errors": [],
}


def get_state() -> dict:
    """Retorna una còpia superficial de l'estat actual (thread-safe per a lectures simples de dict)."""
    return dict(_state)


def set_state(**kwargs) -> None:
    """Actualitza camps de l'estat. Cridar únicament des del thread de refresh."""
    _state.update(kwargs)
```
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python3 -c "import refresh_state; assert hasattr(refresh_state, '_lock'); assert hasattr(refresh_state, '_state'); assert hasattr(refresh_state, 'get_state'); assert hasattr(refresh_state, 'set_state'); print('refresh_state OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import refresh_state"` no llança cap error
    - `refresh_state._state["status"]` == "idle" en importar per primera vegada
    - `refresh_state._lock.acquire(blocking=False)` retorna True (lock lliure inicialment)
    - `refresh_state._lock.release()` allibera correctament
    - El fitxer NO conté cap import de `flask`, `app`, ni cap mòdul de `scrapers`
    - grep "^import threading" fp-cercador/backend/refresh_state.py retorna resultat
  </acceptance_criteria>
  <done>refresh_state.py existeix, és importable, i exposa _lock, _state, get_state, set_state sense dependències de Flask.</done>
</task>

<task type="auto">
  <name>Task 2: Crear tests/test_api.py (suite RED — 9 tests)</name>
  <files>fp-cercador/backend/tests/test_api.py</files>
  <read_first>
    - fp-cercador/backend/tests/test_pipeline.py (convencions completes: imports, fixtures, patch paths, assertions)
    - fp-cercador/backend/refresh_state.py (creat a Task 1 — per entendre _state i _lock)
    - fp-cercador/backend/app.py (stub actual — confirmar que no hi ha rutes i que tots els tests fallaran amb 404/ImportError fins al pla 02)
    - .planning/phases/04-flask-api/04-PATTERNS.md (secció "test_api.py — patró de fixture i tests")
    - .planning/phases/04-flask-api/04-RESEARCH.md (secció "Code Examples — Test d'integració Flask")
  </read_first>
  <action>
Crear fp-cercador/backend/tests/test_api.py amb exactament 9 funcions de test que cobreixen API-01 a API-09. La suite ha d'estar en estat RED (tots els tests fallen) perquè app.py no té cap ruta.

Contingut exacte del fitxer:

```python
"""
test_api.py — Tests d'integració de les rutes Flask (API-01 a API-09).

Tests sense xarxa real: pipeline.run i os.path.exists estan mockejats.
Fase RED: tots fallen fins que app.py implementa les rutes (Pla 02).
"""
import os
import time
import unittest.mock as mock

import pytest

# ---------------------------------------------------------------------------
# Patch paths (convencions: constants al top del mòdul, com a test_pipeline.py)
# ---------------------------------------------------------------------------
PATCH_PIPELINE_RUN = "app.pipeline.run"
PATCH_OS_PATH_EXISTS = "app.os.path.exists"

MOCK_PIPELINE_RESULT = {
    "total": 100,
    "by_grado": {"A": 20, "B": 20, "C": 20, "D": 20, "E": 20},
    "duration_seconds": 1.5,
    "errors": [],
}

# ---------------------------------------------------------------------------
# Fixtures
# ---------------------------------------------------------------------------

@pytest.fixture(autouse=True)
def reset_refresh_state():
    """Restableix _state i allibera _lock entre tests (evita contaminació)."""
    import refresh_state
    refresh_state._state.update({
        "status": "idle",
        "last_run": None,
        "total": None,
        "by_grado": None,
        "duration_seconds": None,
        "errors": [],
    })
    if refresh_state._lock.locked():
        refresh_state._lock.release()
    yield
    # Cleanup post-test: alliberar lock si algun test el va adquirir sense alliberar
    if refresh_state._lock.locked():
        refresh_state._lock.release()


@pytest.fixture
def client():
    os.environ["ADMIN_TOKEN"] = "test-token"
    from app import app as flask_app
    flask_app.config["TESTING"] = True
    with flask_app.test_client() as c:
        yield c


# ---------------------------------------------------------------------------
# API-07: GET /health
# ---------------------------------------------------------------------------

def test_health(client):
    """API-07: GET /health retorna {"status": "ok"} sense autenticació."""
    r = client.get("/health")
    assert r.status_code == 200
    assert r.get_json() == {"status": "ok"}


# ---------------------------------------------------------------------------
# API-01: GET /api/ofertes — 200 quan el fitxer existeix
# ---------------------------------------------------------------------------

def test_ofertes_200(client):
    """API-01: GET /api/ofertes retorna 200 i una llista JSON quan ofertes.json existeix."""
    fake_data = [{"id": 1, "denominacion": "Test", "grado": "A"}]
    with mock.patch(PATCH_OS_PATH_EXISTS, return_value=True), \
         mock.patch("app.json.load", return_value=fake_data), \
         mock.patch("builtins.open", mock.mock_open(read_data="[]")):
        r = client.get("/api/ofertes")
    assert r.status_code == 200
    data = r.get_json()
    assert isinstance(data, list)
    assert len(data) == 1
    assert data[0]["id"] == 1


# ---------------------------------------------------------------------------
# API-02: GET /api/ofertes — 503 quan el fitxer no existeix
# ---------------------------------------------------------------------------

def test_ofertes_503_when_no_file(client):
    """API-02: GET /api/ofertes retorna 503 amb {"error": ...} si ofertes.json no existeix."""
    with mock.patch(PATCH_OS_PATH_EXISTS, return_value=False):
        r = client.get("/api/ofertes")
    assert r.status_code == 503
    body = r.get_json()
    assert "error" in body
    assert isinstance(body["error"], str)
    assert len(body["error"]) > 0


# ---------------------------------------------------------------------------
# API-08: CORS headers presents a les respostes
# ---------------------------------------------------------------------------

def test_cors_headers(client):
    """API-08: Les respostes inclouen el header Access-Control-Allow-Origin."""
    r = client.get("/health", headers={"Origin": "http://localhost:3000"})
    assert r.status_code == 200
    assert "Access-Control-Allow-Origin" in r.headers


# ---------------------------------------------------------------------------
# API-04: POST /api/admin/refresh — 401 amb token incorrecte
# ---------------------------------------------------------------------------

def test_refresh_401_wrong_token(client):
    """API-04: POST /api/admin/refresh retorna 401 si el token Bearer és incorrecte."""
    r = client.post("/api/admin/refresh",
                    headers={"Authorization": "Bearer wrong-token"})
    assert r.status_code == 401
    assert "error" in r.get_json()


def test_refresh_401_no_header(client):
    """API-04: POST /api/admin/refresh retorna 401 si no hi ha header Authorization."""
    r = client.post("/api/admin/refresh")
    assert r.status_code == 401
    assert "error" in r.get_json()


# ---------------------------------------------------------------------------
# API-05: POST /api/admin/refresh — 409 si procés en curs
# ---------------------------------------------------------------------------

def test_refresh_409_while_running(client):
    """API-05: POST /api/admin/refresh retorna 409 si el lock ja està adquirit (procés en curs)."""
    import refresh_state
    refresh_state._lock.acquire()
    try:
        r = client.post("/api/admin/refresh",
                        headers={"Authorization": "Bearer test-token"})
        assert r.status_code == 409
        assert "error" in r.get_json()
    finally:
        refresh_state._lock.release()


# ---------------------------------------------------------------------------
# API-03: POST /api/admin/refresh — retorna {"status": "started"} immediatament
# ---------------------------------------------------------------------------

def test_refresh_started(client):
    """API-03: POST /api/admin/refresh llança el pipeline en background i retorna {"status": "started"}."""
    with mock.patch(PATCH_PIPELINE_RUN, return_value=MOCK_PIPELINE_RESULT):
        r = client.post("/api/admin/refresh",
                        headers={"Authorization": "Bearer test-token"})
    assert r.status_code == 200
    assert r.get_json() == {"status": "started"}


# ---------------------------------------------------------------------------
# API-06: GET /api/refresh-status — retorna estat complet
# ---------------------------------------------------------------------------

def test_refresh_status_idle(client):
    """API-06: GET /api/refresh-status retorna l'estat complet (status, last_run, total, by_grado, duration_seconds, errors)."""
    r = client.get("/api/refresh-status")
    assert r.status_code == 200
    data = r.get_json()
    assert data["status"] == "idle"
    assert "last_run" in data
    assert "total" in data
    assert "by_grado" in data
    assert "duration_seconds" in data
    assert "errors" in data


# ---------------------------------------------------------------------------
# API-09: ADMIN_TOKEN llegit de .env (no hardcoded)
# ---------------------------------------------------------------------------

def test_admin_token_from_env(client):
    """API-09: El token s'obté de ADMIN_TOKEN de l'entorn; un token diferent retorna 401."""
    # El fixture client ja posa ADMIN_TOKEN="test-token"
    # Verificar que un token diferent no passa l'autenticació
    r = client.post("/api/admin/refresh",
                    headers={"Authorization": "Bearer diferent-token"})
    assert r.status_code == 401
    # Verificar que el token correcte sí funciona
    with mock.patch(PATCH_PIPELINE_RUN, return_value=MOCK_PIPELINE_RESULT):
        r2 = client.post("/api/admin/refresh",
                         headers={"Authorization": "Bearer test-token"})
    assert r2.status_code == 200
```
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && python3 -m pytest tests/test_api.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - El fitxer tests/test_api.py existeix i és parsejable per pytest (no SyntaxError)
    - `python3 -m pytest tests/test_api.py --collect-only -q` mostra exactament 9 tests sense errors de col·lecció
    - `python3 -m pytest tests/test_api.py -q` falla (estat RED esperats): tots o la majoria fallen amb 404 o AttributeError perquè app.py no té rutes
    - grep "def test_" tests/test_api.py | wc -l retorna 9
    - grep "autouse=True" tests/test_api.py retorna resultat (fixture de reset)
    - La fixture reset_refresh_state apareix dues vegades al fitxer (definició + yield cleanup)
    - El fitxer NO conté `time.sleep` (no sleep en tests)
    - Els constants PATCH_PIPELINE_RUN i PATCH_OS_PATH_EXISTS estan definits al top del fitxer
  </acceptance_criteria>
  <done>test_api.py existeix amb 9 tests que es col·lectegen correctament i fallen (RED) perquè les rutes no estan implementades. refresh_state.py importat correctament als tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → app | El test_client de Flask simula requests HTTP sense xarxa real |
| test → refresh_state | Els tests accedeixen directament a _state i _lock per simular condicions (patró acceptable en tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-P01-01 | Spoofing | tests/test_api.py fixture `client` | accept | Els tests usen ADMIN_TOKEN="test-token" en entorn de test; no és un secret real. La variable d'entorn s'estableix explícitament al fixture. |
| T-04-P01-02 | Information Disclosure | refresh_state._state | accept | L'accés directe a _state en tests és un antipatró de producció però acceptable en tests d'integració. En producció, refresh_state no és accessible des del client. |
| T-04-P01-03| Elevation of Privilege | reset_refresh_state autouse | mitigate | La fixture `autouse=True` allibera el lock al teardown per evitar que un test que falli deixi el lock adquirit i contamini tests posteriors. El bloc `finally` del cleanup garanteix el release. |
</threat_model>

<verification>
```bash
# 1. Verificar refresh_state.py importable i complet
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  python3 -c "import refresh_state; s=refresh_state.get_state(); assert s['status']=='idle'; print('refresh_state OK')"

# 2. Verificar 9 tests col·lectats
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  python3 -m pytest tests/test_api.py --collect-only -q 2>&1 | grep "test session starts" -A 20

# 3. Confirmar estat RED (tests fallen perquè no hi ha rutes)
cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/backend" && \
  python3 -m pytest tests/test_api.py -q 2>&1 | tail -3
# Esperat: "FAILED" o "ERROR" per a tots els tests — 404 o AttributeError
```
</verification>

<success_criteria>
- refresh_state.py existeix i és importable sense errors
- tests/test_api.py existeix amb exactament 9 funcions de test
- `--collect-only` no mostra cap error de col·lecció
- Tots els tests fallen (estat RED) perquè app.py no té rutes implementades
- La suite existent (55 tests) segueix en verd: `python3 -m pytest tests/ --ignore=tests/test_api.py -q` passa
</success_criteria>

<output>
Després de completar, crear `.planning/phases/04-flask-api/04-01-SUMMARY.md`
</output>
