---
phase: 02-pdf-scrapers-grados-a-b-c
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - fp-cercador/backend/scrapers/pdf_scraper.py
  - fp-cercador/backend/tests/__init__.py
  - fp-cercador/backend/tests/conftest.py
  - fp-cercador/backend/tests/test_pdf_scraper.py
autonomous: true
requirements:
  - PDF-02
  - PDF-03
  - PDF-04
  - PDF-05
  - PDF-06

must_haves:
  truths:
    - "parse_grado_a(), parse_grado_b(), parse_grado_c() existeixen a pdf_scraper.py i retornen llistes de dicts"
    - "Cada registre retornat té exactament els camps: codigo, denominacion, familia, nivel, plan_antiguo, observaciones"
    - "El prefix del codi es mapeja correctament als 24 noms de família via PREFIX_MAP"
    - "plan_antiguo=True quan la cel·la del codi conté '(Plan antiguo)'; clean_code no conté aquest marcador"
    - "Grado C nou pla: _3B→nivel=1, _4B→nivel=2, _5B→nivel=3; Grado A i B nou pla: nivel=None"
    - "Pla antic Grado B (MF2268_2): nivel=2 extret del sufix _N"
    - "Família desconeguda produeix logging.warning i inclou el registre amb familia='Desconeguda'"
    - "Les pàgines 1–5 (índex 0–4) no produeixen cap registre; el parsing comença a l'índex 5"
    - "Codis duplicats entre pàgines: només el primer occurrence es conserva"
    - "La suite de tests unitaris passa completament sense xarxa (usant fixtures)"
  artifacts:
    - path: "fp-cercador/backend/scrapers/pdf_scraper.py"
      provides: "PDF parsing functions per Grado A, B, C"
      exports: ["parse_grado_a", "parse_grado_b", "parse_grado_c", "PREFIX_MAP"]
    - path: "fp-cercador/backend/tests/test_pdf_scraper.py"
      provides: "Suite de tests unitaris per PDF-02 a PDF-06"
      contains: "def test_"
    - path: "fp-cercador/backend/tests/conftest.py"
      provides: "Fixtures compartides: pàgines de taula simulades, codis de mostra"
  key_links:
    - from: "fp-cercador/backend/scrapers/pdf_scraper.py"
      to: "fp-cercador/backend/scrapers/pipeline.py"
      via: "from scrapers.pdf_scraper import parse_grado_a, parse_grado_b, parse_grado_c"
      pattern: "def parse_grado_[abc]"
---

<objective>
Implementar `pdf_scraper.py` seguint el cicle TDD (RED→GREEN→REFACTOR). El mòdul exposa tres funcions públiques (`parse_grado_a`, `parse_grado_b`, `parse_grado_c`) i la lògica compartida interna per detectar família, nivel i plan_antiguo. Els tests s'escriuen amb fixtures locals (sense xarxa).

Purpose: Establir el nucli del parsing de PDFs amb cobertura de tests abans de la integració al pipeline. Els tests de fixtures seran la salvaguarda quan els URLs dels PDFs canviïn en futures versions.

Output: `pdf_scraper.py` (implementació completa), `tests/test_pdf_scraper.py` (suite d'unitat), `tests/conftest.py` (fixtures).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pdf-scrapers-grados-a-b-c/02-CONTEXT.md
@.planning/phases/02-pdf-scrapers-grados-a-b-c/02-RESEARCH.md
@.planning/phases/02-pdf-scrapers-grados-a-b-c/02-PATTERNS.md

<interfaces>
<!-- Paquet existent — no modificar -->
<!-- fp-cercador/backend/scrapers/__init__.py: -->
# Paquet scrapers — contingut a la Fase 2

<!-- Esquema dels registres del JSON de mostra (fp-cercador/backend/data/ofertes.json): -->
{
  "id": int,          -- afegit pel pipeline (NO per pdf_scraper)
  "grado": str,       -- afegit pel pipeline (NO per pdf_scraper)
  "nivel": int|null,
  "familia": str,
  "codigo": str,
  "denominacion": str,
  "plan_antiguo": bool,
  "observaciones": str
}
-- NOTA: pdf_scraper retorna records SENSE "id" i SENSE "grado"; pipeline els afegeix.
</interfaces>
</context>

<feature>
  <name>pdf_scraper.py — parsing de PDFs dels Grados A, B, C</name>
  <files>
    fp-cercador/backend/scrapers/pdf_scraper.py,
    fp-cercador/backend/tests/__init__.py,
    fp-cercador/backend/tests/conftest.py,
    fp-cercador/backend/tests/test_pdf_scraper.py
  </files>
  <behavior>
    Tests unitaris (sense xarxa, sense PDFs reals — tots usant fixtures de taules simulades):

    test_prefix_map_completeness:
      - PREFIX_MAP té exactament 24 entrades
      - PREFIX_MAP['AFD'] == 'Actividades Físicas y Deportivas'
      - PREFIX_MAP['IEX'] == 'Industrias Extractivas'  (NO 'Imagen y Espectáculos')
      - PREFIX_MAP['IMS'] == 'Imagen y Espectáculos'
      - PREFIX_MAP['VIC'] == 'Vidrio y Cerámica'

    test_plan_antiguo_detection:
      - _parse_row amb cel·la 'UF0297 (Plan antiguo)' → is_old=True, clean_code='UF0297'
      - _parse_row amb cel·la 'AFD_A_3003_01' → is_old=False
      - _parse_row amb cel·la 'MF2268_2 (Plan antiguo)' → is_old=True, clean_code='MF2268_2'

    test_nivel_grado_a:
      - _nivel_grado_a('AFD_A_3003_01', False) → None
      - _nivel_grado_a('UF0297', True) → None

    test_nivel_grado_b:
      - _nivel_grado_b('AFD_B_3003', False) → None
      - _nivel_grado_b('MF2268_2', True) → 2
      - _nivel_grado_b('MF2268_1', True) → 1
      - _nivel_grado_b('MF2268_3', True) → 3
      - _nivel_grado_b('MF2268', True) → None  (sense sufix)

    test_nivel_grado_c:
      - _nivel_grado_c('AFD_C_001_3B', False) → 1
      - _nivel_grado_c('AFD_C_001_4B', False) → 2
      - _nivel_grado_c('AFD_C_001_5B', False) → 3
      - _nivel_grado_c('AFDA0511', True) → None

    test_familia_lookup:
      - Prefix 'AFD' → familia='Actividades Físicas y Deportivas', cap warning
      - Prefix 'XXX' (desconegut) → familia='Desconeguda', logging.warning emès (capturar amb caplog)

    test_record_schema:
      - Donada una taula simulada amb una fila de dades vàlida de Grado C:
        [['AFD_C_001_3B', 'Acondicionamiento físico en sala de entrenamiento polivalente', '', '']]
      - _extract_records retorna un registre amb exactament els camps:
        {codigo, denominacion, familia, nivel, plan_antiguo, observaciones}
      - cap camp 'id', cap camp 'grado' (els afegeix pipeline)
      - codigo='AFD_C_001_3B', nivel=1, plan_antiguo=False, familia='Actividades Físicas y Deportivas'

    test_page_skip:
      - Donada una taula de mostra a la pàgina índex 4 (pàgina 5 del PDF), el registre NO s'extreu
      - Donada la mateixa taula a la pàgina índex 5 (pàgina 6 del PDF), el registre SÍ s'extreu

    test_duplicate_deduplication:
      - Donades dues pàgines amb el mateix codi 'AFD_C_001_3B', _extract_records retorna exactament 1 registre

    test_continuation_rows_ignored:
      - Fila de continuació (totes les cel·les del codi buides) no produeix registre

    test_public_api_exports:
      - parse_grado_a, parse_grado_b, parse_grado_c importables des de scrapers.pdf_scraper
      - Cadascuna accepta pdf_path: str i retorna list[dict]
  </behavior>
  <implementation>
    ## Wave 0: Infraestructura de tests

    1. Crear `fp-cercador/backend/tests/__init__.py` (fitxer buit).
    2. Crear `fp-cercador/backend/tests/conftest.py` amb les fixtures:
       - `sample_table_grado_c`: llista de files que simula una pàgina de taula de Grado C
       - `sample_table_grado_b_old`: llista de files que simula Grado B pla antic
       - `sample_table_grado_a_new`: llista de files que simula Grado A nou pla
       - `caplog` disponible automàticament via pytest

    3. Instal·lar pytest si no hi és: `cd fp-cercador/backend && pip install pytest`
    4. Verificar: `cd fp-cercador/backend && python -m pytest --collect-only` mostra tests detectats.

    ## RED: Escriure tots els tests fallits

    Crear `fp-cercador/backend/tests/test_pdf_scraper.py` amb tots els casos del bloc `<behavior>`.
    En aquest punt, importar des de `scrapers.pdf_scraper` ha de fallar perquè el fitxer no existeix.
    Executar: `cd fp-cercador/backend && python -m pytest tests/test_pdf_scraper.py -x -q`
    RESULTAT ESPERAT: tots els tests fallen (ImportError o AssertionError).

    ## GREEN: Implementar pdf_scraper.py

    Crear `fp-cercador/backend/scrapers/pdf_scraper.py` amb:

    ### Constants
    ```python
    PREFIX_MAP = {
        'AFD': 'Actividades Físicas y Deportivas',
        'ADG': 'Administración y Gestión',
        'AGA': 'Agraria',
        'ARG': 'Artes Gráficas',
        'COM': 'Comercio y Marketing',
        'ELE': 'Electricidad y Electrónica',
        'ENA': 'Energía y Agua',
        'EOC': 'Edificación y Obra Civil',
        'FME': 'Fabricación Mecánica',
        'HOT': 'Hostelería y Turismo',
        'IEX': 'Industrias Extractivas',
        'IFC': 'Informática y Comunicaciones',
        'IMA': 'Instalación y Mantenimiento',
        'IMP': 'Imagen Personal',
        'IMS': 'Imagen y Espectáculos',
        'INA': 'Industrias Alimentarias',
        'MAM': 'Madera, Mueble y Corcho',
        'MAP': 'Marítimo-Pesquera',
        'QUI': 'Química',
        'SEA': 'Seguridad y Medio Ambiente',
        'SSC': 'Servicios Socioculturales y a la Comunidad',
        'TCP': 'Textil, Confección y Piel',
        'TMV': 'Transporte y Mantenimiento de Vehículos',
        'VIC': 'Vidrio y Cerámica',
    }
    ```

    ### Funcions privades
    ```python
    def _nivel_grado_a(code: str, is_old_plan: bool) -> None:
        return None

    def _nivel_grado_b(code: str, is_old_plan: bool) -> int | None:
        if is_old_plan:
            m = re.search(r'_([123])$', code)
            return int(m.group(1)) if m else None
        return None

    def _nivel_grado_c(code: str, is_old_plan: bool) -> int | None:
        if not is_old_plan:
            if code.endswith('_3B'): return 1
            if code.endswith('_4B'): return 2
            if code.endswith('_5B'): return 3
        return None

    def _parse_row(row: list, new_code_re: re.Pattern) -> tuple:
        """Retorna (code_cell, denom_cell, obs_parts) o (None, None, [])."""
        # Iterar totes les cel·les — el nombre de columnes varia (Pitfall 1)
        for i, cell in enumerate(row):
            if not cell or not str(cell).strip():
                continue
            cell_str = str(cell).strip()
            is_new = bool(new_code_re.match(cell_str))
            is_old = '(Plan antiguo)' in cell_str
            if is_new or is_old:
                denom = None
                for j in range(i + 1, len(row)):
                    if row[j] and str(row[j]).strip():
                        denom = str(row[j]).strip()
                        break
                obs = [str(row[k]).strip() for k in range(i + 2, len(row))
                       if row[k] and str(row[k]).strip()]
                return cell_str, denom, obs
        return None, None, []

    def _extract_records(pdf_path: str, grado_letter: str, nivel_fn) -> list[dict]:
        """Extreu registres d'un PDF de Grado X. Pàgines 0–4 ignorades (portada/intro)."""
        records = {}  # code_cell -> dict (garanteix unicitat — Pitfall 4)
        new_code_re = re.compile(rf'^[A-Z]{{2,4}}_{grado_letter}_\d')

        with pdfplumber.open(pdf_path) as pdf:
            for page in pdf.pages[5:]:  # skip pàgines 1–5 (índex 0–4)
                table = page.extract_table()
                if not table:
                    continue
                for row in table:
                    if not row:
                        continue
                    code_cell, denom_cell, obs_parts = _parse_row(row, new_code_re)
                    if code_cell and code_cell not in records:
                        is_old = '(Plan antiguo)' in code_cell
                        clean_code = code_cell.replace(' (Plan antiguo)', '').strip()
                        prefix = clean_code.split('_')[0] if '_' in clean_code else ''
                        familia = PREFIX_MAP.get(prefix)
                        if not familia:
                            logging.warning(
                                f"Família desconeguda per prefix '{prefix}' al codi '{clean_code}'"
                            )
                            familia = 'Desconeguda'
                        records[code_cell] = {
                            'codigo': clean_code,
                            'denominacion': denom_cell or '',
                            'observaciones': ' '.join(obs_parts),
                            'familia': familia,
                            'nivel': nivel_fn(clean_code, is_old),
                            'plan_antiguo': is_old,
                        }
        return list(records.values())
    ```

    ### Funcions públiques
    ```python
    def parse_grado_a(pdf_path: str) -> list[dict]:
        return _extract_records(pdf_path, 'A', _nivel_grado_a)

    def parse_grado_b(pdf_path: str) -> list[dict]:
        return _extract_records(pdf_path, 'B', _nivel_grado_b)

    def parse_grado_c(pdf_path: str) -> list[dict]:
        return _extract_records(pdf_path, 'C', _nivel_grado_c)
    ```

    ### Imports necessaris al capdamunt del fitxer
    ```python
    import re
    import logging
    import pdfplumber
    ```

    ## REFACTOR
    Revisar per eliminar qualsevol duplicació. Els tests han de continuar passant.

    ## Notes crítiques per evitar pitfalls (de RESEARCH.md):
    - Mai usar `row[0]` com a índex fix — el nombre de columnes varia entre pàgines
    - Usar diccionari `records = {}` amb `code_cell` com a clau per evitar duplicats entre pàgines
    - `logging.warning(...)` mai `print()` per a famílies desconegudes (D-05)
    - El camp 'grado' i 'id' els afegeix pipeline.py, NO pdf_scraper.py
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF bytes → pdfplumber | Contingut de fitxer extern (PDF descarregat); pot ser malformat o maliciós |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `_extract_records` — pdfplumber.open() | mitigate | Encapsular `page.extract_table()` en try/except per pàgina; si una pàgina falla, continuar i registrar warning. El fail fast del pipeline (D-01) ja protegeix contra corrupcions greus que elevin excepció al parser. |
| T-02-02 | Denial of Service | `_extract_records` — memòria per PDFs grans (~4.2 MB, ~12K registres) | accept | El volum màxim és ~12K registres (~4.2 MB PDF). Python pot gestionar-ho en memòria sense limitació addicional. Si el servidor serveix un PDF malformat molt gran, `raise_for_status()` al pipeline ho capturarà si no és PDF vàlid. |
| T-02-03 | Spoofing | `pdf_scraper.py` — noms de família inferits de PREFIX_MAP | accept | El mapping és estàtic i verificat contra els PDFs reals. Un PDF amb un prefix nou retornarà `familia='Desconeguda'` amb warning (D-04/D-05) — cap impacte de seguretat, només de dades. |
</threat_model>

<verification>
## Tests automàtics (sense xarxa)

```bash
cd /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/backend && python -m pytest tests/test_pdf_scraper.py -v
```

Resultat esperat: tots els tests de `test_pdf_scraper.py` passen (verd).

## Verificació d'importació

```bash
cd /Users/rogermasellas/AI/Cercador\ Graus/fp-cercador/backend && python -c "from scrapers.pdf_scraper import parse_grado_a, parse_grado_b, parse_grado_c, PREFIX_MAP; print('OK', len(PREFIX_MAP), 'prefixos')"
```

Resultat esperat: `OK 24 prefixos`
</verification>

<success_criteria>
- `fp-cercador/backend/scrapers/pdf_scraper.py` existeix i és importable
- `from scrapers.pdf_scraper import parse_grado_a, parse_grado_b, parse_grado_c` funciona
- `len(PREFIX_MAP) == 24`
- PREFIX_MAP['IEX'] == 'Industrias Extractivas' (no 'Imagen y Espectáculos')
- Suite de tests `test_pdf_scraper.py` passa completament sense xarxa
- Cap test usa `requests` ni accés a fitxers PDF reals
- Cicle TDD complet: commits RED → GREEN → REFACTOR (si s'aplica)
</success_criteria>

<output>
After completion, create `.planning/phases/02-pdf-scrapers-grados-a-b-c/02-01-SUMMARY.md`
</output>
