---
phase: quick-260924-1rf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/frontend/index.html
  - .planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/type_check.py
autonomous: true
requirements: [quick-260924-1rf]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - ":root defines exactly these nine role tokens after the colour tokens: --fs-display 48px, --fs-display-sm 30px, --fs-title 20px, --fs-lead 15px, --fs-body 14px, --fs-small 13px, --fs-label 11px, --fs-mono 12px, --fs-input 16px. Every font-size declaration in the <style> block uses var(--fs-*). The one exception is the .centres-modal-close glyph at 18px. No em or rem font sizes remain (approved type system)"
    - "The nested em chain is gone at 1280 and 390px. The results table body is 13px. Programme names (td.col-nom) are 14px and keep weight 500. .textpetit ('acreditació parcial de competència de:') is 11px, uses var(--warm), and has opacity 1. .detall-ciclos-d and the .ciclos-d-list inside it (cicles D list, FPO module list) are 13px"
    - "At 1280 and 390px, no element with visible text computes below 11px. This covers name mode with a detail row and a centres panel open, and FPO mode"
    - "Under (pointer: coarse), every select and text/search input computes at least 16px, so iOS Safari does not zoom on focus. That covers #search, the occupation search, #filter-familia, #filter-nivel, the 8 FPO selects, the FPO text search (which has an inline font-size), the centres CCAA select and .centres-search-input. On a fine pointer the same controls keep 13px and #search stays 16px"
    - "Level cells (main and FPO), FPO hours and active-course cells, .results-count, .pagination-info and the pagination buttons render with font-variant-numeric: tabular-nums"
    - "The Google Fonts request asks for DM Sans 400/500/600/700 plus real italic 400 (no 300), Geist Mono 400 only, and DM Serif Display ital@0;1 unchanged, all with display=swap. DM Sans italic text (.results-count, empty row) uses the loaded italic face instead of a synthetic oblique"
    - "At 320 and 390px, fine and coarse pointer, CA and ES, logged out and logged in, name and FPO mode: no horizontal overflow beyond the known caveat from 260924-0s3 (FPO filter selects, ids starting with fpo-). The 0s3 regression groups cards, fpo, topbar, toggle and touch still pass"
    - "Markup and JS outside <style> are byte-identical to 1bc9f47, except that each inline 13px font-size now reads var(--fs-small). No colour, copy or JS behaviour changes. Exactly one commit touches only fp-cercador/frontend/index.html, with the exact Catalan subject and no attribution trailer"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: "Role type tokens in :root; token-based font sizes across the whole <style> block; tabular-nums rules; iOS focus-zoom rule inside the existing @media (pointer: coarse) block; trimmed Google Fonts URL; inline 13px sizes switched to var(--fs-small)"
      contains: "--fs-display: 48px"
    - path: ".planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/type_check.py"
      provides: "Headless-Chrome harness (Playwright, channel=chrome, /api/* mocked) derived from 260924-0s3 mobile_check.py. The desktop pixel-diff is replaced by compound, static, roles, minsize, fonts, ioszoom, overflow and shots groups. The cards, fpo, topbar, toggle and touch regression groups are kept"
      contains: "TYPE_CHECK_OK"
  key_links:
    - from: ":root --fs-* tokens"
      to: "every font-size declaration in the style block and the inline style attributes"
      via: "var(--fs-*) references; the html root font-size is the browser default 16px, so 0.75rem == 12px == --fs-mono"
      pattern: "font-size: var\\(--fs-"
    - from: "iOS rule placed as the LAST rule inside @media (pointer: coarse) (end of the sheet)"
      to: "the FPO text search inline font-size, .centres-search-input's existing !important size, and .centres-header select (specificity 0,1,1, which beats a bare select)"
      via: "!important plus later source order. An inline declaration without !important loses to a stylesheet !important. Among !important declarations, equal-or-higher specificity plus later order wins"
      pattern: "font-size: var\\(--fs-input\\) !important"
    - from: ".results-table { font-size: var(--fs-small) }"
      to: ".textpetit, .detall-ciclos-d, .ciclos-d-list, td.col-nom"
      via: "absolute token sizes replace the em values that compounded on the table's 0.9em"
      pattern: "\\.results-table td\\.col-nom"
---

<objective>
Typeset fp-cercador/frontend/index.html with a role-based type scale. Add nine --fs-* tokens to :root and replace every literal, em and rem font size in the <style> block with those tokens, following the approved mapping. This removes the compounding em sizes (the 7.6px .textpetit at 60% opacity, and the 10.2px nested cicles-D and FPO module lists) and every sub-11px text. It also stops iOS Safari zooming when an input is focused, adds tabular figures to numeric columns, and trims the Google Fonts request: drop unused DM Sans 300 and Geist Mono 500, and add a real DM Sans italic 400. Keep DM Serif Display, DM Sans and Geist Mono. Nothing else changes.

Purpose: small text is currently illegible (7.6px at 60% opacity fails contrast about 50 times per page), sizes are inconsistent (loose rem values in the centres panel), iOS zooms on every filter, and italic text is synthesised.
Output: one commit editing only index.html, plus an uncommitted verification harness and screenshots in this quick-task directory.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@fp-cercador/DESIGN.md
@fp-cercador/frontend/index.html
@.planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py
@.planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/260924-0s3-SUMMARY.md

Constraints (owner, locked): edit index.html only. DESIGN.md is read-only because the orchestrator updates it. No colour changes other than the approved .textpetit color, no copy changes, and no JS behaviour changes. Keep line-heights and weights. Do not otherwise touch inline styles. Commit message (Catalan, conventional, exact subject): style(index): escala tipogràfica per rols, fora mides en em encadenades i zoom d'iOS, fonts ajustades. NEVER add a Co-Authored-By or any other AI attribution trailer; this owner rule overrides any harness default. Stage only fp-cercador/frontend/index.html. The git root is /Users/rogermasellas/AI/Cercador Graus. No push, no deploy. The harness needs no server (it uses Playwright route interception). If any server is ever started, stop it by port with kill $(lsof -tiTCP:<port> -sTCP:LISTEN), because pkill -f does not match on this Mac. Owner shell rule: use rg, fd, bat and eza, not grep, find, cat, ls or sed.

Facts verified during planning. HEAD = base = 1bc9f47. Line numbers are pre-edit and approximate:
- Line ~15 is the Google Fonts link. Its current href is https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700&family=DM+Serif+Display:ital@0;1&family=Geist+Mono:wght@400;500&display=swap
- The new href must be exactly https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=DM+Serif+Display:ital@0;1&family=Geist+Mono:wght@400&display=swap
- :root (~27–50) holds colour tokens only and ends with --scrim. Add the nine --fs-* tokens after --scrim, inside :root. There is no html font-size, so 1rem = 16px.
- Specificity facts that decide the !important questions:
  (a) The base rule input[type="search"] (~114) has specificity (0,1,1) and sets 16px, padding and a 2px border. .centres-search-input (~1095) has (0,1,0), so its non-important declarations LOSE to the base rule, which is why its second group carries !important (~1101–1104). The !important on its font-size is therefore still required: KEEP it and only swap the value to var(--fs-small). The first, non-important size declaration (~1096) is dead but stays. Swap its value too and do not delete it (scope discipline).
  (b) .centres-header select (~1088) has (0,1,1), which beats a bare select. The FPO text search input (~2634) has an inline style that includes a 13px font-size. So the coarse-pointer iOS rule must use !important, and it must be the last rule inside @media (pointer: coarse), which is itself the last block before </style> (~1580–1630).
- The page has no input[type="text"], input[type="email"] or textarea. The real controls are: input[type=search] #search (~2543), the occupation search (~2555), the FPO text search (~2634, inline style), and the centres search .centres-search-input (~3032, rendered only when a centres panel is open); plus select #filter-familia, #filter-nivel, #fpo-familia, #fpo-area, #fpo-especialitat, #fpo-comarca, #fpo-municipi, #fpo-nivell, #fpo-estat and #fpo-modalitat, and the centres CCAA select inside .centres-header (panel only). Checkboxes and radios are unaffected by the rule. Keep input[type="text"] and input[type="email"] in the selector list because they are part of the approved rule.
- There are 12 inline 13px font-size occurrences outside <style>, all written without a space after the colon: ~2635 (FPO search), 3280, 3286, 3292, 3311, 3386, 3420, 3434, 3436, 3438, 3440, and 3442 inside a commented-out footer link. Swap all 12 values to var(--fs-small), including the commented-out one, so that the markup check is an exact textual substitution.
- There are no CSS rules for td.col-hores or td.col-ncursos. Those classes exist only on the FPO table cells (~3258, ~3260; added by 260924-0s3). td.col-niv exists on both tables (~2854, ~3256).
- The pagination buttons are real <button> elements. The Chrome UA font shorthand resets font-variant-numeric on buttons, so put tabular-nums directly on .pagination button and not only on the .pagination nav.
- Real DOM nesting that the harness probe must mirror:
  - main row td.col-nom > span > span.textpetit (~2838)
  - detail row td[colspan=5].detall-certificat > div.detall-inner > div.detall-btns (a.btn-doc), and a sibling div.detall-ciclos-d > div > ul.ciclos-d-list > li (~2858–2885)
  - centres row tr.centres-panel > td[colspan=5] > div.centres-container > p.textpetit + div.centres-header (span, select, input[type=search].centres-search-input, button.watch-btn, then a plain ✕ button) + div.centre-item > div > (div.centre-nom > span + span.badge-nou, then div.centre-meta) + p.centres-more / p.centres-buit / p.centres-upsell > a (~3016–3130)
- The centres panel opens by clicking a .badge-centres (rendered when /api/centres/count gives >0, and the mock gives 3). The mocked /api/centres returns [], so the header with its select and search still renders. .centres-header button (0,1,1) and button.watch-btn (0,1,1) tie, and the later rule wins, so watch-btn takes the button.watch-btn size.
- Harness facts (from 0s3): python3 has playwright (sync API) and PIL. chromium.launch(channel="chrome") drives the installed Google Chrome headless. A context with has_touch=True makes (pointer: coarse) and (hover: none) match. Do not set is_mobile, because it breaks innerWidth. There is no GNU timeout binary. The 0s3 overflow caveat: with the protective html/body/[x-data] rule neutralised, the only offenders were FPO filter <select>s at 320/390 (right edge about 612px).

APPROVED MAPPING. Replace the value only and keep the rest of each rule. "sm" = var(--fs-small), "body" = var(--fs-body), "lbl" = var(--fs-label), "mono" = var(--fs-mono):
| ~line | selector | now | becomes |
|---|---|---|---|
| 54 | body | 14px | body |
| 74 | .topbar-logo | 20px | var(--fs-title) |
| 87 | .hero h1 | 48px | var(--fs-display) |
| 95 | .hero-sub | 15px | var(--fs-lead) |
| 119 | input[type="search"] | 16px | var(--fs-input) |
| 153 | .grau-tabs button | 14px | body |
| 184 | .mode-toggle button | 14px | body |
| 207, 220, 230 | .fpo-note, .fpo-banner, .fpo-curs | 13px | sm |
| 248 | .fpo-estat | 11px | lbl |
| 283 | .repr-toggle button | 13px | sm |
| 310 | .ocup-card h2 | 15px | var(--fs-lead) |
| 328 | .ocup-tag | 12px | sm (13px, accepted one-step change) |
| 343, 357 | .ocup-btn, .ocup-link | 13px | sm |
| 374 | .ocup-wall | 14px | body |
| 396, 405, 421, 446, 460 | .filter-bar-label, select, .checkbox-label, .clear-btn, .save-alert-btn | 13px | sm |
| 490 | .results-count | 13px | sm (+ tabular-nums) |
| 500 | .results-table | 0.9em | sm |
| 517 | .results-table th | 11px | lbl |
| 561 | .results-table td.col-nom (has only font-weight: 500) | none | ADD body |
| 567 | .results-table td.col-codi | 12px | mono |
| 572, 587 | .results-table td.col-fam (two rules) | 13px | sm |
| after 581–584 | NEW rule: .results-table td.col-niv, .results-table td.col-hores, .results-table td.col-ncursos | none | font-variant-numeric: tabular-nums (a new rule, NOT added to the col-niv rule, because that rule's color and min-width must not spread to the other cells) |
| 641 | .sort-indicator | 10px | lbl |
| 654 | .badge-old | 11px | lbl |
| 667 | .badge-nou | 10px | lbl |
| 716 | .error-state | 14px | body |
| 733 | .pagination-info | 13px | sm (+ tabular-nums) |
| 751 | .pagination button | 13px | sm (+ tabular-nums) |
| 782 | .pagination-ellipsis | 13px | sm |
| 815 | @768 .hero h1 | 30px | var(--fs-display-sm) |
| 819 | @768 .hero-sub | 14px | body |
| 833 | @768 .grau-tabs button | 13px | sm |
| 884 | .detall-inner .detall-hores | 13px | sm |
| 896, 909 | .btn-doc, .watch-btn | 12px | sm |
| 1003 | .badge-centres | 0.75rem | mono (12px, same value as today) |
| 1022 | .textpetit | 0.6em plus opacity 0.6 | lbl, color: var(--warm), opacity: 1 (keep margin-left: 5px) |
| 1036 | .badge-itinerari-b | 11px | lbl |
| 1043 | .ciclos-d-list | 0.9em | sm |
| 1059 | .detall-ciclos-d | 0.9em | sm |
| 1080 | .centres-header | 0.9rem | body |
| 1089 | .centres-header select | 0.85rem | sm |
| 1096 | .centres-search-input (first, non-important) | 0.85rem | sm |
| 1104 | .centres-search-input (second) | 0.85rem !important | sm !important (KEEP !important, see fact (a)) |
| 1118 | .centres-header button (✕) | 1rem | body |
| 1123 | button.watch-btn | 12px | sm |
| 1166 | .centre-nom | 0.88rem | body |
| 1183, 1190, 1197 | .centre-meta, .centres-more, .centres-buit | 0.78/0.82/0.85rem | sm |
| 1228 | .centres-modal-close | 18px | LEAVE AS IS (glyph) |
| 1240 | .centres-modal-msg | 16px | body (prose) |
| 1258 | .centres-modal-btn | 14px | body |
| 1280, 1291 | .centres-modal-btn--secondary, .centres-modal-login-link | 13px | sm |
| 1308 | .centres-upsell | 0.85rem | sm |
| 1337, 1341 | .auth-greeting, .auth-btn | 13px | sm |
| 1380 | .lang-btn | 11px | lbl |
| 1462 | @768(2nd) card meta td:not(.col-nom) | 13px | sm |
| 1468 | @768(2nd) card td.col-codi | 12px | mono |
| 1474 | @768(2nd) card td:not(.col-nom)::before | 13px | sm |
| 1506 | @768(2nd) #results-table td.col-grau | 11px | lbl |
| end of (pointer: coarse) | NEW iOS rule (Task 3) | none | var(--fs-input) !important |
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — tokens plus the em-chain fix, proven end-to-end in headless Chrome at 1280 and 390px</name>
  <files>.planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/type_check.py, fp-cercador/frontend/index.html</files>
  <precondition>python3 imports playwright.sync_api and chromium.launch(channel="chrome") starts the installed Google Chrome (verified at planning time; 0s3 used the same setup)</precondition>
  <action>
Part A — the harness. Copy .planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py to type_check.py in this quick-task directory. It sits at the same depth, so REPO_ROOT = parents[3] still resolves to the git root. Then make these changes:
- Set BASE_REF to "1bc9f47". Rename the module docstring and usage text to type_check.py. The success marker becomes TYPE_CHECK_OK and the failure header becomes TYPE CHECK FAILURES:.
- Delete the desktop pixel-diff group (check_desktop and render_desktop) and the PIL import. The desktop pixel-diff no longer applies, because desktop changes by design.
- Keep the cards, fpo, topbar, toggle and touch groups verbatim as regression groups. Remove the old overflow group, because Task 3 replaces it.
- Add helpers:
  - px(page, selector, prop='fontSize') returns a float from getComputedStyle of the first matching element and fails loudly if nothing matches.
  - inject_probe(page) appends a static, visible table with classes "results-table" and id __type_probe__ (NOT id results-table) to the end of the main element. It reproduces the real nesting chains listed in <context>, copying the wrapper classes from the real markup:
    - (1) tbody > tr > td.col-nom > span > span.textpetit
    - (2) tbody > tr > td[colspan=5].detall-certificat > div.detall-inner > (div.detall-btns > a.btn-doc) + div.detall-ciclos-d > div > (span + ul.ciclos-d-list > li)
    - (3) tbody > tr.centres-panel > td[colspan=5] > div.centres-container containing: p.textpetit; div.centres-header with a span, a select with one option, an input type=search with class centres-search-input, a button.watch-btn, and a plain ✕ button; div.centre-item > div > (div.centre-nom > span + span.badge-nou) + div.centre-meta; p.centres-more; p.centres-buit; and p.centres-upsell > a
    - (4) outside the table, a p.centres-modal-msg, a button.centres-modal-close, a button.centres-modal-btn, a button.centres-modal-btn.centres-modal-btn--secondary and an a.centres-modal-login-link, each with short text.
  The probe exercises the real cascade for conditional or data-dependent structures without depending on API data.
- Add group compound. It runs for width in (1280, 390), fine pointer, lang ca, auth user, name mode, after inject_probe. It asserts:
  - each of the nine --fs-* custom properties, read with getComputedStyle(document.documentElement).getPropertyValue and trimmed, equals its approved value
  - the first #results-table tbody td.col-nom is 14px with fontWeight 500
  - the first #results-table tbody td.col-niv is 13px
  - both probe .textpetit elements are 11px, with color equal to resolve_color(page, '--warm') and opacity '1'
  - the probe .detall-ciclos-d, its .ciclos-d-list and that list's li are all 13px (this proves the nested em compounding is gone)
Organisation of the rest is at your discretion, but keep --only group selection and the non-zero exit on failure.

Part B — index.html, tracer slice only:
- Add the nine tokens to :root after --scrim, with these values: --fs-display 48px, --fs-display-sm 30px, --fs-title 20px, --fs-lead 15px, --fs-body 14px, --fs-small 13px, --fs-label 11px, --fs-mono 12px, --fs-input 16px (approved type system).
- Set .results-table's font size to var(--fs-small).
- Add font-size var(--fs-body) to the existing .results-table td.col-nom rule that holds font-weight 500. Do not create a new rule.
- Change .textpetit to font-size var(--fs-label), add color var(--warm), change its opacity to 1, and keep margin-left.
- Set .ciclos-d-list and .detall-ciclos-d to var(--fs-small).
Do not commit yet. The owner wants a single commit, which Task 3 makes.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && Q=.planning/quick/260924-1rf-typeset-index-html-role-type-scale-token && F=fp-cercador/frontend/index.html && python3 "$Q/type_check.py" --only compound,cards && ! rg -q 'font-size: 0\.(6|9)em' "$F" && test "$(rg -c 'var\(--fs-' "$F")" -ge 5 && echo T1_OK</automated>
  </verify>
  <done>type_check.py exists, has no desktop pixel-diff group, and prints TYPE_CHECK_OK for --only compound,cards. The tokens resolve in the browser. At both 1280 and 390, names are 14px/500, the table body is 13px, .textpetit is 11px in --warm at full opacity, and the nested cicles-D list is 13px. The 0s3 cards regression still passes. Nothing is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Token migration of every remaining size, inline sizes, tabular figures and font delivery, with static, role, min-size and font gates</name>
  <files>fp-cercador/frontend/index.html, .planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/type_check.py</files>
  <action>
Part A — index.html:
- Apply the APPROVED MAPPING table in <context> to every remaining row. Replace only the value and leave each rule's other declarations untouched. The .centres-modal-close glyph stays at 18px. The second .centres-search-input size keeps its !important (fact (a) in <context>).
- Add font-variant-numeric tabular-nums to the .results-count, .pagination-info and .pagination button rules.
- Insert the NEW grouped rule for .results-table td.col-niv, .results-table td.col-hores and .results-table td.col-ncursos (tabular-nums only) directly after the existing .results-table td.col-niv rule that holds color var(--warm). Write the selector list one selector per line in the file's existing style.
- In the markup, swap the 12 inline 13px font-size values for var(--fs-small). Change nothing else in those style attributes, and include the commented-out footer link.
- Replace the Google Fonts href with the exact new URL given in <context>.
Keep every CSS comment you add on a single line (/* ... */). Do not add or remove colours or change any line-height or weight.

Part B — type_check.py, add these groups:
- static (source-only, no browser). Read the work-tree index.html and git show 1bc9f47:fp-cercador/frontend/index.html, and split each into head (before the <style> line), style block, and rest (from </style> on). Assert:
  - (S1) :root in the work style block declares each of the nine tokens with its exact value.
  - (S2) Every font-size declaration in the work style block has a value matching var(--fs-(display|display-sm|title|lead|body|small|label|mono|input)) with an optional !important, or is exactly 18px. The 18px value appears exactly once, and no em or rem font size remains.
  - (S3) The work rest equals the base rest with every occurrence of the no-space inline 13px font-size replaced by font-size:var(--fs-small). Also assert the base had exactly 12 occurrences. This proves the markup and all JS are otherwise byte-identical.
  - (S4) The work head equals the base head with the old font href replaced by the new one, using the exact strings from <context>.
  - (S5) A difflib line diff of the two style blocks where every added or removed line (ignoring whitespace-only lines) matches one of these allowlist patterns: contains font-size; contains font-variant-numeric; contains --fs-; is exactly opacity 0.6 or opacity 1; is exactly color var(--warm); is a selector line (no semicolon, ends with a comma or an opening brace); is a closing brace; is a single-line comment. Exactly one added line is color var(--warm), and no removed line contains color or background.
  - (S6) The .textpetit rule block contains var(--fs-label), color var(--warm), opacity 1 and margin-left 5px.
  - (S7) The .centres-search-input rule still carries var(--fs-small) !important.
  - (S8) The style block's opening and closing braces are balanced.
- roles. Contexts: 1280 fine pointer and 390 fine pointer, lang ca, auth user, name mode, with inject_probe; plus 1280 and 390 in FPO mode. Assert the computed font sizes (px):
  - both widths: body 14; .topbar-logo 20; #search 16; .mode-toggle button 14; #filter-familia 13; .results-count 13 with fontStyle italic; the first #results-table th 11; the first #results-table th .sort-indicator 11; the first #results-table tbody td.col-nom 14; td.col-niv 13; td.col-codi 12; td.col-fam 13; .pagination-info 13; the first .pagination button 13; the first .auth-btn 13; the first .lang-btn 11; the first .badge-centres 12
  - 1280 only: .hero h1 48; .hero-sub 15; .grau-tabs button 14; td.col-grau 13
  - 390 only: .hero h1 30; .hero-sub 14; .grau-tabs button 13; td.col-grau 11
  - probe elements: .btn-doc 13; .centres-header 14; .centres-header select 13; .centres-search-input 13; button.watch-btn 13; the ✕ button 14 (the .centres-header button that is not .watch-btn); .centre-nom 14; .centre-meta 13; .centres-more, .centres-buit and .centres-upsell 13; .badge-nou 11; .centres-modal-msg 14; .centres-modal-close 18; .centres-modal-btn 14; .centres-modal-btn--secondary 13; .centres-modal-login-link 13
  - FPO: .results-table--fpo tbody td.col-nom 14; td.col-hores and td.col-ncursos 13
  - fontVariantNumeric is tabular-nums on #results-table td.col-niv, .results-count, .pagination-info, .pagination button, and the FPO td.col-niv, td.col-hores and td.col-ncursos
- minsize. Use the same four contexts, with the probe injected in name mode. Walk every element that has a direct non-whitespace text node, is not display none or visibility hidden, has getClientRects().length greater than 0, and is not inside .sr-only. Fail on any element whose computed fontSize is below 10.99, listing its tag, class, first 30 characters of text and size. At 390 also check each visible #results-table and FPO card td ::before that has content other than none or normal.
- fonts. Use a 1280 context with fonts=True. Record the fonts.googleapis.com/css2 response status through page.on('response') and page.on('requestfailed'). Assert the link href equals the new URL. Then, if the stylesheet loaded with status 200:
  - await document.fonts.load for italic 400 16px "DM Sans"
  - iterate document.fonts, stripping quotes from family
  - assert that at least one DM Sans face with style italic has status loaded
  - assert that no DM Sans face's weight string contains 300
  - assert that no Geist Mono face's weight string contains 500
  - assert that .results-count computes fontStyle italic
  If the stylesheet did not load, print FONTS_LIVE=SKIPPED_OFFLINE without failing, because S4 still pins the URL. Otherwise print FONTS_LIVE=PASS.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && Q=.planning/quick/260924-1rf-typeset-index-html-role-type-scale-token && F=fp-cercador/frontend/index.html && python3 "$Q/type_check.py" --only compound,static,roles,minsize,fonts,cards,fpo && test -z "$(rg -o 'font-size: ?[0-9.]+(px|r?em)' "$F" | rg -v '^font-size: 18px$')" && test "$(rg -c 'font-size: 18px' "$F")" -eq 1 && test "$(rg -c 'font-size:var\(--fs-small\)' "$F")" -eq 12 && rg -q 'Geist\+Mono:wght@400&display=swap' "$F" && echo T2_OK</automated>
  </verify>
  <done>All style-block sizes are tokens, except the single 18px glyph. The 12 inline sizes read var(--fs-small). The markup and JS outside <style> are proven byte-identical apart from that swap. Every computed role size matches the approved scale at 1280 and 390. No visible text is below 11px. Tabular figures apply to the numeric cells, count and pagination. The font URL is the new one, and the italic face loads live (or FONTS_LIVE=SKIPPED_OFFLINE is printed). The compound, cards and fpo groups still pass.</done>
</task>

<task type="auto">
  <name>Task 3: iOS focus-zoom rule, overflow and regression gates, screenshots, single commit</name>
  <files>fp-cercador/frontend/index.html, .planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/type_check.py</files>
  <action>
Part A — index.html. Add the approved iOS rule as the LAST rule inside the existing @media (pointer: coarse) block, immediately before that block's closing brace. The selectors are select, input[type="search"], input[type="text"], input[type="email"] and .centres-search-input, one per line. The only declaration is font-size var(--fs-input) !important. Precede the rule with one single-line English comment explaining that !important is needed to beat the FPO search's inline size, the .centres-search-input !important, and .centres-header select's higher specificity. Do not create a new @media block.

Part B — type_check.py, add these groups:
- ioszoom. Build a 390 context with has_touch=True (auth user, name mode) and assert that matchMedia('(pointer: coarse)').matches is true. If it is not, fail with a clear message; never pass silently. Click the first .badge-centres and wait for .centres-header select and .centres-search-input. Enumerate every select, input[type=search], input[type=text], input[type=email] and textarea in the DOM (computed size resolves even for x-show-hidden bars). Assert each is at least 16px. Also assert that the enumeration includes #filter-familia, #filter-nivel, all 8 #fpo-* selects, the FPO text search (.filter-bar input[type=search] bound to fpoFilters.text), .centres-header select, .centres-search-input and #search. Repeat the enumeration on an FPO-mode coarse page.
  Then run a control in a 1280 fine-pointer context with the centres panel open. Assert that matchMedia coarse is false, #filter-familia, the centres select, .centres-search-input and the FPO text search are 13px, and #search is 16px. This proves the rule does not leak to desktop and that the kept !important still works.
  Print CENTRES_HEADER_OVERFLOW@390coarse=<scrollWidth minus clientWidth of .centres-header> as information only.
- overflow. Reuse the 0s3 neutralising style (html, body and [x-data] get overflow-x visible and max-width none, both !important). Scenarios:
  - fine pointer: 320 and 390 × ca and es × guest and user, in name mode; plus FPO mode at 320 and 390 (ca, user)
  - coarse pointer (has_touch): 320 and 390 × ca and es (user), in name mode; plus FPO mode at 320 and 390 (ca)
  For each scenario, pass if documentElement.scrollWidth is at most innerWidth + 0.5. Otherwise collect ALL offenders: visible elements with rect.right greater than innerWidth + 0.5, excluding elements that have an ancestor (other than html, body and [x-data]) whose computed overflow-x is auto, scroll or hidden, and excluding thead and .sr-only. Pass only if every offender is a SELECT whose id starts with fpo- (the documented 0s3 caveat). Any other offender, or an overflow with no identified offender, fails.
  Print OVERFLOW_GATE=PASS or the offender list.
- shots. Save viewport PNGs (fonts=True, transitions and animations disabled, viewport height 1400) to a shots/ subdirectory of this quick-task directory:
  - after-1280-name.png, after-1280-fpo.png, after-390-name.png, after-390-fpo.png, after-320-name.png
  - after-390-centres-coarse.png (has_touch; first .badge-centres clicked and scrolled into view)
  - before-1280-name.png and before-390-name.png, rendered from ref 1bc9f47
  Do not inject the probe for screenshots.
Set DEFAULT_ORDER to compound, static, roles, minsize, fonts, ioszoom, cards, fpo, topbar, toggle, touch, overflow, shots.

Run the full harness (no --only) with a Bash timeout of at least 600000 ms.

If, and only if, the overflow group reports an offender that is a select without an fpo- id, and it appears only under coarse pointer (caused by the 16px rule), then add max-width 100% to the select selector in a separate rule at the end of the same coarse block, record it as a deviation in the SUMMARY, and rerun. Do not add it pre-emptively.

Part C — commit. Stage only fp-cercador/frontend/index.html and leave the harness, log and shots uncommitted. Commit with the exact subject from <context>. Add a short Catalan body listing the role tokens, the removed em chain, the iOS zoom fix, the tabular figures and the font trim. Add no trailer of any kind.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && Q=.planning/quick/260924-1rf-typeset-index-html-role-type-scale-token && F=fp-cercador/frontend/index.html && python3 "$Q/type_check.py" > "$Q/type_check.log" 2>&1; tail -15 "$Q/type_check.log"; rg -q '^TYPE_CHECK_OK$' "$Q/type_check.log" && rg -q '^OVERFLOW_GATE=PASS' "$Q/type_check.log" && test "$(rg -c '@media \(pointer: coarse\)' "$F")" -eq 1 && test "$(rg -c '@media \(max-width: 768px\)' "$F")" -eq 2 && test "$(rg -c 'font-size: var\(--fs-input\) !important' "$F")" -eq 1 && test -z "$(rg -o 'font-size: ?[0-9.]+(px|r?em)' "$F" | rg -v '^font-size: 18px$')" && test "$(fd -e png . "$Q/shots" | wc -l | tr -d ' ')" -ge 8 && test "$(git show --name-only --format= HEAD)" = "$F" && test -z "$(git diff --name-only -- fp-cercador)" && test "$(git log -1 --format=%s)" = "style(index): escala tipogràfica per rols, fora mides en em encadenades i zoom d'iOS, fonts ajustades" && BODY="$(git log -1 --format=%B)" && test -n "$BODY" && ! printf '%s' "$BODY" | rg -qi 'co-authored|claude-session|generated with' && echo ALL_GATES_OK</automated>
  </verify>
  <done>The full harness prints TYPE_CHECK_OK and OVERFLOW_GATE=PASS. Every real select and search/text input computes at least 16px under a coarse pointer and keeps its previous size on a fine pointer. At least 8 screenshots, including before/after pairs, are in shots/. Exactly one new commit exists: it touches only index.html, has the exact subject and carries no attribution. The working tree under fp-cercador is clean, and DESIGN.md and i18n.js are untouched. The SUMMARY records the CENTRES_HEADER_OVERFLOW value, the FONTS_LIVE result, and whether the conditional select max-width was needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → fonts.googleapis.com / fonts.gstatic.com | Existing third-party stylesheet and font fetch. The origin is not new; only the query string changes |
| harness → local Chrome | Dev-only script. Every non-mocked host is aborted except the Google Fonts hosts in font-enabled contexts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1rf-01 | Information disclosure | Google Fonts link in index.html head | low | accept | The request already exists with the same origins and display=swap. This task only trims the axis/weight list and adds no new third party |
| T-1rf-02 | Tampering | index.html markup/JS while editing styles | medium | mitigate | Static group S3/S4 proves everything outside <style> is byte-identical to 1bc9f47 except the inline token swap and font href. S5 allowlists every changed style line, so no colour, JS or copy change can slip in |
| T-1rf-03 | Repudiation | commit scope/attribution | low | mitigate | Task 3 verify asserts that HEAD touches only index.html, that the subject is exact, and that the body has no co-authored, claude-session or generated-with text |
| T-1rf-04 | Denial of service | harness route handler | low | accept | Local dev tool with no network listener, aborting all unmocked hosts. No package installs, so no supply-chain surface |
</threat_model>

<verification>
- `python3 .planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/type_check.py` prints TYPE_CHECK_OK. Its groups are: compound, static, roles, minsize, fonts, ioszoom, and the cards, fpo, topbar, toggle and touch regression groups, then overflow and shots.
- No font-size literal remains in index.html except the single 18px modal-close glyph, and no em or rem size remains.
- The single commit's subject is exact, it touches only index.html, and it has no trailer.
</verification>

<success_criteria>
- Role tokens exist and drive every font size. The compounding em sizes are gone. Nothing visible renders below 11px, and .textpetit is legible (11px, --warm, opacity 1).
- iOS Safari no longer zooms on any filter, search or centres control (at least 16px under a coarse pointer), and desktop controls keep their size.
- Numeric columns, the count and pagination use tabular figures.
- The fonts request drops the unused weights and ships a real DM Sans italic.
- No new horizontal overflow at 320/390, and all 0s3 regression groups pass.
- One clean commit edits only fp-cercador/frontend/index.html.
</success_criteria>

<output>
Create `.planning/quick/260924-1rf-typeset-index-html-role-type-scale-token/260924-1rf-SUMMARY.md` when done
</output>
