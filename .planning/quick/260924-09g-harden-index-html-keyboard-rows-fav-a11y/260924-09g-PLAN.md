---
phase: quick-260924-09g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/frontend/index.html
  - fp-cercador/frontend/i18n.js
autonomous: true
requirements: [quick-260924-09g]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Keyboard users can Tab to every actionable result row (grade C always; A/B/C with ficha_id; D/E with ficha_url) and to every FPO row. Enter or Space on a focused row does exactly what a click does. Enter or Space on a nested heart button or badge does NOT activate the row (scope item 1)"
    - "Grade C result rows and FPO rows expose aria-expanded (\"true\"/\"false\"), which follows the visible detail row. A keyboard-focused row shows the shared 2px umber (--warm) outline. Mouse clicks do not show it (scope item 1)"
    - "The favourite heart is visible on touch devices (hover: none) and whenever it has keyboard focus, with the shared focus outline. Both heart buttons (results table and FPO table) announce an aria-label matching their title and an aria-pressed state (scope item 2)"
    - "Exactly one <main id=\"main\"> landmark wraps the hero, filters, loading and error states and all result views (name, occupation, FPO). The top bar, the two modals and the footer stay outside it, and the layout does not change (scope item 3)"
    - "Pagination shows a … ellipsis between page-number runs that are not next to each other. With two ellipses (e.g. page 10 of 50) both render, because every x-for key is unique (scope item 4)"
    - "Occupation card titles are h2 elements (h1 → h2 order) and look the same as before: 15px, weight 600, var(--dark), margin-bottom 8px (scope item 5)"
    - "When n === 1, the singular string renders in CA and ES for: FPO availability ('1 curs actiu'), centres badge ('1 centre'), centres overflow ('i 1 centre més'), export tooltip ('1 registre'). Other counts keep their existing plural strings (scope item 6)"
    - "When the catalogue fails to load, public users see a plain-language message with no emoji and a 'Torna-ho a provar' / 'Volver a intentarlo' button. The button re-runs only the /api/ofertes load. It does not fetch auth again, reload favourites, or register watchers or listeners again. On success the results appear, and a ?oferta= deep link still opens (scope item 7)"
    - "Exactly one commit touches exactly index.html and i18n.js, with the exact Catalan conventional subject and no AI attribution"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: "activateRow/rowIsActionable/loadCatalog component methods; keyboard-operable result and FPO rows; heart a11y + visibility CSS; <main> landmark; single-root pagination items with unique keys; h2 occupation card titles; _one plural key selection; error state with retry button"
      contains: "<main id=\"main\">"
    - path: "fp-cercador/frontend/i18n.js"
      provides: "New CA+ES keys: index.itinerari.fpo_disponible_one, index.centres.count_one, index.centres.more_one, index.btn.exportTitle_one, index.error.retry; rewritten index.error copy"
      contains: "'index.error.retry'"
  key_links:
    - from: "results <tr> @click / @keydown.enter.self / @keydown.space.self.prevent"
      to: "activateRow(row) component method"
      via: "single shared method so that click and keyboard cannot drift apart"
      pattern: "activateRow\\(row\\)"
    - from: "results <tr> :class row-link and :tabindex"
      to: "rowIsActionable(row) predicate"
      via: "one predicate drives both the pointer cursor class and focusability"
      pattern: "rowIsActionable\\(row\\)"
    - from: "error-state retry button @click"
      to: "loadCatalog() (no argument → fresh fetch)"
      via: "init() calls loadCatalog(catalogRequest) with the fetch it already started, so the retry path and the init path share one parser"
      pattern: "loadCatalog\\("
    - from: "t(n === 1 ? '<key>_one' : '<key>', {n}) calls in index.html"
      to: "the *_one keys in both the ca and es blocks of i18n.js"
      via: "flat-key lookup in window.t / _resolve (falls back to CA, then to the raw key)"
      pattern: "_one'"
---

<objective>
Harden fp-cercador/frontend/index.html (Alpine.js 3.15 + vanilla JS, no build step) for keyboard, screen-reader and touch users. Also fix two latent bugs: pagination ellipses never render, and the catalogue error state is a dead end. The work covers the 7 approved scope items and nothing else.

Purpose: result rows and hearts can be reached without a mouse, and the page has proper landmarks and heading order. Singular counts read correctly, and a transient API failure can be recovered without a page reload.
Output: edited index.html and i18n.js, in one commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@fp-cercador/DESIGN.md

Hard constraints (from CLAUDE.md, DESIGN.md and the owner):
- Only two files change: fp-cercador/frontend/index.html and fp-cercador/frontend/i18n.js. Add no dependencies and no new colours. Every colour is a `var(--token)` from `:root`. Focus treatment is `outline: 2px solid var(--warm); outline-offset: 2px` (the row rule uses -2px, see Task 1).
- Alpine gotcha (project memory): a `<template x-if>` or `<template x-for>` keeps only its FIRST element child. A second sibling root silently disappears. This is the root cause of scope item 4.
- The user's shell rule: use rg/fd/bat/eza, never grep/find/cat/ls.
- Line numbers below refer to HEAD 71d4d68. They drift once earlier edits insert lines, so anchor every edit on the quoted content, not the number.

Code map (index.html at HEAD 71d4d68):
- CSS: `.ocup-card` heading rule L309 (explicit font-size 15px / weight 600 / color / margin-bottom; a universal reset at L19-25 zeroes margin/padding, so h2 vs h3 UA defaults do not leak). `.save-alert-btn` L459-482. `.results-table tbody tr.row-link` L599. `.error-state` L701. `.pagination-ellipsis` L762. `.btn-fav` L941-962 (opacity 0 until row hover). Existing focus pattern: `.badge-centres:focus-visible` L1001.
- Component `Alpine.data('cercador', …)` inside the single inline `<script>` L1394-2231. `init()` L1512-1573. `buildPagination()` L1668 (returns `{type:'page', n}` and `{type:'ellipsis'}` items; two ellipses are identical objects). `toggleFavorite(row)` L1804 (already gates on loggedIn). `toggleFpoDetall(codi)` L1977. `fetchFpoByCert(codigo)` L1987 (idempotent: returns early if cached).
- Markup: `<div x-data="cercador">` L2237 wraps topbar + everything up to `</div><!-- /x-data -->` L3149. The footer (L3151) is OUTSIDE x-data. Topbar L2240-2249. Hero opens L2252. Error state L2453-2454. Results row `<tr>` L2528-2540 (long inline @click ternary). Main-table heart L2542-2550. Nested role=button spans L2562-2565 and L2576-2579 already use `@keydown.enter.stop` / `@keydown.space.prevent.stop`. Grade C detail rows: L2587 (plan_antiguo) and L2658 (!plan_antiguo), both keyed on expandedRows. FPO availability span L2648. Pagination x-for L2881-2890. Occupation card heading L2913. FPO content div L2940-3088 (ends right after the `t('fpo.empty')` paragraph). FPO row L2961. FPO heart L2963-2971. Alert modal starts at the comment "Modal confirmació alerta desada" L3090.
- i18n.js: flat keys in two blocks, `ca: {` (L3) and `es: {` (L526). `window.t(key, vars)` L1057 does `{name}` interpolation and has no plural support. Relevant keys: index.error CA L86 (double-quoted, contains an apostrophe) / ES L609. index.centres.count L102/L625. index.centres.more L115/L638. index.btn.exportTitle L126/L649. index.itinerari.fpo_disponible L178/L701.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Keyboard-operable result rows (tracer: component method → row markup → focus CSS, main table + FPO table)</name>
  <files>fp-cercador/frontend/index.html</files>
  <read_first>
    fp-cercador/frontend/index.html L595-605 (row-link CSS), L1975-1995 (toggleFpoDetall, fetchFpoByCert), L2526-2590 (results row + heart + nested badges), L2956-2982 (FPO row)
  </read_first>
  <action>
Scope item 1. This task is the end-to-end slice. It covers the Alpine component method, the row bindings and the focus style, and it proves that keyboard activation works on one path before the other items are touched.

A. Component methods. Add two methods to the `cercador` Alpine.data object, next to `fetchFpoByCert(codigo)`. Keep comma/brace hygiene: every method is followed by a comma exactly like its neighbours.
- `rowIsActionable(row)` returns a boolean: `!!( row.grado === 'C' || (['A','B','C'].includes(row.grado) && row.ficha_id) || (['D','E'].includes(row.grado) && row.ficha_url) )`. This is the exact condition of the current `row-link` class.
- `activateRow(row)` is a plain `if` chain with the SAME semantics as the current inline @click ternary on the results `<tr>`:
  1. If `row.grado === 'C'`: replace `this.expandedRows` with a spread copy whose `[row.id]` entry is toggled. Then, if `row.plan_antiguo`, call `this.fetchFpoByCert(row.codigo)`. The call runs on both expand and collapse, as today, and is idempotent. Then return.
  2. Else if `['A','B','C'].includes(row.grado) && row.ficha_id`: when logged in, call `window.open(API_BASE + '/api/ficha-redirect?grado=' + row.grado + '&codigo=' + encodeURIComponent(row.codigo), '_blank')`. Otherwise call `this.showCentresModal()`.
  3. Else if `['D','E'].includes(row.grado) && row.ficha_url`: when logged in, call `window.open(row.ficha_url, '_blank')`. Otherwise call `this.showCentresModal()`.
  4. Otherwise do nothing.
  Keep the window.open arguments byte-identical to the current expression. Do not add noopener or change targets; that is out of scope.

B. Results `<tr>` (L2528-2540). Replace the whole multi-line inline @click ternary with `@click="activateRow(row)"`. Change the `row-link` entry of the existing `:class` object to `'row-link': rowIsActionable(row)` and keep the `'row-fav'` entry unchanged. Add:
- `:tabindex="rowIsActionable(row) ? 0 : null"`
- `@keydown.enter.self="activateRow(row)"`
- `@keydown.space.self.prevent="activateRow(row)"`
- `:aria-expanded="row.grado === 'C' ? String(!!expandedRows[row.id]) : null"`. Grade C rows always have a detail row, L2587 or L2658, so the state is truthful.
Keep `:data-row-id` (openOfertaFromLink queries it).

Discretion note on `.self` vs per-control `.stop`: the scope asked that Enter on a nested button must not bubble to the row. The `.self` modifier makes the row listen only when the row element itself is the event target. That covers the heart button and both nested role=button spans, plus any control added later, without editing each one. The `.prevent` then applies only to Space on the row itself, so native Space and Enter on nested buttons keep working. In Alpine 3 the `.self` check wraps outside `.prevent` regardless of the order you write the modifiers. The two nested spans already carry `.stop` on their keydown handlers; leave them as they are.

C. FPO `<tr class="row-link">` (L2961). Keep the class and the `@click="toggleFpoDetall(esp.codi)"`. Add:
- a static `tabindex="0"`, because every FPO row is actionable
- `@keydown.enter.self="toggleFpoDetall(esp.codi)"`
- `@keydown.space.self.prevent="toggleFpoDetall(esp.codi)"`
- `:aria-expanded="String(!!fpoExpanded[esp.codi])"`

D. CSS. Directly after the `.results-table tbody tr.row-link { cursor: pointer; }` rule (L599-601), add:
- `.results-table tbody tr.row-link:focus-visible { outline: 2px solid var(--warm); outline-offset: -2px; }`. The negative offset keeps the ring inside the row, because `.table-wrap` may clip it.
- `.results-table tbody tr.row-link:focus-visible td { background: var(--warm2); }`. This mirrors the existing hover tint, so focus stays visible in any engine that skips outlines on table rows under `border-collapse: collapse` (the table uses collapse, L499). It uses no new colour.

Do NOT commit at the end of this task. There is one commit, at the end of Task 3.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/frontend" && node -e 'const h=require("fs").readFileSync("index.html","utf8");const re=/<script(?![^>]*\ssrc=)[^>]*>([\s\S]*?)<\/script>/g;let m,n=0;while((m=re.exec(h))){new Function(m[1]);n++}if(n<1)throw new Error("no inline script");console.log("inline scripts parse:",n)' && test "$(rg -c '^\s+activateRow\(row\) \{' index.html)" = 1 && test "$(rg -c '^\s+rowIsActionable\(row\) \{' index.html)" = 1 && test "$(rg -c 'rowIsActionable\(row\)' index.html)" -ge 3 && rg -q '@click="activateRow\(row\)"' index.html && rg -q '@keydown\.enter\.self="activateRow\(row\)"' index.html && rg -q '@keydown\.space\.self\.prevent="activateRow\(row\)"' index.html && rg -q '@keydown\.enter\.self="toggleFpoDetall\(esp\.codi\)"' index.html && rg -q '@keydown\.space\.self\.prevent="toggleFpoDetall\(esp\.codi\)"' index.html && test "$(rg -c ':aria-expanded=' index.html)" = 2 && rg -q 'tr\.row-link:focus-visible \{' index.html && ! rg -q 'expandedRows = \{ \.\.\.expandedRows' index.html && echo TASK1-OK</automated>
  </verify>
  <done>
The inline script still parses. activateRow and rowIsActionable are each defined exactly once. The results `<tr>` has no inline ternary left: its click, Enter and Space all call activateRow(row), and its class and tabindex both come from rowIsActionable(row). Grade C rows and FPO rows carry aria-expanded. FPO rows are focusable and toggle on Enter/Space. The focus-visible row rule uses var(--warm). TASK1-OK is printed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Heart visibility + a11y, &lt;main&gt; landmark, single-root pagination items, h2 occupation titles</name>
  <files>fp-cercador/frontend/index.html</files>
  <read_first>
    fp-cercador/frontend/index.html L300-315 (ocup card CSS), L760-767 (pagination-ellipsis CSS), L940-963 (btn-fav CSS), L2236-2253 (x-data root, topbar, hero start), L2540-2550 and L2960-2971 (heart buttons), L2878-2893 (pagination x-for), L2910-2915 (occupation card), L3084-3092 (end of FPO content, start of alert modal)
  </read_first>
  <action>
A. Hearts (scope item 2).
- CSS: after the `.btn-fav:hover, .btn-fav--active { color: var(--heart); }` rule (L959-962), add `.btn-fav:focus-visible { opacity: 1; outline: 2px solid var(--warm); outline-offset: 2px; }` and `@media (hover: none) { .btn-fav { opacity: 1; } }`.
- Results-table heart (L2542): add `:aria-label="favorites.has(row.id) ? t('index.fav.remove') : t('index.fav.add')"` (the same expression as its `:title`) and `:aria-pressed="String(favorites.has(row.id))"`.
- FPO heart (L2963): add `:aria-label="fpoFavSet.has(esp.codi) ? t('index.fav.remove') : t('index.fav.add')"` and `:aria-pressed="String(fpoFavSet.has(esp.codi))"`.
- Keep the existing `:title` bindings and the @click handlers as they are.

B. `<main>` landmark (scope item 3). The x-data root `<div x-data="cercador">` wraps the topbar as well, and the footer sits outside it. So place `<main id="main">` INSIDE the x-data div:
- Open it on a new line right after the topbar's closing `</div>`, which ends the `.topbar` block at L2249, and before the `<!-- Hero / Search -->` comment.
- Close it with `</main>` right after the closing `</div>` of the FPO content block. That is the div opened by `x-show="state === 'ready' && searchMode === 'fpo'" class="content"`, whose last child is the `t('fpo.empty')` paragraph. The close goes before the `<!-- Modal confirmació alerta desada -->` comment.
- Both modals stay outside main as siblings. Re-indent nothing else; a two-space indent change inside is optional and not required.
- Layout safety, already checked at HEAD: no CSS uses child or sibling combinators that the new wrapper could break. The only structural selectors are `.centres-header>span`, `tr:last-child td` and `.centre-item:last-child`, and none of them spans the wrapper. The mobile rule `[x-data] { max-width: 100vw; overflow-x: hidden }` still targets the outer div. `main` is display:block with zero margin and padding, because of the universal reset. Add no CSS for main.

C. Pagination (scope item 4). The x-for at L2881 currently has two sibling `<template x-if>` roots, and Alpine keeps only the first, so ellipses never render.
- Wrap both x-if templates in one root element: `<span class="pagination-item">` … `</span>`. The page-button x-if comes first and the ellipsis x-if second, both unchanged inside.
- CSS: after `.pagination-ellipsis` (L762-766), add `.pagination-item { display: contents; }`. This keeps the flex `gap: 4px` of `.pagination-controls` applying to the buttons exactly as today.
- Keys: the current key serialises the whole item, so the two ellipsis items get the same key when both render (e.g. page 10 of 50 gives `1 … 8 9 10 11 12 … 50`). Alpine then drops or misplaces one of them. Keep a `:key`, but make it unique: change the loop to `(item, idx) in buildPagination()` and the key to `item.type === 'page' ? 'p' + item.n : 'e' + idx`. This is the minimum change that lets item 4 actually render. Do not modify buildPagination().

D. Heading order (scope item 5). In the occupation card (L2913), change the heading element from h3 to h2 and keep its `x-text` expression unchanged. Rename the CSS rule at L309 so that its element selector targets `h2` under `.ocup-card` instead of the level-3 heading. Keep its four declarations (font-size 15px, font-weight 600, color var(--dark), margin-bottom 8px) unchanged. They already set size and weight explicitly, and the universal reset zeroes UA margins, so the computed styles stay identical. There is no other h2 rule in the file.

Do NOT commit at the end of this task.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/frontend" && test "$(rg -c '<main id="main">' index.html)" = 1 && test "$(rg -c '</main>' index.html)" = 1 && node -e 'const h=require("fs").readFileSync("index.html","utf8");const a=h.indexOf("<main id=\"main\">"),b=h.indexOf("</main>");const ok=a>h.indexOf("class=\"topbar\"")&&a<h.indexOf("<div class=\"hero\">")&&b>h.indexOf("t(\x27fpo.empty\x27)")&&b<h.indexOf("Modal confirmació alerta desada")&&b<h.indexOf("</div><!-- /x-data -->");if(!ok)throw new Error("main misplaced a="+a+" b="+b);console.log("main placement ok")' && test "$(rg -c ':aria-pressed="String\(' index.html)" = 2 && test "$(rg -c ':aria-label="(favorites|fpoFavSet)\.has' index.html)" = 2 && rg -q '\.btn-fav:focus-visible \{' index.html && rg -q '@media \(hover: none\)' index.html && rg -U -q '<span class="pagination-item">\s*<template x-if="item\.type === .page.">' index.html && rg -q '\.pagination-item \{' index.html && ! rg -q ':key="JSON\.stringify\(item\)"' index.html && rg -q '<h2 x-text="g\.denominacion' index.html && ! rg -q '<h3 x-text="g\.denominacion' index.html && rg -q '\.ocup-card h2 \{' index.html && ! rg -q '\.ocup-card h3' index.html && echo TASK2-OK</automated>
  </verify>
  <done>
One main landmark sits between the topbar and the hero and closes after the FPO content, before the modals. Both hearts carry aria-label and aria-pressed, and they become visible on focus-visible and on hover:none devices. The pagination x-for has one root per item with unique keys, and the ellipsis template now renders. The occupation card title is an h2, styled by the renamed rule. TASK2-OK is printed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Singular strings + catalogue error recovery (loadCatalog + retry), then the single commit</name>
  <files>fp-cercador/frontend/index.html, fp-cercador/frontend/i18n.js</files>
  <read_first>
    fp-cercador/frontend/index.html L1512-1573 (init), L2350-2355 (export button), L2452-2454 (error state), L2561-2565 (centres badge), L2646-2649 (FPO availability), L2853-2856 (centres more)
    fp-cercador/frontend/i18n.js L84-130, L170-180, L605-655, L695-705
  </read_first>
  <action>
A. Plurals (scope item 6). An audit of every `t('…', {n: …})` call in index.html found these:
- `index.itinerari.fpo_disponible` (L2648, n = nCursos ≥ 1) reads "1 cursos actius". FIX.
- `index.centres.count` (L2564, rendered only when n > 0) reads "1 centres" / "1 centros". FIX.
- `index.centres.more` (L2855, n = length − 50, rendered only when length > 50 so n ≥ 1) reads "i 1 centres més". FIX.
- `index.btn.exportTitle` (L2354, button shown only when filteredCount > 0) reads "Exporta 1 registres". FIX.
- These need no change. `index.results.many` (L2474, L2942): the singular is already handled by `index.results.one`. `pagination.page` has no plural noun. `index.itinerari.ciclos_d_parcial` (L2686/L2690): m is the total module count, rendered only when shared < total, so m ≥ 2 in practice. `pagination.showing` is not an n-count. Keys used only by other pages are out of scope.

i18n.js: add each new key directly under its base key, in BOTH blocks:
- `index.itinerari.fpo_disponible_one`
  - CA: `També s'ofereix com a formació per a l'ocupació a Catalunya — 1 curs actiu`
  - ES: `También se ofrece como formación para el empleo en Cataluña — 1 curso activo`
- `index.centres.count_one`
  - CA: `1 centre`
  - ES: `1 centro`
- `index.centres.more_one`
  - CA: `... i 1 centre més`
  - ES: `... y 1 centro más`
- `index.btn.exportTitle_one`
  - CA: `Exporta 1 registre a CSV`
  - ES: `Exportar 1 registro a CSV`

The singular strings contain the literal 1, as `index.results.one` does. Escape CA apostrophes the way the neighbouring base key does: the base CA fpo_disponible uses backslash-escaped single quotes.

index.html: at each of the four call sites, keep the existing `{n: …}` vars object and choose the key with `(<same n expr>) === 1 ? '<key>_one' : '<key>'` as the first argument of t(). Do not introduce a plural helper; there are four call sites and each needs one ternary.

B. Error recovery (scope item 7).

i18n.js:
- Replace the value of `index.error` (keep the key name). The new text has no leading emoji.
  - CA: `No s'han pogut carregar les dades del catàleg. Torna-ho a provar d'aquí a uns moments.` Keep it double-quoted, because it contains apostrophes.
  - ES: `No se han podido cargar los datos del catálogo. Vuelve a intentarlo en unos momentos.`
- Add `index.error.retry` right after `index.error` in both blocks.
  - CA: `Torna-ho a provar`
  - ES: `Volver a intentarlo`

index.html component. Split the catalogue load out of `init()` (L1512) into a new method `async loadCatalog(pendingRequest)`, placed right after init():
- loadCatalog first sets `this.state = 'loading'`. Then, inside try: `const res = await (pendingRequest || fetch(API_BASE + '/api/ofertes'))`. If `!res.ok`, set state to 'error' and return. Otherwise move VERBATIM from init: the `res.json()` parse, the FAMILIA_ALIASES map, allRecords, families, and the _bByCode/_bByUfNum index loop. Then set `this.state = 'ready'`. Then move the two fire-and-forget fetches (`/api/centres/count`, `/api/centres/nous`). Then move the `?oferta=` deep-link handling, reading it from its own `new URLSearchParams(location.search).get('oferta')`. With the deep link here, a successful retry still honours an email-alert link. The catch sets state to 'error'.
- The new init() keeps its current ordering guarantee: loggedIn and searchMode are set BEFORE state becomes 'ready', so nothing flickers.
  1. Start the catalogue request first, so it runs in parallel with auth exactly as the current Promise.all does: `const catalogRequest = fetch(API_BASE + '/api/ofertes');`
  2. Immediately attach a no-op `.catch(() => {})` to catalogRequest, as a separate statement, with a one-line comment. The comment says the rejection is handled inside loadCatalog, and this only prevents an unhandled-rejection console warning while /api/auth/me is still pending.
  3. Keep the existing try/catch. Inside it: await the auth fetch with its existing `.catch(() => ({ ok: false }))`, then keep, unchanged, the loggedIn assignment, the `mode` URL-param handling (including fetchFpoEspecs) and the three logged-in loaders.
  4. Finally `await this.loadCatalog(catalogRequest)`.
  5. The catch still sets `this.state = 'error'`.
- Behaviour change to record in the SUMMARY: in the current code, a network rejection on /api/ofertes aborted the whole Promise.all before loggedIn and the mode were applied. Now they are applied either way, which is what a later retry needs.
- init() must not register any new $watch, event listener or interval. loadCatalog must not touch auth, favourites, watches, FPO data or searchMode.

index.html markup (L2453-2454). Restructure the error block:
- The outer div keeps `x-show="state === 'error'"`, `class="error-state"`, `role="alert"` and both x-transition attributes. REMOVE the x-text from that outer div.
- Inside it, add `<p x-text="t('index.error')"></p>`, followed by `<button type="button" class="save-alert-btn" @click="loadCatalog()" x-text="t('index.error.retry')"></button>`.
- The call MUST include the empty parentheses. A bare method reference would make Alpine pass the click event as pendingRequest.
- `.save-alert-btn` is chosen because it is the existing outlined secondary button: transparent background, so it sits on the --alert-bg tint, and 13px, matching the filter-bar actions. `.btn-doc` is a white 12px chip meant for documents.
- CSS: after the `.error-state` rule (L701-708), add `.error-state .save-alert-btn { margin-top: 10px; }`. Add no other CSS and no new colours.

C. Verify, then commit ONCE. Run this task's verify command. Then run Task 1's and Task 2's verify commands again: all three must print their OK line. Then, from the git root `/Users/rogermasellas/AI/Cercador Graus`:
- Stage ONLY `fp-cercador/frontend/index.html` and `fp-cercador/frontend/i18n.js`, by explicit path. Never use `git add -A` or `git add .`.
- Commit with exactly this subject and no body: `fix(index): accessibilitat de teclat, favorits tàctils, landmark main, paginació, plurals i reintent d'error`
- Owner rule, which overrides any harness default: add no AI attribution, trailer or generated-by line of any kind.
- Do not push or deploy.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador/frontend" && node --check i18n.js && for k in index.itinerari.fpo_disponible_one index.centres.count_one index.centres.more_one index.btn.exportTitle_one index.error.retry; do test "$(rg -c -F "'$k'" i18n.js)" = 2 || { echo "missing $k"; exit 1; }; done && ! rg -q "^\s+'index\.error': [\"']⚠" i18n.js && rg -q "fpo_disponible_one" index.html && rg -q "centres\.count_one" index.html && rg -q "centres\.more_one" index.html && rg -q "exportTitle_one" index.html && test "$(rg -c '^\s+async loadCatalog\(' index.html)" = 1 && rg -q 'this\.loadCatalog\(catalogRequest\)' index.html && rg -q '@click="loadCatalog\(\)"' index.html && rg -q "t\('index\.error\.retry'\)" index.html && ! rg -q 'role="alert" x-text=' index.html && node -e 'const h=require("fs").readFileSync("index.html","utf8");const re=/<script(?![^>]*\ssrc=)[^>]*>([\s\S]*?)<\/script>/g;let m,n=0;while((m=re.exec(h))){new Function(m[1]);n++}if(n<1)throw new Error("no inline script");console.log("inline scripts parse:",n)' && echo TASK3-OK</automated>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && SUBJ="$(git log -1 --format=%s)" && BODY="$(git log -1 --format=%B)" && FILES="$(git show --name-only --format= HEAD)" && test "$SUBJ" = "fix(index): accessibilitat de teclat, favorits tàctils, landmark main, paginació, plurals i reintent d'error" && ! printf '%s' "$BODY" | rg -qi 'co-authored-by|generated with|claude' && test "$(printf '%s\n' "$FILES" | rg -c .)" = 2 && printf '%s\n' "$FILES" | rg -q '^fp-cercador/frontend/index\.html$' && printf '%s\n' "$FILES" | rg -q '^fp-cercador/frontend/i18n\.js$' && echo COMMIT-OK</automated>
  </verify>
  <done>
i18n.js passes node --check and has all five new keys in both CA and ES. index.error has no emoji and tells the user to retry. The four n-count call sites pick the _one key when n === 1. loadCatalog exists once: init() calls it with the pre-started request, and the retry button calls it with no argument. The alert container holds a message paragraph and a retry button. TASK1-OK, TASK2-OK, TASK3-OK and COMMIT-OK all print. HEAD is one commit with the exact subject, exactly the two files, and no attribution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → browser | /api/ofertes, /api/fpo/* and /api/auth/me responses are rendered by Alpine. The fields row.ficha_url and row.codigo feed window.open |
| i18n dictionary → DOM | New keys are rendered through x-text (textContent), never through data-i18n innerHTML |
| user input → network | The retry button issues GET /api/ofertes on demand |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260924-01 | Tampering / Elevation | activateRow window.open targets | low | accept | The URLs and targets are byte-identical to the current inline expression (Task 1 A). The ficha-redirect URL keeps encodeURIComponent(row.codigo). No new sink is introduced; noopener hardening is out of the approved scope |
| T-260924-02 | Information disclosure / Injection | new i18n strings (error, retry, _one keys) | low | mitigate | They are rendered only via x-text / t() (textContent). None is referenced by a data-i18n attribute, which would use innerHTML. The verify gate checks that the error message sits in a `<p x-text>` |
| T-260924-03 | Denial of service | retry button → /api/ofertes | low | mitigate | loadCatalog sets state='loading' first, which hides the error block and its button through x-show. So at most one user-triggered request is in flight per click, and there are no automatic retry loops or timers |
| T-260924-04 | Spoofing / Elevation | gated actions reachable by keyboard | low | mitigate | The keyboard path calls the same activateRow method as the click path, so the loggedIn check and the showCentresModal() gate apply identically. The hearts keep toggleFavorite / the FPO loggedIn guard |
</threat_model>

<verification>
- All three task verify commands print TASK1-OK, TASK2-OK and TASK3-OK, and the commit check prints COMMIT-OK.
- `node --check fp-cercador/frontend/i18n.js` passes, and the single inline script in index.html parses (new Function), so no unbalanced braces or missing commas remain in the component object.
- No duplicate method names: activateRow, rowIsActionable and loadCatalog each have exactly one definition line.
- Browser verification (keyboard Tab/Enter/Space on rows, touch visibility of hearts, pagination ellipsis on a page > 4, error retry with the API down) is done by the orchestrator after execution and is not part of this plan.
</verification>

<success_criteria>
- All 7 approved scope items are implemented in index.html and i18n.js only, with no new colours or dependencies.
- Click behaviour of result and FPO rows is unchanged, and keyboard users get the same behaviour.
- One commit with the exact Catalan conventional subject, exactly two files, no attribution, not pushed.
</success_criteria>

<output>
Create `.planning/quick/260924-09g-harden-index-html-keyboard-rows-fav-a11y/260924-09g-SUMMARY.md` when done. Record: the plural-audit table (fixed vs untouched keys and why), the `.self` choice over per-control `.stop`, the pagination key change, and the init()/loadCatalog behaviour note (loggedIn and mode are now applied even when /api/ofertes rejects).
</output>
