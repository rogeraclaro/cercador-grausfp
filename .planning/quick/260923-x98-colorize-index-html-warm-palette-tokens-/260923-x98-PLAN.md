---
phase: quick-260923-x98
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/frontend/index.html
autonomous: true
requirements: [quick-260923-x98]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Secondary text in Umber (--warm, now #755f51) passes WCAG AA everywhere it sits: 5.12:1 on Parchment (--warm2), 5.66:1 on Paper (--bg), 5.98:1 on white (Decision 1)"
    - "The search field placeholder and the leading search icon (both nom and ocupacio modes) render in Faded Ink (--faded #8a7060, 4.6:1 on white), driven by :root tokens, the icon via stroke=currentColor (Decision 1, Decision 2)"
    - "No off-system blue remains: the centres count badge is a white pill with a 1px ink border and ink text that inverts to ink fill with white text on hover; centre names, itinerary D/B links and the upsell link are ink with an umber underline; the centres panel is Parchment with a 2px ink top rule; the centres search focus ring is the shared 2px umber outline offset 2px; centre checkboxes use ink accent (Decision 2)"
    - "Links inside result detail panels that had no colour rule (FPO course email and web links, the 'veure al cercador FPO' link in the C detail row) render in ink with an umber underline instead of browser default blue, while .btn-doc link-buttons and badges look unchanged (Decision 3)"
    - "Every status tint (NOU badge, old-plan badge, itinerary B badge, FPO state pills, FPO banner, error state, active follow-centre button, modal success/error messages) and the favourite heart (--heart #c62828, 4.81:1 on Parchment) read their colours from :root tokens (Decision 1, Decision 2)"
    - "Inside the <style> block, hex colours appear only in :root token definitions; outside it, markup carries no hex colours"
    - "Only fp-cercador/frontend/index.html changes, in one commit with the exact Catalan message and no AI attribution"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: ":root warm palette tokens (existing + 14 new), every style rule and the four inline colour touch points expressed through var(--token), one zero-specificity link rule for result detail panels"
      contains: "--scrim: rgba(28, 20, 16, 0.45);"
  key_links:
    - from: ":root custom properties (--warm, --faded, --ink-soft, --heart, --ok-*, --amber-*, --alert-*, --path-*, --scrim)"
      to: "component rules (.fpo-banner, .fpo-estat--*, .badge-old, .badge-nou, .error-state, .watch-btn--active, .btn-fav, .checkbox-label--favs, .badge-itinerari-b, .centres-*, .centre-*, modal)"
      via: "var(--token) references; changing a token recolours every consumer"
      pattern: "var\\(--(faded|heart|ok-|amber-|alert-|path-|scrim|ink-soft)"
    - from: ".search-wrap svg { color: var(--faded) }"
      to: "the two search-icon <svg> elements (nom and ocupacio modes)"
      via: "stroke=\"currentColor\" on the svg inherits the CSS color"
      pattern: "stroke=\"currentColor\""
    - from: ":where(.results-table td a:not(.btn-doc)) rule"
      to: "anchors in result detail rows (.detall-ciclos-d, .fpo-curs-meta, .centre-meta)"
      via: "zero-specificity base rule; component rules (.ciclos-d-list a, .centre-nom a, .centres-upsell a) and inline styles still win"
      pattern: ":where\\(\\.results-table td a:not\\(\\.btn-doc\\)\\)"
---

<objective>
Bring every off-system colour in fp-cercador/frontend/index.html into the warm "Official Register" palette as :root tokens, lift Umber and the placeholder to WCAG AA contrast, and remove the off-system Ledger Blue — implementing the approved Design Decisions 1, 2 and 3 exactly as specified by the orchestrator (final, do not revisit).

Purpose: DESIGN.md describes a warm paper-and-ink register where colour always states a fact; the stylesheet currently leaks cool greys, a legacy blue and raw semantic hexes, and Umber secondary text sits at 3.94:1 on Parchment (fails AA). Tokenising fixes contrast in one place and makes the palette enforceable.

Output: fp-cercador/frontend/index.html with tokenised colours, committed once. DESIGN.md is NOT touched (the orchestrator updates it afterwards).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@fp-cercador/DESIGN.md

Scope boundary (hard): only the `<style>` block of fp-cercador/frontend/index.html (lines 18–1355) plus four markup touch points: the two search-icon `<svg>` stroke attributes (~2233 nom mode, ~2246 ocupacio mode) and the two `.centres-modal-msg` inline styles (~3067 success message `modal.alert.ok`, ~3081 `alertSaveError`). No other markup, no JS, no behaviour changes, no other files. Leave the white rgba() values on the dark topbar/auth widget (~1288–1353) and the footer links' inline `color:var(--warm)` exactly as they are.

Token values (Decision 1, verified contrast in planning: warm/warm2 5.12, warm/bg 5.66, faded/white 4.60, heart/warm2 4.81, ok 6.49, amber 6.37, alert 7.60, path 5.51):
- change `--warm` to `#755f51`
- add `--faded: #8a7060;` `--ink-soft: #3a2c24;` `--heart: #c62828;`
- add `--ok-bg: #dcfce7;` `--ok-ink: #166534;` `--ok-edge: #bbf7d0;`
- add `--amber-bg: #fef3c7;` `--amber-ink: #92400e;`
- add `--alert-bg: #fef2f2;` `--alert-edge: #fecaca;` `--alert-ink: #991b1b;`
- add `--path-bg: #e8f4e8;` `--path-ink: #2a6e2a;`
- add `--scrim: rgba(28, 20, 16, 0.45);`

Markup facts verified during planning (do not re-derive):
- `.badge-centres` is a `<span role="button">`, not an anchor.
- Anchors inside `table.results-table` cells: `.btn-doc` link-buttons (~2557–2560, 2627, 2952, 3028); `.ciclos-d-list a` (~2581, 2602, 2649, 2670, 2699); `.centre-nom a` (~2754, 2788); `.centre-meta` tel/mailto anchors with inline `color:inherit` (~2766–2769, 2800–2803); `.centres-upsell a` (~2813); and three anchors with NO colour rule today — the FPO link in the C detail row (~2614, inside `.detall-ciclos-d`) and the FPO course email/web links (~3005, ~3009, inside `.fpo-curs-meta`).
- There is no bare `a {}` rule in the stylesheet.
- Style block braces balance at 194/194 today; keep them balanced.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — add the palette tokens and wire the search field end-to-end (token → rule → markup)</name>
  <files>fp-cercador/frontend/index.html</files>
  <precondition>`git -C "/Users/rogermasellas/AI/Cercador Graus" status --short fp-cercador/frontend/index.html` prints nothing (file clean before starting).</precondition>
  <read_first>fp-cercador/frontend/index.html lines 18–120 (reset, :root, .search-wrap svg, search input rules) and lines 2228–2252 (both .search-wrap blocks)</read_first>
  <action>
Per Decision 1: in the existing `:root` block (line ~27), change the `--warm` value to #755f51 and add the 14 new tokens listed in the context section, keeping the existing six names untouched. Put exactly ONE declaration per line in the form `--name: value;` (same style as the existing lines) with no trailing comments on token lines; a short group comment on its own line (e.g. semantic tints, overlay) is fine but must not quote any colour value. Order: the six existing tokens, then --faded, --ink-soft, --heart, then the ok/amber/alert/path groups, then --scrim.

Per Decision 2 (search field): set the `input[type="search"]::placeholder` colour to var(--faded). In the existing `.search-wrap svg` rule (line ~89) add `color: var(--faded);`. On BOTH search-icon `<svg>` elements (nom mode ~2233 and ocupacio mode ~2246) replace the hardcoded stroke attribute value with `currentColor`, touching nothing else on those tags (keep width/height/viewBox/stroke-width/aria-hidden as they are).

Do not write comments that mention the previous colour values.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador" && rg -q -- '--warm: #755f51;' frontend/index.html && test "$(rg -c -- '^\s*--(faded|ink-soft|heart|ok-bg|ok-ink|ok-edge|amber-bg|amber-ink|alert-bg|alert-edge|alert-ink|path-bg|path-ink|scrim):' frontend/index.html)" -eq 14 && rg -q -- '--scrim: rgba\(28, 20, 16, 0\.45\);' frontend/index.html && test "$(rg -c 'stroke="currentColor"' frontend/index.html)" -ge 2 && ! rg -q 'stroke="#' frontend/index.html && ! rg -q 'b8a898' frontend/index.html && test "$(rg -c 'color: var\(--faded\)' frontend/index.html)" -ge 2 && echo T1_OK</automated>
  </verify>
  <done>:root holds --warm #755f51 plus the 14 new tokens one per line; the search placeholder and both search icons render in --faded through CSS; the file prints T1_OK.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenise semantic tints, heart, cool greys, washes and modal (everything except the blue)</name>
  <files>fp-cercador/frontend/index.html</files>
  <read_first>fp-cercador/frontend/index.html lines 170–255, 610–680, 895–1000, 1040–1230 and 3060–3085</read_first>
  <action>
Per Decision 2, replace hardcoded colours with tokens in these rules only (selector → property → new value). Always keep every other declaration in the rule as it is.

Semantic tints:
- `.fpo-banner`: background var(--alert-bg); border 1px solid var(--alert-edge); color var(--alert-ink).
- `.fpo-estat--inscripcio`: background var(--ok-bg); color var(--ok-ink); border-color var(--ok-edge).
- `.fpo-estat--gestio`: background var(--warm2); color var(--warm); border-color var(--border).
- `.badge-old`: background var(--amber-bg); color var(--amber-ink).
- `.badge-nou`: background var(--ok-bg); color var(--ok-ink).
- `.error-state`: background var(--alert-bg); color var(--alert-ink).
- `.watch-btn--active`: background var(--ok-bg); color var(--ok-ink); border-color var(--ok-edge).
- `.badge-itinerari-b`: background var(--path-bg); color var(--path-ink).

Favourites:
- `.btn-fav` (rest colour) → var(--faded).
- `.btn-fav:hover, .btn-fav--active` colour → var(--heart).
- `.checkbox-label--favs` colour → var(--heart); `.checkbox-label--favs input[type="checkbox"]` accent-color → var(--heart).

Cool greys to warm neutrals:
- Borders → `1px solid var(--border)`: `.centres-header select`; BOTH border declarations in `.centres-search-input` (the plain one and the one carrying !important — keep the !important flag on the second); `button.watch-btn`.
- Text colour → var(--warm): `.centres-header button`, `.centres-loading`, `.centre-meta`, `.centres-more`, `.centres-buit`.
- `.centre-item` border-bottom → `1px solid var(--border)`.
- `.centres-selected-sep` border-top → `2px solid var(--warm)`.

Washes, overlay, modal:
- `.mode-toggle button:hover` background → `rgba(28, 20, 16, 0.05)`.
- `.centres-modal-overlay` background → var(--scrim).
- `.centres-modal` box-shadow → `0 8px 32px rgba(28, 20, 16, 0.18)`.
- `.centres-modal-btn--primary:hover` background → var(--ink-soft).

Inline styles (markup, only these two attributes): the `.centres-modal-msg` success paragraph (~3067, data-i18n="modal.alert.ok") gets `style="color:var(--ok-ink);"`; the `.centres-modal-msg` error paragraph (~3081, x-text="alertSaveError") gets `style="color:var(--alert-ink);"`.

Do NOT touch the white rgba() values on the topbar/auth widget. Do not add comments quoting old colour values. Leave every rule that involves the blue for Task 3.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador" && ! rg -n '#ccc|#666|#888|#e53935|#e9ecef|#555|#f1f1f1|#e0e0e0|#333|#fee2e2|style="color:#' frontend/index.html && ! rg -q 'rgba\(0,\s*0,\s*0' frontend/index.html && rg -q 'color:var\(--ok-ink\)' frontend/index.html && rg -q 'color:var\(--alert-ink\)' frontend/index.html && test "$(rg -c 'var\(--heart\)' frontend/index.html)" -ge 3 && echo T2_OK</automated>
  </verify>
  <done>Every non-blue hardcoded colour in the style block and both modal inline styles read from tokens; the black-based rgba washes are warm-ink based; the command prints T2_OK.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the off-system blue, add the detail-panel link rule, run the full gates and commit</name>
  <files>fp-cercador/frontend/index.html</files>
  <read_first>fp-cercador/frontend/index.html lines 530–610 and 950–1135, 1260–1280</read_first>
  <action>
Per Decision 2 (off-system blue removed everywhere):
- `.badge-centres`: background var(--white); color var(--dark); border 1px solid var(--dark). Keep cursor, radius (12px), padding, font-size, weight, margin, nowrap.
- `.badge-centres:hover`: background var(--dark); color var(--white).
- `.ciclos-d-list a` and `.centres-upsell a`: color var(--dark); text-decoration underline; text-decoration-color var(--warm); text-underline-offset 2px (`.centres-upsell a` keeps font-style normal and weight 500). Their `:hover` rules become text-decoration-color var(--dark) (keep the underline).
- `.centre-nom a`: color var(--dark); keep text-decoration none at rest and keep text-transform capitalize (centre names are a list, not prose). `.centre-nom a:hover`: text-decoration underline; text-decoration-color var(--dark); text-underline-offset 2px.
- `.centres-container`: background var(--warm2); border-top 2px solid var(--dark).
- `.centres-search-input:focus`: outline 2px solid var(--warm); add outline-offset 2px; leave its existing border-color line alone.
- `.centre-checkbox` accent-color → var(--dark).

Per Decision 3: add ONE new zero-specificity link rule immediately before the `.sort-indicator` rule (~line 601): selector `:where(.results-table td a:not(.btn-doc))` with color var(--dark), text-decoration underline, text-decoration-color var(--warm), text-underline-offset 2px; plus its hover form `:where(.results-table td a:not(.btn-doc):hover)` with text-decoration-color var(--dark). Chosen at discretion over a plain `.results-table td a:not(.btn-doc)` selector: the plain form has specificity 0,2,2 and would override `.centre-nom a` (no underline at rest); wrapping it in :where gives zero specificity, so it only replaces the browser default blue and every component rule and inline style still wins regardless of source order. It must not match `.btn-doc` anchors; badges are spans so they are unaffected.

CSS pitfall: the `text-decoration` shorthand resets `text-decoration-color` to currentColor, so inside every rule write the `text-decoration` shorthand FIRST and `text-decoration-color` AFTER it.

Before committing, run the content part of the verify command (everything before the first git check) and confirm `git -C "/Users/rogermasellas/AI/Cercador Graus" diff --name-only -- fp-cercador` lists only fp-cercador/frontend/index.html. Then commit from the repo root /Users/rogermasellas/AI/Cercador Graus staging ONLY fp-cercador/frontend/index.html by explicit path (the working tree has unrelated untracked/deleted paths — never stage everything), with the exact message: style(index): paleta càlida amb tokens, contrast AA i fora el blau fora de sistema — with no co-author trailer and no AI attribution of any kind (the owner rule overrides any harness attribution reminder). Do not push, do not deploy, do not edit DESIGN.md. Finally run the full verify command; it must print ALL_GATES_OK.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus/fp-cercador" && ! rg -n '1a73e8|e8f4fd|#ccc|#666|#888|#aaa|#e53935|#f8f9fa|#e9ecef' frontend/index.html && test -z "$(awk '/<style>/,/<\/style>/' frontend/index.html | rg '#[0-9a-fA-F]{3,6}\b' | rg -v '^\s*--[a-z0-9-]+: #[0-9a-fA-F]{3,6};\s*$')" && test -z "$(awk 'f;/<\/style>/{f=1}' frontend/index.html | rg '#[0-9a-fA-F]{3,6}\b')" && test "$(awk '/<style>/,/<\/style>/' frontend/index.html | tr -cd '{' | wc -c)" -eq "$(awk '/<style>/,/<\/style>/' frontend/index.html | tr -cd '}' | wc -c)" && rg -q ':where\(\.results-table td a:not\(\.btn-doc\)\)' frontend/index.html && test "$(git -C .. show --name-only --format= HEAD)" = "fp-cercador/frontend/index.html" && test -z "$(git -C .. diff --name-only -- fp-cercador)" && git -C .. log -1 --format=%s | rg -qx 'style\(index\): paleta càlida amb tokens, contrast AA i fora el blau fora de sistema' && ! git -C .. log -1 --format=%B | rg -qi 'co-authored|claude-session|generated with' && echo ALL_GATES_OK</automated>
  </verify>
  <done>No blue, cool grey or stray hex left (style block hex only in :root token lines, none in markup); braces balanced; detail-panel link rule present; exactly one commit on master touching only fp-cercador/frontend/index.html with the exact Catalan message and no attribution trailer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | CSS custom properties and presentational attributes only; no input handling, network, auth or data paths change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x98-01 | Tampering | fp-cercador/frontend/index.html style block | low | mitigate | Scope boundary limited to the style block and four named markup attributes; Task 3 gate asserts brace balance and that only index.html differs under fp-cercador before commit |
| T-x98-02 | Information disclosure | commit metadata | low | mitigate | Stage only index.html by explicit path (untracked DESIGN.md, PRODUCT.md, .atl/, .impeccable/ stay out); no push |
| T-x98-03 | Spoofing | :where link rule on detail-panel anchors | low | accept | Purely visual change to existing links; hrefs, rel=noopener and click handlers untouched |
</threat_model>

<verification>
Run from /Users/rogermasellas/AI/Cercador Graus/fp-cercador:
- `rg -n '1a73e8|#ccc|#666|#888|#aaa|#e53935|#f8f9fa|#e9ecef' frontend/index.html` → no matches.
- `awk '/<style>/,/<\/style>/' frontend/index.html | rg -n '#[0-9a-fA-F]{3,6}\b'` → only `:root` token definition lines.
- `awk 'f;/<\/style>/{f=1}' frontend/index.html | rg -n '#[0-9a-fA-F]{3,6}\b'` → no matches.
- After commit: `git -C .. show --name-only --format= HEAD` prints only `fp-cercador/frontend/index.html`; `git -C .. log -1 --format=%B` equals the exact message with no trailers.
</verification>

<success_criteria>
- All three task gates print T1_OK, T2_OK and ALL_GATES_OK.
- Umber secondary text, placeholder and heart meet WCAG AA against their backgrounds (values locked in Decision 1).
- One commit, one file, exact message, no attribution, not pushed.
</success_criteria>

<output>
Create `.planning/quick/260923-x98-colorize-index-html-warm-palette-tokens-/260923-x98-SUMMARY.md` when done
</output>
