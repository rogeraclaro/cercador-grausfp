---
status: complete
---
# Clarify empty states (index.html)

Done inline (no subagents). Main table, occupation and FPO empty states now say what happened and what to do next; the count line no longer repeats the empty-row message. New key `index.table.empty.search` interpolates the query. The CA copy explains that catalogue names are Spanish. Verified in a real browser, CA and ES, four states each, no console errors.

Open product gap (not a copy issue): occupation search for "electricista" returns nothing even though it is Spanish. See improve plan 050 (CA<->ES synonyms) and the occupation index coverage.
