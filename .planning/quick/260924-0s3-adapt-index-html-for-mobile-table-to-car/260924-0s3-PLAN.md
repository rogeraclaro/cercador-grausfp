---
phase: quick-260924-0s3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - fp-cercador/frontend/index.html
  - .planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py
autonomous: true
requirements: [quick-260924-0s3]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "At 320, 390 and 768px the main results table (name mode, and the occupation-mode 'taula' view, which reuses #results-table) shows each record as a card. The name is on the first line at full width. Below it, one wrapping 13px Umber meta line shows: the grade as a 1px Ink-outline tag (first), then 'Nivell N' / 'Nivel N', then the code in Geist Mono, then the family, separated by ' · '. Cells that render '—' or are empty are hidden. .table-wrap has no horizontal scroll (scope item 1)"
    - "The FPO-mode table gets the same card treatment. Level, hours and active-course count carry a label prefix taken from the existing fpo.col.nivell / fpo.col.hores / fpo.col.ncursos keys, because a bare '320 · 3' means nothing once the header row is hidden (scope item 1)"
    - "On both tables the thead is still in the DOM but visually hidden (sr-only clip technique, not display:none). A keyboard-focused card shows the shared 2px solid var(--warm) outline. The hover, focus and favourite tint (var(--warm2)) covers the whole card. Expanded detail rows (td[colspan]) are full-width blocks with 16px padding (scope item 1)"
    - "At 320, 390 and 768px, in CA and ES, logged out and logged in, the top bar never clips or overflows: the greeting text is hidden, auth buttons stay on one line, and CA/ES stays right-aligned, wrapping to a second line when it cannot fit (scope item 2)"
    - "At 768px and below, the three mode buttons stack in a column, fill the width, keep a single-line label at 320px in CA and ES, and are at least 44px tall (scope item 3)"
    - "Under (pointer: coarse): .lang-btn, .auth-btn, select, .save-alert-btn, .clear-btn, .mode-toggle button, footer links and checkbox/radio labels are at least 44px tall. The heart (.btn-fav) has a 44x44 hit area and .badge-centres / .badge-itinerari-b[role=button] have 44px-tall hit areas, all through ::after. The icon and badge boxes keep their visual size (scope item 4)"
    - "The mobile rule on html, body and [x-data] (max-width 100vw plus overflow-x hidden) is removed exactly when the harness overflow gate shows no horizontal overflow at 320/390/768. If the gate fails, the rule stays and SUMMARY.md lists the offending elements (scope item 2)"
    - "Desktop (wider than 768px, fine pointer) is pixel-identical to base commit 9b3edf2 at 1280px and 800px in name mode and at 1280px in FPO mode"
    - "Exactly one commit touches only fp-cercador/frontend/index.html, with the exact Catalan conventional subject and no attribution trailer. i18n.js is not modified, because every label key already exists"
  artifacts:
    - path: "fp-cercador/frontend/index.html"
      provides: "End-of-stylesheet @media (max-width: 768px) block (cards, top bar, mode toggle) and @media (pointer: coarse) block (hit areas); :data-label and cell-empty bindings on meta cells; results-table--fpo hook plus col-area/col-hores/col-ncursos classes on the FPO table"
      contains: "@media (pointer: coarse)"
    - path: ".planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py"
      provides: "Headless-Chrome (Playwright, channel=chrome) layout harness with mocked /api/*: card, FPO, top bar, toggle, touch, overflow-gate and desktop-parity checks"
      contains: "MOBILE_CHECK_OK"
  key_links:
    - from: "td.col-niv (main) and FPO niv/hores/ncursos cells: :data-label=\"t('<existing key>')\""
      to: "CSS ::before content built from attr(data-label) inside the end-of-sheet 768px block"
      via: "existing i18n keys index.col.niv, fpo.col.nivell, fpo.col.hores, fpo.col.ncursos (setLang reloads the page, so a one-time binding is enough)"
      pattern: ":data-label=\"t\\('(index\\.col\\.niv|fpo\\.col\\.(nivell|hores|ncursos))'\\)\""
    - from: "td :class cell-empty bindings (row.codigo == null, row.nivel == null, !esp.nivell, !esp.hores)"
      to: "card rule that hides td.cell-empty and td:empty"
      via: "class binding, because x-text renders '—', so :empty alone cannot catch it"
      pattern: "cell-empty"
    - from: "new @media blocks placed immediately before </style>"
      to: "base rules for #auth-widget, .auth-btn, .lang-selector, .lang-btn, .btn-fav, .badge-centres, .detall-inner, which are declared AFTER the existing 768px block (~line 797)"
      via: "source order: equal-specificity overrides only win if they come later in the sheet"
      pattern: "@media \\(pointer: coarse\\)"
---

<objective>
Adapt fp-cercador/frontend/index.html (Alpine.js 3.15, vanilla CSS, no build step) for phones. The reference width is 390px, and the layout must also hold at 320px and 768px. Work CSS-first and change markup only where CSS alone cannot do the job. Implement exactly the 4 approved scope items: result rows as cards, the top bar, stacked mode buttons, and 44px touch targets. Add nothing else.

Purpose: at 390px the key facts (grade and level) are off-screen in a 695px-wide table with no scroll affordance. The top bar clips the ES button, the mode labels wrap to 3 lines, and many touch targets are 15–33px.
Output: an edited index.html in one commit, plus an uncommitted verification harness in this quick-task directory.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@fp-cercador/DESIGN.md
@fp-cercador/frontend/index.html
@fp-cercador/frontend/auth.js

Facts verified during planning (HEAD = base = 9b3edf2; line numbers are pre-edit and approximate):
- :root (~27–50) defines colour tokens only (--dark, --warm, --warm2, --border, --bg, --white, --faded, --ink-soft, semantic --ok-*/--amber-*/--alert-*/--path-*, --heart, --scrim). There are NO radius custom properties, so the tag radius is the literal 3px, the same value .badge-old and .badge-nou use. Colours must be var(--token) only.
- The existing @media (max-width: 768px) block runs from ~797 to ~876. It opens with the rule on html, body and [x-data] (max-width 100vw; overflow-x hidden, ~799–804) and holds the table rules ~851–865 (.table-wrap overflow-x auto, .results-table min-width 560px, th/td.col-nom padding-left 12px and min-width 180px).
- SOURCE-ORDER TRAP: these base rules are declared AFTER that block, so any same-specificity override placed inside it silently loses: .detall-inner (~885), .btn-fav (~958), .badge-centres (~1012), .badge-itinerari-b (~1043), #auth-widget (~1344), .auth-btn (~1356), .lang-selector (~1388), .lang-btn (~1395). Put ALL new rules in new blocks immediately before </style> (~1419).
- Main table: table.results-table#results-table (~2537–2643). Every record is its own <tbody> (x-for over tbody). It holds the main <tr :data-row-id> with td.col-nom (heart button, name, badges, optional .badge-centres[role=button], .badge-itinerari-b[role=button]), td.col-codi (x-text row.codigo ?? '—'), td.col-fam, td.col-grau, td.col-niv (x-text row.nivel ?? '—'). It is followed by x-show detail rows (td colspan=5 .detall-certificat) and an x-if tr.centres-panel (td colspan=5, padding 0 !important). x-show hides rows with an inline display:none, so never put !important on a tr display rule.
- FPO table: second table.results-table (~3004–3046), main tr.row-link with td.col-nom, td.col-codi (esp.codi), td.col-fam (may render ''), unclassed area td (may render ''), td.col-niv (esp.nivell || '—'), unclassed hours td (esp.hores ? rounded : '—'), unclassed nCursos td; detail row td colspan=7 .detall-certificat.
- Data: nivel is 1/2/3 for grades A–D and null for all 36 grade-E records; 231 records have codigo null. So "Nivell 2" reads well and the hide-empty rule is exercised by real data.
- Existing i18n keys (no new keys needed): index.col.niv (CA 'Nivell' / ES 'Nivel'), fpo.col.nivell ('Nivell'/'Nivel'), fpo.col.hores ('Hores'/'Horas'), fpo.col.ncursos ('Cursos actius'/'Cursos activos'). setLang() reloads the page.
- Top bar markup (~2295–2304): a.topbar-logo, #auth-widget (filled by auth.js: span.auth-greeting plus a.auth-btn 'El meu perfil' plus button.auth-btn--logout 'Sortir' when logged in; a.auth-btn 'Entra' plus a.auth-btn--primary "Registra't gratis" when logged out), .lang-selector with two .lang-btn. Do not edit auth.js.
- Mode toggle markup (~2313–2320): div.mode-toggle with 3 buttons. The footer (~3218–3229) uses inline styles on its 4 <a> links; there is no display property inline, so a stylesheet display rule applies.
- Tooling verified on this machine: python3 has playwright (sync API) and PIL. Bundled Playwright browsers are NOT installed, but chromium.launch(channel="chrome") drives the installed Google Chrome headless. A context with has_touch=True makes (pointer: coarse) and (hover: none) match. Do not set is_mobile (it breaks innerWidth). getComputedStyle(el, '::before').content returns resolved attr() text. There is no GNU `timeout` binary on macOS. Owner shell rule: use rg/fd/bat/eza, not grep/find/cat/ls/sed.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — the harness plus main-table cards, proven end-to-end in headless Chrome at 390px, with desktop parity</name>
  <files>.planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py, fp-cercador/frontend/index.html</files>
  <read_first>fp-cercador/frontend/index.html lines ~490–615 (table base rules), ~797–876 (existing 768px block), ~1415–1420 (end of style), ~1541–1612 (init/loadCatalog API usage), ~2537–2645 (main table markup); fp-cercador/frontend/i18n.js lines 93–97 and 203–209</read_first>
  <action>
Part A — create the harness mobile_check.py in this quick-task directory. It is Python 3, uses only playwright.sync_api and PIL (both already installed), and must NOT install anything or run playwright install. Launch with chromium.launch(channel="chrome"). Resolve paths from the file location: git root = three parents up, frontend dir = git root/fp-cercador/frontend, data file = git root/fp-cercador/backend/data/ofertes.json.
- Serving: load http://grausfp.test/index.html (plus ?mode=fpo for FPO). The hostname is not 'localhost', so API_BASE is '' and every /api call is same-origin. context.route on all requests:
  - For host grausfp.test, paths under /api/ get the mocks below.
  - /index.html comes from the working tree by default, or from the output of git show 9b3edf2:fp-cercador/frontend/index.html when rendering the parity baseline.
  - Any other path is served from the frontend dir with a mimetypes content type. Reject paths that resolve outside that dir with 404.
  - fonts.googleapis.com and fonts.gstatic.com are continued in layout checks and aborted in parity renders.
  - Every other host is aborted.
- Mocks:
  - /api/ofertes returns the raw bytes of ofertes.json.
  - /api/auth/me returns 200 with email test@local.dev and is_admin false for the 'user' scenario, and 401 for 'guest'.
  - /api/centres/count returns an object mapping str(id) to 3 for every record id, so .badge-centres renders on every row.
  - /api/centres/nous returns an empty object.
  - /api/fpo/especialitats returns an object whose especialitats list has 3 fixtures (warning null). Each fixture has: codi, titol{ca,es}, familia{codi,desc{ca,es}}, area{codi,desc{ca,es}}, nivell, hores, nCursos, esCertProf, and empty comarques/municipis/estats/modalitats lists.
    - Fixture 1 is normal: nivell 2, hores 320, nCursos 3, esCertProf true.
    - Fixture 2 has nivell null, hores 0 and no area (it exercises the hidden cells).
    - Fixture 3 has a ~100-character title, which keeps the overflow check realistic.
  - Any other /api/* returns 200 with an empty JSON list.
- Loader open(width, lang, auth, touch=False, mode=None, ref='work', fonts=True):
  - Create a new context with viewport width x 900 and has_touch=touch. Add an init script that sets localStorage lang.
  - Wait until Alpine.$data(document.querySelector('[x-data]')).state === 'ready' and '#auth-widget .auth-btn' exists. In FPO mode, also wait until fpoLoaded is true and FPO rows exist.
  - Wait for document.fonts.ready, raced against a 5s timeout.
  - Record every pageerror and every console message containing 'Alpine' as failures. If the same error also appears when rendering ref 9b3edf2, fix the mock shape, not the page.
- Check groups, selected by --only a,b,c (default: all). Each failure appends a readable message. Exit 1 if any failure exists; otherwise print MOBILE_CHECK_OK. Every tolerance is 1px.
  - desktop: fine pointer, CA, user. Render full-page screenshots (animations disabled) of ref 9b3edf2 and of the working tree at 1280 and at 800 in name mode, and at 1280 in FPO mode. Sizes must match and PIL ImageChops.difference(...).getbbox() must be None. On mismatch, save both PNGs into a tempfile.mkdtemp() dir and print its path.
  - cards: at 390 in CA and ES and at 320 and 768 in CA (user, fine):
    - #results-table's .table-wrap has scrollWidth <= clientWidth.
    - The thead computed position is absolute and its rect height is <= 1.
    - The first tr[data-row-id] computes display flex, padding-top 14px, padding-left 16px and border-bottom-width 1px.
    - Its td.col-nom width is at least the row clientWidth minus 32, i.e. full width.
    - td.col-grau has border-top 1px solid, border-top-color equal to the computed colour of --dark, and a rect.left smaller than every other visible meta cell's rect.left (it comes first). Its rect.top is at or below the bottom of td.col-nom.
    - td.col-niv's data-label is 'Nivell' (CA) / 'Nivel' (ES), and its ::before content contains that label.
    - td.col-codi's computed font-family contains 'Geist Mono'.
    - Focus the first tr.row-link with el.focus(). It must match :focus-visible and compute outline-style solid, outline-width 2px, and background-color equal to the computed --warm2.
    - Then set filterGrado to 'E' and currentPage to 1 via Alpine.$data and wait for grade-E rows. At least one row td must have text '—', and every such td must compute display none.
  - fpo: 390, CA, user, ?mode=fpo. table.results-table--fpo exists. Its thead is visually hidden, its first main row computes display flex, and its .table-wrap does not overflow. td.col-hores and td.col-ncursos carry data-label 'Hores' and 'Cursos actius', and td.col-niv carries 'Nivell'. The first meta cell td.col-codi has ::before content 'none'. For fixture 2, the level and hours tds compute display none.
  - topbar: for 320/390/768 x CA/ES x guest/user (fine):
    - .topbar-inner has scrollWidth <= clientWidth.
    - Every .auth-btn and .lang-btn lies inside the .topbar-inner rect on both sides.
    - Every .auth-btn has height < 36 and scrollWidth <= clientWidth (one line, no nowrap spill).
    - For user, .auth-greeting computes display none.
    - The .lang-selector right edge equals the .topbar-inner right edge (right-aligned on one line or when wrapped).
  - toggle: for 320/390 x CA/ES (fine): .mode-toggle computes flex-direction column. Every button's width is at least the toggle clientWidth minus 1, its height is <= 46 (single line), and its computed min-height is 44px.
  - touch: at 390 CA user with touch=True, and again after setting filterGrado 'B' so .badge-itinerari-b[role=button] exists:
    - .btn-fav ::after computes width and height 44px, while the button rect height stays <= 24 and its svg rect stays 16x16.
    - .badge-centres and .badge-itinerari-b[role=button] ::after compute height 44px, while each badge's own rect height stays <= 24.
    - Every visible .lang-btn, .auth-btn, select, .save-alert-btn, .clear-btn, .mode-toggle button, footer a and .checkbox-label has rect height >= 43.5.
  - overflow: for 320/390/768 x CA/ES x guest/user in name mode, plus 320 and 390 CA user in FPO mode:
    - Inject a style that sets html, body and [x-data] to overflow-x visible !important and max-width none !important, then test document.documentElement.scrollWidth <= innerWidth. Otherwise collect up to 10 offenders: visible elements whose rect.right > innerWidth + 0.5, excluding descendants of .grau-tabs, thead and .sr-only.
    - Then read the working-tree index.html. The rule is present if a line matches optional whitespace, then [x-data], then optional whitespace and an opening brace.
    - Gate PASS with the rule present is a failure: "overflow gate passes — remove the html/body/[x-data] rule".
    - Gate FAIL with the rule absent is a failure listing the offenders.
    - Gate FAIL with the rule present prints OVERFLOW_GATE=FAIL plus the offenders and is NOT a failure (the rule is kept by design).
    - Gate PASS with the rule absent prints OVERFLOW_GATE=PASS.
  - Organisation is at your discretion. Sharing one page load across topbar/toggle/overflow per scenario is encouraged, but run overflow last, because its injected style changes layout.

Part B — main-table cards (scope item 1), in index.html:
- Markup, main table only:
  - On td.col-codi, add :class binding cell-empty when row.codigo == null.
  - On td.col-niv, add :class binding cell-empty when row.nivel == null, plus :data-label="t('index.col.niv')".
  - Keep the static classes and x-text exactly as they are.
  - Add no i18n keys.
- In the EXISTING 768px block, delete the three table rules at ~851–865 (the .table-wrap horizontal scroll, the .results-table min-width 560px, and the th/td.col-nom padding-left and min-width). Leave the html/body/[x-data] rule alone for now; the Task 2 gate decides it.
- Add a NEW @media (max-width: 768px) block immediately before </style>, commented "Mobile ≤768: cards, top bar, mode toggle". In it:
  - .results-table: position relative. The table, every tbody and every tr: display block. Never !important, so x-show's inline display:none still wins.
  - .results-table thead: visually hidden with the same declarations as the existing .sr-only class (absolute, 1px, overflow hidden, clip, nowrap, border 0, margin -1px), plus clip-path inset(50%). It stays in the DOM for assistive tech.
  - Card row selector .results-table tbody tr:has(> td.col-nom): display flex; flex-wrap wrap; align-items baseline; row-gap 6px; padding 14px 16px; border-bottom 1px solid var(--border). Background var(--warm2) on :hover, on .row-fav and on .row-link:focus-visible, so the tint covers the whole card, including gaps. The existing tr focus-visible outline rule keeps working unchanged. Do not override outline.
  - Every td in a card row: display block; padding 0; border-bottom none; text-align left; min-width 0; max-width none; width auto; white-space normal; overflow visible. This neutralises the base col-fam nowrap/min-width 190px and the col-grau/col-niv min-widths.
  - td.col-nom: flex 1 1 100% (full-width first line). Keep its weight 500.
  - Meta cells (every card td except col-nom): font-size 13px; color var(--warm); margin-left 4px.
  - Separator: every meta cell's ::before renders the text "· " in the DM Sans stack at 13px, so the mono code cell does not widen it. A td that has data-label renders "· ", then attr(data-label), then a space.
  - Hide td.cell-empty and td:empty in card rows (display none).
  - Scoped to #results-table: order 1 for td.col-grau, 2 for td.col-niv, 3 for td.col-codi, 4 for td.col-fam.
  - Grade tag, #results-table td.col-grau:
    - Box: border 1px solid var(--dark); border-radius 3px (literal: no radius tokens exist); padding 1px 6px.
    - Type: font-size 11px; font-weight 600; line-height 1.4; color var(--dark).
    - Spacing and separator: margin-left 0; no ::before (content none).
  - td.col-codi keeps its existing Geist Mono 12px Umber. Do not set font-family on meta tds.
  - td[colspan] (detail, centres-panel and empty rows): display block. Inside this block, .results-table .detall-inner gets padding 16px (full-width detail block with 16px padding and no double padding). .centres-panel td keeps its existing padding 0 !important.
- Colours only via var(--token). Add no hex values and no new translucent colour functions (the rgba count must stay equal to base).
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && python3 .planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py --only cards,desktop && rg -q ":data-label=\"t\('index\.col\.niv'\)\"" fp-cercador/frontend/index.html && test "$(rg -c 'cell-empty' fp-cercador/frontend/index.html)" -ge 3 && ! rg -q 'min-width: 560px' fp-cercador/frontend/index.html && echo T1_OK</automated>
  </verify>
  <done>The harness exists and runs every group on demand. The cards and desktop groups pass: main-table rows are cards at 320/390/768 in CA and ES, with the grade tag first, the level label, hidden '—' cells and a visible focus outline. Desktop at 1280 and 800 is pixel-identical to 9b3edf2. No Alpine or page errors occur. Run the harness with a Bash timeout of about 300000 ms.</done>
</task>

<task type="auto">
  <name>Task 2: FPO cards, top bar, stacked mode toggle, then the overflow-x gate decision</name>
  <files>fp-cercador/frontend/index.html</files>
  <read_first>fp-cercador/frontend/index.html lines ~59–78 and ~172–199 (topbar and mode-toggle base), ~1343–1418 (auth and lang base), ~2295–2320 (top bar and mode toggle markup), ~3004–3046 (FPO table markup)</read_first>
  <action>
FPO table markup (scope item 1, markup allowed because CSS cannot label the cells otherwise):
- Add class results-table--fpo to the FPO <table class="results-table">.
- On the FPO main-row tds:
  - Area td: add class col-area.
  - Hours td: add class col-hores, :data-label="t('fpo.col.hores')" and :class cell-empty when !esp.hores.
  - nCursos td: add class col-ncursos and :data-label="t('fpo.col.ncursos')".
  - td.col-niv: add :data-label="t('fpo.col.nivell')" and :class cell-empty when !esp.nivell.
- Keep every x-text unchanged.
- Discretion choice, documented here: the hours and course-count labels are included because a bare '320 · 3' is meaningless once the header row is hidden.

In the end-of-sheet 768px block from Task 1, add:
- FPO: .results-table--fpo td.col-codi::before content none. The code is the first meta cell and gets no separator; its margin-left is 0. All other FPO meta cells use the generic card, separator and label rules from Task 1 in DOM order (no order values; the order rules are scoped to #results-table).
- Top bar (scope item 2):
  - .topbar-inner: flex-wrap wrap; height auto; min-height 52px; padding-block 4px; row-gap 6px; column-gap 8px; justify-content flex-end.
  - .topbar-logo: margin-right auto. The logo stays left even when it sits alone on a line; justify-content flex-end right-aligns a wrapped auth group or CA/ES.
  - .auth-greeting: display none. It stays in the DOM, and auth.js is untouched.
  - #auth-widget: gap 6px (keep its margin-left auto).
  - .auth-btn: white-space nowrap; padding 5px 10px.
  - .lang-selector: margin-left 8px (no auto margin).
  - Change nothing else in the top bar and no colours.
- Mode toggle (scope item 3):
  - .mode-toggle: flex-direction column; gap 8px.
  - .mode-toggle button: width 100%; min-height 44px; padding 8px 12px.
  - Do not add nowrap or ellipsis; full width is what yields one line. If the toggle group still reports a two-line label at 320 ES, reduce the horizontal padding to 8px. Keep the 14px/500 type.

Overflow-x gate (scope item 2): run the harness with --only overflow.
- If it reports that the gate passes and the rule should go, delete the whole html, body, [x-data] rule (max-width 100vw and overflow-x hidden) at the top of the EXISTING 768px block, then re-run until OVERFLOW_GATE=PASS.
- If it prints OVERFLOW_GATE=FAIL with the rule present, keep the rule and copy the offender list into SUMMARY.md under "overflow-x kept because". Do NOT fix out-of-scope offenders such as long FPO select options: that would expand scope.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && python3 .planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py --only cards,fpo,topbar,toggle,overflow,desktop && rg -q 'results-table--fpo' fp-cercador/frontend/index.html && rg -q ":data-label=\"t\('fpo\.col\.hores'\)\"" fp-cercador/frontend/index.html && rg -q ":data-label=\"t\('fpo\.col\.ncursos'\)\"" fp-cercador/frontend/index.html && rg -q ":data-label=\"t\('fpo\.col\.nivell'\)\"" fp-cercador/frontend/index.html && echo T2_OK</automated>
  </verify>
  <done>The FPO rows are labelled cards. The top bar fits or wraps without clipping in all 12 scenarios. The mode buttons stack full-width on one line at ≥44px. The overflow rule is removed and the gate passes, or it is kept with the offenders recorded. Desktop parity still passes.</done>
</task>

<task type="auto">
  <name>Task 3: 44px touch targets under pointer:coarse, full verification and the single commit</name>
  <files>fp-cercador/frontend/index.html</files>
  <read_first>fp-cercador/frontend/index.html lines ~417–483 (checkbox-label, clear-btn, save-alert-btn), ~957–1055 (btn-fav, badge-centres, badge-itinerari-b), ~3218–3229 (footer)</read_first>
  <action>
Scope item 4: add an @media (pointer: coarse) block AFTER the end-of-sheet 768px block and immediately before </style>, commented "Touch: 44px targets". It applies at every width. In it:
- .lang-btn, .auth-btn, .save-alert-btn, .clear-btn and footer a: display inline-flex; align-items center; justify-content center; min-height 44px. Footer links keep their inline font, colour and margin.
- select and .mode-toggle button: min-height 44px.
- .checkbox-label (it covers .checkbox-label--favs and the radio labels): min-height 44px. It is already flex or inline-flex with align-items center, so the whole label row is the 44px tappable area.
- .btn-fav: position relative. Its ::after: content empty string; position absolute; left 50%; top 50%; width 44px; height 44px; translate(-50%, -50%). Leave the existing padding and the 16px svg alone, so there is no visual size jump. The existing (hover: none) opacity rule stays.
- .badge-centres and .badge-itinerari-b[role="button"]: position relative. Their ::after: content empty string; position absolute; left 0; right 0; top 50%; height 44px; translateY(-50%). Do not change padding, font, border or display, so the look is unchanged.
- No colours, no hex values, and no new translucent colour functions.

Then run the full harness with no --only flag and fix any regression. Run the static gates in the verify command below.

Commit from the git root /Users/rogermasellas/AI/Cercador Graus:
- Stage by path only: git add fp-cercador/frontend/index.html. Do not stage the harness, i18n.js or any other file.
- Commit with exactly this subject and no body or trailer: feat(index): adaptació mòbil — taula en targetes, barra superior, modes apilats i àrees tàctils de 44px
- Owner rule: never add any attribution trailer. Do not push or deploy.

In SUMMARY.md, record:
- the overflow-x decision (with offenders if kept);
- that sorting headers are unavailable in the card view (thead visually hidden, as approved);
- that screen readers may lose table semantics in some engines once table/tr/td are display block/flex (accepted per the approved "otherwise accept"; no ARIA roles added);
- that .grau-tabs buttons, .pagination buttons, .btn-doc and .watch-btn were intentionally not resized (outside the approved list);
- that the orchestrator's real-browser pass at 320/390/768/1280 remains authoritative, because the harness uses mocked FPO data.
  </action>
  <verify>
    <automated>cd "/Users/rogermasellas/AI/Cercador Graus" && F=fp-cercador/frontend/index.html && python3 .planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py && test "$(rg -c '@media \(pointer: coarse\)' $F)" -eq 1 && test "$(rg -c '@media \(max-width: 768px\)' $F)" -eq 2 && test -z "$(awk '/<style>/,/<\/style>/' $F | rg '#[0-9a-fA-F]{3,6}\b' | rg -v '^\s*--[a-z0-9-]+: #[0-9a-fA-F]{3,6};\s*$')" && test -z "$(awk 'f;/<\/style>/{f=1}' $F | rg '#[0-9a-fA-F]{3,6}\b')" && BASE_SRC="$(git show 9b3edf2:$F)" && test -n "$BASE_SRC" && test "$(rg -c 'rgba\(' $F)" -eq "$(printf '%s' "$BASE_SRC" | rg -c 'rgba\(')" && test "$(awk '/<style>/,/<\/style>/' $F | tr -cd '{' | wc -c)" -eq "$(awk '/<style>/,/<\/style>/' $F | tr -cd '}' | wc -c)" && CHANGED="$(git show --name-only --format= HEAD)" && test "$CHANGED" = "$F" && DIRTY="$(git diff --name-only -- fp-cercador)" && test -z "$DIRTY" && SUBJ="$(git log -1 --format=%s)" && test "$SUBJ" = 'feat(index): adaptació mòbil — taula en targetes, barra superior, modes apilats i àrees tàctils de 44px' && BODY="$(git log -1 --format=%B)" && test -n "$BODY" && ! printf '%s' "$BODY" | rg -qi 'co-authored|claude-session|generated with' && echo ALL_GATES_OK</automated>
  </verify>
  <done>Every harness group passes (touch included) and prints MOBILE_CHECK_OK. The static gates pass: one coarse block, two 768px blocks, no new hex or translucent colours, balanced braces. HEAD is one commit touching only fp-cercador/frontend/index.html, with the exact Catalan subject and no attribution. The working tree is clean under fp-cercador/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n dictionary → DOM attribute → CSS content | Static, repo-owned strings are bound into data-label and rendered by CSS attr(), which only ever renders text |
| harness route handler → local filesystem | A test-only mock server maps request paths to files under fp-cercador/frontend |
| working tree → git history | The commit must contain only the intended file and no attribution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0s3-01 | Tampering | :data-label bindings / CSS attr() | low | accept | Values come from the static i18n.js dictionary, not user input. CSS content never parses HTML, so there is no injection path |
| T-0s3-02 | Information disclosure | mobile_check.py route handler | low | mitigate | Resolve the request path and serve only when it stays inside fp-cercador/frontend (otherwise 404). Abort all non-test hosts except Google Fonts. The mock identity is the fake test@local.dev with no real credentials |
| T-0s3-03 | Tampering | git commit scope | medium | mitigate | Stage by explicit path only. The verify gate asserts HEAD touches exactly fp-cercador/frontend/index.html and the message has no attribution trailer |
| T-0s3-04 | Denial of service (UX) | tr display rules vs Alpine x-show | medium | mitigate | No !important on tr display, so x-show's inline display:none keeps hiding detail rows. The harness cards/fpo groups and Alpine error capture prove the rows render and toggle state |
| T-0s3-SC | Tampering | package installs | low | accept | No packages are installed. playwright and PIL are pre-installed, the browser is the system Chrome via channel=chrome, and running playwright install or pip install is forbidden |
</threat_model>

<verification>
- `python3 .planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/mobile_check.py` (from the git root) prints MOBILE_CHECK_OK and exits 0. The groups are desktop parity vs 9b3edf2, cards, fpo, topbar, toggle, touch and the overflow gate.
- The static gates of Task 3 pass: exactly one `@media (pointer: coarse)`; exactly two `@media (max-width: 768px)` (the original block plus the new end-of-sheet block); hex only in :root token lines and none in markup; the translucent-colour count equals base; braces are balanced.
- The overflow-x decision is consistent with the gate: rule removed and OVERFLOW_GATE=PASS, or rule kept with the offenders listed in SUMMARY.md.
- The orchestrator's follow-up real-browser pass at 320/390/768/1280 (real data, real fonts) is authoritative for the visual result.
</verification>

<success_criteria>
- At 390px no key fact is off-screen. Each result is a readable card with grade and level visible without horizontal scrolling. The same holds at 320px and 768px, and in FPO mode.
- The top bar and mode toggle never clip or wrap labels awkwardly in CA or ES, logged in or out.
- On touch devices every listed control or hit area is ≥44px tall without visual size jumps.
- Desktop (>768px, fine pointer) is pixel-identical to 9b3edf2.
- One commit on master touches only fp-cercador/frontend/index.html, with the exact subject and no attribution. Nothing is pushed.
</success_criteria>

<output>
Create `.planning/quick/260924-0s3-adapt-index-html-for-mobile-table-to-car/260924-0s3-SUMMARY.md` when done
</output>
